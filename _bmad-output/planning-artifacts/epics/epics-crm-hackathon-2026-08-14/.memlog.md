---
topic: Kế hoạch dựng Why Now theo sáu cục song song
updated: 2026-08-14T17:24
---

- (decision) Căn cứ ưu tiên GIÁ TRỊ là *thiệt hại nếu không làm* — người dùng chọn. Trượt một điểm T-1…T-10 hoặc hỏng D37 là mất trắng, không gỡ lại trong ngày thi; ấn tượng giám khảo hỏng một phần vẫn còn phần khác.
- (decision) Cắt việc theo NĂM TẦNG kiến trúc chạy song song, mỗi cục nội bộ tuần tự — người dùng chọn. Nút thắt của đội là đụng độ tệp chứ không phải giờ-người, vì dev dùng AI chạy nhiều đầu việc song song.
- (decision) Thêm Cục 0 đóng băng hợp đồng, TUẦN TỰ và không né được. Lý do: chuỗi ⑤←④←③←②←① không song song hoá được ở phần hiện thực, chỉ song song hoá được ở phần hợp đồng — Interface Analysis `10.24`.
- (decision) Cục 0 đóng băng ở mức CHỮ KÝ KIỂU + THÂN RỖNG — người dùng chọn. Năm cục biên dịch được ngay từ phút đầu, `tsc` bắt lệch hợp đồng tức thì; đổi lấy 60–90 phút tuần tự trước.
- (decision) Cục ⑤ xếp nội bộ theo CÁI MỞ KHOÁ NHIỀU NHẤT — người dùng chọn. Company → Contact → Opportunity trước, mỗi thực thể xong là một đèn xanh cho các cục trên.
- (decision) Nhịp là BA MỐC theo trạng thái chạy được, không theo giờ — người dùng chọn. Trượt giờ thì cắt phạm vi, không dời mốc.
- (decision) Viết lại bộ story; dải E1-S1…E7-S8 của Mức 3 xuống làm tham chiếu tiêu chí nghiệm thu qua cột *Kế thừa*. Lý do: dải cũ cắt theo nhóm tính năng, đội cắt theo tầng — giữ cả hai làm đơn vị giao là hai sổ công việc cho một đội.
- (decision) Quy tắc gán T: mỗi điểm nghiệm thu thuộc cục chứa MẮT XÍCH CUỐI của nó, tức cục mà khi xong thì phép kiểm chuyển xanh. Bỏ tiêu chí *cục liên quan nhiều nhất* vì tiêu chí đó không quyết được.
- (constraint) Bốn tệp tranh chấp đóng băng ở Cục 0, sau đó chỉ đọc: `src/core/errors.ts` (cả sáu cục muốn thêm mã lỗi), `prisma/schema.prisma`, `src/config.ts`, `package.json`.
- (constraint) `tests/` KHÔNG nằm được trong cục ① như bản đồ tầng ghi: T-1…T-10 gọi xuyên cả năm tầng. Chữ ký lời gọi thuộc Cục 0 (`C0-9`); thân phép kiểm về cục sở hữu mắt xích cuối.
- (event) Sửa máy trạng thái `src/core/opportunity/stage.ts`: bản trước CẤM lùi giai đoạn và nhảy cóc, còn nêu đích danh hai thứ đó như nhóm bị cấm — PRD §5.1 dòng 2 và FR-4 cho phép cả hai. Đã quét vét cạn 49 cặp so oracle chép tay từ §5.1, 17/17 phép kiểm xanh.
- (event) Ba lỗi khác cùng tệp: (1) `changeStage` chặn cả `tam_dung` nên `tam_dung`→`thang`/`thua` bị bác, trong khi A1 đã xác nhận đường đó hợp lệ; (2) `latestOpenStage` gán thẳng `current.stage` nên `tam_dung`→`thua` ghi `tam_dung` vào cột, vi phạm §3.1 và bị `CHECK opp_latest_open_stage_running` bác; (3) `resumeOrReopen` thiếu cả chặn máy (NFR-14) lẫn giới hạn vai Quản trị (D43).
- (event) `BR-D3` bị dùng SAI làm mã lỗi giai đoạn — PRD §7.1 định nghĩa nó là luật Bản lưu ↔ Công ty. Bảng chuyển tiếp không có mã BR-D nào; đã thêm `PRD-5.1`, `D43`, `D44` vào `BusinessRuleCode`, cả ba tra được ở thượng nguồn.
- (assumption) Override `_bmad/custom/bmad-create-epics-and-stories.toml` khai `doc_standards`, nhưng skill CE chỉ phơi bốn key và KHÔNG đọc key đó — hai chỉ thị rà soát cuối là mã chết. Lần này áp bằng tay. Sửa đúng chỗ là chuyển chúng sang `on_complete`.
- (event) Chạy bmad-review 5 lượt (adversarial × 2, edge-case-hunter × 2, verification-gap × 1) trên epics.md và trên src/. Bỏ structure/prose vì hỏi góc kỹ thuật; babok-solution-eval tự loại trên kế hoạch.
- (change) SỬA SAI CỦA CHÍNH BẢN VÁ TRƯỚC: `resumeOrReopen` đã nhận `role` để tự canh `D43`. `AD-CR-10` đã CÂN NHẮC VÀ LOẠI đúng phương án đó, nguyên văn *'hai điểm kiểm quyền là hai chỗ trôi khỏi nhau, và PRD §6 đã kết luận cần đúng MỘT điểm kiểm'*. Đã gỡ `role` khỏi lõi; `D43` về Cổng bước ⑤.
- (change) Ba mã lỗi `PRD-5.1`/`D43`/`D44` tôi tự đặt bị thay bằng ba mã `AD-CR-1` đã đặt tên sẵn: `STATE_TRANSITION_NOT_ALLOWED`, `STATE_INITIAL`, `STATE_TARGET_FIXED`. Lý do: `tests/` khẳng định theo mã, nên đặt tên khác spine là làm spine thành tài liệu chết.
- (change) `changeStage`/`resumeOrReopen` nhận `actor: Actor` thay cho cờ `byMachine`. Cờ đó (a) cho phép khai trạng thái bất khả `{byMachine:true, role:'admin'}`, (b) bỏ quên nhánh thứ ba `kind:'seed'`. Chốt nay là `!isHuman`, nên `seed` cũng không đổi được Giai đoạn.
- (change) Thêm kiểu hẹp `RunningStage`; `isRunning` thành vị từ thu hẹp kiểu. Trước đó kiểu TS (`Stage | null`) LỎNG HƠN `CHECK opp_latest_open_stage_running`, nên bất biến chỉ được chú thích giữ.
- (change) `resumeOrReopen` phân nhánh theo `stage` (qua `canResume`) thay vì theo `latestOpenStage`. Bản trước hỏi cột hệ quả, nên với hàng hỏng `{stage:'thuong_luong', latestOpenStage:'tiep_can'}` nó thực hiện một chuyển tiếp im lặng không qua bảng §5.1.
- (change) `assertInvariant` kiểm bất biến hai chiều ở lối vào CẢ HAI hàm. Trước đó `nextLatestOpenStage` ghi đè giá trị hỏng bằng `from`, nên hàng hỏng tự lành và nguồn gây hỏng không bao giờ lộ.
- (change) `metrics.unclassifiedRatio` đếm sai đại lượng: dùng `signalType='other'` trong khi `BR-B5` định nghĩa theo `signalSubtype='unclassified'`. `other` gộp cả 12 phân loại phụ THÀNH CÔNG (rfp, partnership, m_and_a…), nên báo động bật trên dữ liệu lành. Đã sửa.
- (change) `normalize` XOÁ U+3000 dù chú thích bước ④ nói không đụng tới nó: `String.prototype.trim()` coi U+3000 là khoảng trắng theo định nghĩa ECMAScript. Đã đo: `('x'+U+3000).trim()` trả `'x'`. Thay bằng cắt tường minh `[ \t]` và `[ \t\n]`.
- (event) Phép quét lên 24/24, thêm ca: `seed` bị chặn, `from===to` trên giai đoạn đã đóng, ba mã dùng đúng chỗ, hàng hỏng bị bắt thay vì tự lành, U+3000 sống sót ở đầu/cuối chuỗi và đầu/cuối mỗi dòng.
- (question) CHƯA SỬA — lược đồ thiếu ba thứ mà story cục ⑤ cần, và `prisma/schema.prisma` đã bị tuyên bố đóng băng: `Contact` không có cột đầu mối chính (`BR-D4`); `Activity` không có `contactId` (`E1-S6`); `NextAction.content`/`dueDate` là NOT NULL, ngược với `BR-B1` *'thiếu một trong hai vẫn lưu được'*.
- (question) CHƯA SỬA — `next_action_opportunity_id_key` do Prisma sinh, thiếu `WHERE deleted_at IS NULL`. Cùng tệp migration đã nêu đích danh cái bẫy này cho `suggestion` và `user.email`. Hệ quả: xoá mềm Việc tiếp theo rồi tạo cái mới sẽ ăn unique violation — chạm thẳng `T-6`/`T-7`.
- (question) CHƯA SỬA — `FR-49` đăng nhập và phiên (`E7-S6`) và `E7-S5` cấu hình giả lập production không story nào phủ. Cột *Kế thừa* của epics.md còn ánh xạ lệch bốn chỗ, trong đó `C5-17` ghi `E7-S8` (Grafana) trong khi đúng phải là `E7-S7` (nạp dữ liệu một lệnh).
- (question) CHƯA SỬA — hai mâu thuẫn nội tại trong epics.md: `D37` vừa nằm trong điều kiện xanh của M3 vừa được tuyên bố *'không nằm trong mốc nào'* ba dòng sau; và cột *Cắt gì nếu trượt* của cả M2 lẫn M3 đều cắt đúng bề mặt mà `T-9`/`T-8`/`T-4` cần.
- (decision) Sửa THẲNG vào `20260814083017_init` thay vì thêm migration thứ hai — người dùng chọn, và đã đồng ý rõ ràng cho `migrate reset` trên CSDL demo 5442. Giữ đúng MỘT migration sạch cho giám khảo đọc.
- (change) Lược đồ: thêm `contact.is_primary` (`BR-D4`), thêm `activity.contact_id` + khoá ngoại (`E1-S6`), cho `next_action.content`/`due_date` NULLABLE (`BR-B1` đòi *thiếu một trong hai vẫn lưu*, NOT NULL làm luật đó bất khả thi).
- (change) Thay `next_action_opportunity_id_key` (Prisma sinh, thiếu vế `deleted_at IS NULL`) bằng chỉ mục một phần `next_action_one_active`. Hệ quả cũ: xoá mềm Việc tiếp theo thì ô bị khoá VĨNH VIỄN, và `T-7` bấm Hoàn tác lần hai ăn unique violation.
- (change) Thêm chỉ mục một phần `contact_one_primary`. Giao dịch cho tính NGUYÊN TỬ, chỉ mục cho tính DUY NHẤT — hai giao dịch cùng đọc *chưa có ai* rồi cùng ghi sẽ tạo hai Đầu mối chính mà không bên nào lỗi.
- (change) `Opportunity.nextAction NextAction?` thành `nextActions NextAction[]`. Bỏ `@unique` phá quan hệ 1-1, và một-nhiều mới là mô hình ĐÚNG: xoá mềm để lại lịch sử nhiều hàng, đúng một hàng sống, ràng buộc thật nằm ở chỉ mục một phần.
- (event) Kiểm HÀNH VI 9 ca qua psql trên CSDL thật: hai Đầu mối chính → lỗi; người thứ hai không phải đầu mối → OK; xoá mềm rồi đặt người mới → OK; NextAction thiếu cả hai ô → OK; hai NextAction đang sống → lỗi; xoá mềm rồi tạo lại → OK; Activity gắn Người liên hệ → OK; gắn người không tồn tại → lỗi khoá ngoại; Activity không gắn ai → OK. `migrate diff` báo không lệch.
- (change) Thêm `C1-16` đăng nhập và phiên (`FR-49`/`E7-S6`) và `C1-17` bản dựng production (`E7-S5`) vào cục ①, cả hai vào M1 — người dùng chọn. Đăng nhập là nguồn của `actor` cho MỌI dòng ghi vết, làm sau thì phải sửa lại mọi đường gọi.
- (change) `D37` tách thành mốc M0 riêng, cò súng 22:00 ngày 14/8. Bản trước vừa đặt nó trong điều kiện xanh của M3 vừa tuyên bố *không nằm trong mốc nào* ba dòng sau.
- (change) Gắn giờ tuyệt đối cho cả bốn mốc làm CÒ SÚNG cắt phạm vi. Lý do bác Estimation `10.19` ở §5 không áp vào đây: đây là mốc lịch, không phải ước lượng vận tốc.
- (change) Viết lại hai đường cắt vì bản trước tự đánh đỏ điểm nghiệm thu: cắt trọn `C1-14` làm `T-9` mất nút bấm (nút tắt AI ngồi trên màn hình Quản trị); giảm `C1-13` xuống một chu kỳ thủ công phá cả `T-8` (*không ai bấm gì*), `T-4` (*ít nhất ba chu kỳ*) và `T-9` (*bấm tắt trong lúc đang chạy*). Thay bằng cắt ba khối SỐ ĐO, và rút chu kỳ xuống 20 giây.
- (change) `T-4` và `T-9` chuyển từ cục ③ sang ①: quy tắc *mắt xích cuối* nói vậy — cả hai cần vòng quét và nút bấm, mà ③ không sở hữu pixel nào. `T-10` tách thành `T-10a` (lõi, ⑤) và `T-10b` (sổ đăng ký, ④), để lộ ra rằng gần trọn cục ④ cũng nằm trong M1.
- (change) Bảng tệp đóng băng mở từ 4 lên 9 mục: thêm chính các tệp hợp đồng Cục 0 sinh ra. Ràng buộc thật là HỢP ĐỒNG, không phải đụng độ tệp — không có dòng này thì cục ② sửa `src/agent/types.ts` đơn phương và bốn cục kia biên dịch với một kiểu đã đổi nghĩa.
- (change) Nhét 16 ca biên CHẠM T vào tiêu chí nghiệm thu: cascade giữ Phát hiện làm bằng chứng · duy nhất bằng chỉ mục · `from===to` là no-op · cờ Đủ điều kiện theo giai đoạn hiện tại chứ không theo sự kiện (nhảy cóc) · hàng `timeline` tạo cùng Công ty · lối ra thứ tư của Gợi ý (`co_goi_y_moi_hon`) · Hoàn tác khi ô vốn trống và bấm hai lần · `FR-34` kiểm-và-ghi nguyên tử · khoá `ai_enabled` · tập cột của *y nguyên* · hạn thuê cho `ScanLog` · gieo phải luỹ đẳng thật.
- (change) Sửa bốn ánh xạ *Kế thừa* lệch: `C5-17` `E7-S8`→`E7-S7`, `C4-2` `E7-S3`→`E7-S1`, `C1-11` `E4-S3`→`E4-S2`, `C5-15`/`C1-10` `E4-S4`→`E4-S3`. Cơ chế truy vết tự dựng ra đang báo xanh nhầm.
- (change) Thêm `R7` (tính năng BTC phát thêm sáng 15/8 — `D41` đã giữ trống nửa quỹ giờ, kế hoạch trước phân bổ 100%) và `R8` (không có bảng phân công người, trong khi luật *tệp sở hữu* giả định mỗi thư mục một người ghi).
- (change) §5 sửa khẳng định sai: sáu tệp lõi là *đã viết*, KHÔNG phải *đã kiểm*. Chỉ `stage.ts` và `normalize.ts` có phép kiểm, và hai trong bốn tệp còn lại vừa lộ lỗi thật khi rà.
- (change) Mở rộng `_bmad/custom/bmad-build.toml`: thêm bảng ĐỊNH TUYẾN ARTIFACT, bảng TẦNG (thư mục → spine → tiền tố AD), luật TRUY VẾT NGƯỢC, và thứ tự phân xử AD-13. Lý do: bmad-build có sẵn cơ chế link — `context:` ở frontmatter spec, subagent tự nạp ở step-03 — nhưng KHÔNG bước nào nói điền gì vào đó; grep `context:` trong step-02-plan.md trả rỗng.
- (decision) Chỉ MỘT `file:` nạp sẵn (`epics.md`, 313 dòng). Đo được: nạp trọn PRD + 6 spine + UX + Mục 0 + ontology là 10.303 dòng vào ngữ cảnh MỌI lần chạy Build, kể cả lần sửa một dòng CSS. Phần còn lại định tuyến theo nhu cầu bằng văn bản.
- (change) Thêm lớp rà thứ tư `traceability` vào `[[workflow.review_layers]]` — kiểm mỗi thay đổi trỏ về mã thượng nguồn nào, mã đó tra được không, và có nói đúng thứ mã nguồn đang làm không. KHÔNG thêm vào `oneshot_review_layers`: đường đó theo định nghĩa là bán kính bằng không.
- (event) Kiểm merge: 10 persistent_facts, đúng 313 dòng nạp sẵn, review_layers thành [blind-hunter, edge-case-hunter, verification-gap, traceability], oneshot giữ nguyên. Path-check 12/12 đường dẫn tồn tại; năm tiền tố AD-UI/AG/GT/CP/CR có thật ở đúng spine; S0–S11 có trong EXPERIENCE.md.
- (change) Sửa số cũ trong override: `D1`–`D41` thành `D1`–`D47` (D46–D47 thêm ngày 14/8).
- (change) Mở rộng `bmad-code-review.toml` và `bmad-qa-generate-e2e-tests.toml` cùng khuôn với build: bảng định tuyến artifact, bảng tầng, luật dẫn nguồn cho mỗi phát hiện/khẳng định. Code-review thêm lớp `traceability` (giới hạn `when review_mode=full`); QA nạp sẵn `epics.md` vì tiêu chí nghiệm thu CHÍNH LÀ nguồn của test.
- (change) QA thêm hai luật mới: (1) `BR-D` và `BR-B` sinh HAI LOẠI khẳng định ngược nhau — viết khẳng định `BR-B` theo kiểu *phải bị chặn* là làm test đỏ trên hành vi ĐÚNG; (2) `T-4` phải nêu tên tập cột đem so, loại `updated_at`, nếu không đỏ vì đúng hành vi `T-8` đòi.
- (event) Kiểm merge cả ba: build 10 facts/313 dòng/4 lớp · code-review 8 facts/0 dòng/5 lớp · qa 17 facts/313 dòng. `on_complete` cả ba đều được đọc — build ở step-05-present.md:75, code-review ở steps/step-04-present.md:130, qa ở SKILL.md:174. Không có key chết.
