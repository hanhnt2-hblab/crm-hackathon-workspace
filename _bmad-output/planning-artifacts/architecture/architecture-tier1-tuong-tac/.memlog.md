---
scope: Tầng ① Tương tác — src/app, src/scan, src/ingest, tests
purpose: Spine epic-altitude, thừa kế AD-1..AD-22
altitude: epic
updated: 2026-08-14T13:22
---

- (constraint) AD-1 (cha) — mọi đường tới lõi đi qua Capability, mọi Capability đi qua Cổng. Ranh giới nhập là DANH SÁCH CHO PHÉP quét toàn repo: src/app KHÔNG được nhập @prisma/client và KHÔNG được nhập src/core, kể cả để ĐỌC. tests/ được đọc thẳng Prisma, chỉ đọc.
- (constraint) AD-2 (cha) — sổ đăng ký Capability là tập ĐÓNG, ba khối CAP_MAY_DUOC (đúng chín) / CAP_CHI_NGUOI / CAP_HE_THONG_NOI_BO. Thêm màn hình mới thì thêm capability ĐỌC vào khối hai, không làm đỏ T-10.
- (constraint) AD-3 (cha) — máy có đúng ba chạm ghi vào Hồ sơ chính thức: appendTimelineEntry, setNextAction, disableAi. Không có chạm thứ tư.
- (constraint) AD-4 (cha) — Cổng có chuỗi bảy bước cố định, là hàm THUẦN; sổ đăng ký gom ngữ cảnh và ghi vết. createGate({seedMode}) là tham số DỰNG: prisma/seed.ts true, instrumentation.ts và src/app false, tests/ true chỉ trong tiện ích dựng dữ liệu nền.
- (constraint) AD-5 (cha) — actor là tham số ĐẦU TIÊN của mọi capability, ba nhánh human|system|seed. Không đọc từ session, biến toàn cục, hay AsyncLocalStorage.
- (constraint) AD-6 (cha) — src/agent không nhập src/core; cả hai đọc ontology. JSON Schema khai draft-07.
- (constraint) AD-7 (cha) — ra biên đúng ba thứ (Bản lưu chuẩn hoá, Loại công ty, danh sách enum), chốt tại một hàm.
- (constraint) AD-8 (cha) — bốn nguyên thuỷ; Claim buộc neo câu trích, đích so khớp là bản CHUẨN HOÁ.
- (constraint) AD-9 (cha) — BR-D là ràng buộc (ném lỗi), BR-B là cờ (vẫn ghi thành công). BR-B5/BR-B6 tính lúc đọc qua src/core/metrics.ts; src/app lấy số qua capability readMetrics.
- (constraint) AD-10 (cha) — máy ghi bằng kiểm-và-ghi nguyên tử; vế WHERE lặp TOÀN BỘ vị từ của AD-3; 0 dòng thì bỏ lượt ghi, không thử lại.
- (constraint) AD-11 (cha) — giới hạn nằm trong bảng settings; NĂM điều kiện dừng, mỗi cái một dòng Nhật ký kèm mã; ba subtype SDK ánh xạ về chúng; đơn vị công việc là MỘT CÔNG TY TRỌN VẸN; lượt bị Cổng từ chối không tính vào trần 20 nhưng có trần max_consecutive_denials riêng.
- (constraint) AD-12 (cha) — vòng quét chốt tập Công ty lúc bắt đầu; điểm vào worker khởi động từ instrumentation.ts register(), chặn ở NEXT_RUNTIME !== 'nodejs', cờ singleton mức module; khoá là HÀNG trong bảng account_lock, lease 10 phút, gia hạn ở mỗi ranh giới Công ty; trạng thái 'vòng đang chạy' SUY TỪ KHOÁ, không lưu cột riêng.
- (constraint) AD-13 (cha) — ontology ở src/ là nguồn sự thật chung, phải có mặt trong bản đã build qua outputFileTracingIncludes. Thứ tự phân xử: đề bài > Mục 0 > PRD > ontology > lược đồ (nội dung và ngữ nghĩa); lược đồ thắng về hình dạng lưu trữ.
- (constraint) AD-14 (cha) — mọi xoá là xoá mềm; lọc deleted_at IS NULL ở đúng một chỗ (Prisma extension trong src/core); thời gian timestamptz UTC, đổi múi giờ chỉ ở tầng trình bày.
- (constraint) AD-15 (cha) — env chỉ chứa bí mật và địa chỉ hạ tầng; tham số nghiệp vụ nằm trong bảng settings, đọc mỗi lần dùng. ĐÚNG HAI TỆP đọc process.env: src/config.ts và instrumentation.ts (đúng một biến NEXT_RUNTIME). Phép kiểm cưỡng chế quét TOÀN REPO.
- (constraint) AD-16 (cha) — cache hai tầng; bảng inference_cache khoá bằng hash đúng ba tham số của AD-7 cộng phiên bản lời nhắc, không chứa ngày/giờ/số ngẫu nhiên.
- (constraint) AD-17 (cha) — src/ingest/mapping.ts là NƠI DUY NHẤT biết tên trường nguồn BTC. Nghiệm thu: đổi tên một trường nguồn chỉ sửa tệp này.
- (constraint) AD-18 (cha) — mốc cắt chuẩn hoá: src/ingest chịu mọi thứ phụ thuộc định dạng nguồn (gỡ thẻ, giải mã thực thể HTML, quyết định readable=false), KHÔNG BAO GIỜ chạm khoảng trắng hay Unicode; src/core/normalize.ts chịu phần còn lại. Đầu ra của ingest là văn bản thuần, nộp qua createArticle. Cấm .replace(/\s+/ và .normalize( trong src/ingest.
- (constraint) AD-19 (cha) — sổ đăng ký phủ MỌI thao tác chạm dữ liệu, đọc lẫn ghi, mọi actor. T-10 khẳng định khối một bằng đúng; khối hai và ba khẳng định không giao.
- (constraint) AD-20 (cha) — nạp dữ liệu là actor thứ ba 'seed', đi qua Cổng ở bước ②, được mọi capability ghi, bỏ qua vị từ chỉ có nghĩa lúc vận hành, KHÔNG sinh ghi vết trừ khi có cờ --giu-ghi-vet.
- (constraint) AD-21 (cha) — hệ quả dây chuyền giữ nguyên actor khởi phát, chạy trong cùng giao dịch, ghi vết mang causedBy.
- (constraint) AD-22 (cha) — mô hình sinh Claim (đúng lược đồ Phát hiện), hệ thống sinh Proposal. queueSuggestion chạy sau, trong lõi, suy xác định.
- (constraint) Quy ước cha — lỗi: lõi ném BusinessRuleError có mã; 'web đổi thành HTTP' (cha KHÔNG nói đổi thế nào — đó là chỗ để mở cho tầng này). Nhật ký ghi vào BẢNG không ghi ra tệp (T-8 đòi truy vấn được). Kiểm thử: tests/T-<n>.test.ts, describe('T-<n> …') + it('Scenario: …') theo NFR-13.
- (constraint) Cha để mở (Deferred) mà tầng này phải đóng: nối bộ component Claude Design vào React 19 (_ds_bundle.js là IIFE React 18 UMD); tokens/fonts.css gọi Google Fonts; chiến lược thử lại khi gọi mô hình lỗi; S4 Pipeline board và S7 Watching list.
- (constraint) Cha chặn (Deferred) — Q15 lược đồ dữ liệu BTC, BTC phát đề sáng 15/08. Đường giảm bất định cha đã chỉ: bảng ánh xạ trường của docs/CRM clone từ Airtable/Tomahawk_CRM_PRD_BuildReady_v3.2.md §12.
- (constraint) Stack cha đã ghim: Next.js 16.3.1, React 19.2.8, TypeScript ~5.9, Node 24.19.0, Vitest 4.1.10, Prisma 7.9.1, Zod 4.4.3. Ứng dụng chạy TRÊN HOST bằng next start (production), chỉ Postgres trong docker compose. 'test': 'vitest run'.
- (event) Đã đọc Tomahawk_CRM_PRD_BuildReady_v3.2.md §12 (dòng 5179-5527) — đường giảm bất định Q15 mà spine cha chỉ ra. Kết quả PHẢN LẠI giả thuyết của cha, xem ba entry dưới.
- (assumption) Giả thuyết của cha — 'lược đồ BTC khớp thư mục docs/CRM clone từ Airtable/' — nay ĐÁNH GIÁ LÀ YẾU. Tomahawk §12 là migration Airtable base appHFjw7QNsdwg46o sang MỘT SẢN PHẨM KHÁC (Tomahawk CRM), không phải bộ dữ liệu BTC. Nó cho biết HÌNH DẠNG một bản xuất Airtable, không cho biết tên trường BTC sẽ dùng.
- (question) Chỉ ~14/74 dòng ánh xạ §12 chạm được mô hình Why Now (~19%). Account 5/32 (Company Name, Industry, Country, Is Watching, AM/Sales); Contact 3/14; Opportunity 6/20; Snapshot 0/0 — Airtable KHÔNG có khái niệm ảnh chụp web, mà Snapshot chính là thứ T-6 và T-8 chạy trên. Nguồn Bản chụp phải do BTC phát riêng, không nằm trong bất kỳ bản xuất CRM nào.
- (question) Enum nguồn HẸP HƠN enum đích, không phải chỉ đổi tên: OpportunityStatus của Airtable có NĂM giá trị (Following, RFP JD, Proposed, Won deal Running, Lost- close) trong khi stage của Mục 0 có BẢY. Thiếu hẳn du_dieu_kien, thuong_luong, tam_dung. icpType (= account_type) trùng đúng năm giá trị nhưng chính Tomahawk ghi 'KHÔNG có ở nguồn, để null'. Nghĩa là ánh xạ enum có ô KHÔNG BIẾT, và mapping.ts phải khai chính sách cho ô đó thay vì ép bừa.
- (decision) legacyAirtableId là khoá đối soát dùng lại được: Tomahawk khai String? @unique trên năm model và §12.6 đặt ngưỡng 'không record nào mất legacyAirtableId'. Tầng này lấy nguyên ý tưởng đó làm khoá tự nhiên cho upsert idempotent (NFR-8) và cho báo cáo đối soát (TR-3) — nhưng đặt tên trung tính source_ref vì nguồn BTC chưa chắc là Airtable.
- (direction) Hệ quả lịch: cái dựng được TỐI 14/08 là NỬA ĐÍCH của ingest (kiểu ghi ra, upsert theo source_ref, báo cáo đối soát, idempotency, chính sách ô không ánh xạ được) cộng toàn bộ src/app, src/scan, tests. Cái phải chờ SÁNG 15/08 chỉ là bảng tên trường nguồn và ánh xạ giá trị enum — đúng một tệp theo AD-17.
- (question) XUNG ĐỘT 1 với cha — bảng danh-sách-cho-phép của AD-1 ghi 'Nhập src/capability/** — được phép ở src/autonomy/**'. Nhưng AD-4 đặt loadCapability() TRONG src/capability và nó gọi decide() của src/autonomy, tức chiều ngược lại; và sơ đồ paradigm vẽ gate --> cap. Đọc theo mặt chữ thì src/app KHÔNG có đường nhập hợp pháp nào tới loadCapability, tức không màn hình nào lấy được dữ liệu — đúng điều AD-1 tự nói là phải tránh. Không sửa cha; nêu lên.
- (question) XUNG ĐỘT 2 với cha — AD-11 khai estimated_cost_per_account là HÀNG TRONG BẢNG settings và 'ghi lại từ chi phí thật của từng Công ty ngay từ vòng đầu'. Nhưng không có capability nào cho phép máy ghi settings (ba chạm của AD-3 không gồm nó), và thêm một cái sẽ làm CAP_MAY_DUOC khác chín, tức T-10 đỏ. Hai cách đọc: (a) máy tự ghi — cần capability thứ mười, phá AD-2; (b) người nhập tay sau khi quan sát vòng đầu — giữ AD-2 nhưng không tự động.
- (question) XUNG ĐỘT 3 với cha — mục Deferred của cha hoãn S4 Pipeline board. Nhưng FR-4 mang nhãn 'phải có' và viết 'đổi Giai đoạn bằng kéo thả, KHÔNG mở biểu mẫu', còn T-1 đòi 'kéo cơ hội qua ba giai đoạn, trong đó có Đủ điều kiện'. backlog-giao-dien.md lại xếp S4 vào nhóm 'để dạng biểu mẫu' tức VẪN DỰNG. Hoãn S4 theo mặt chữ của cha là bỏ một điểm T.
- (decision) instrumentation.ts thuộc nhánh src/scan về ngữ nghĩa nhưng KHÔNG chứa logic. Nó làm đúng ba việc: chặn NEXT_RUNTIME, cờ singleton mức module, rồi await import ĐỘNG sang src/scan/bootstrap.ts. Phương án đã loại: để nguyên lịch trình và vòng lặp trong instrumentation.ts — loại vì tệp đó nằm ngoài mọi thư mục src/, nên nó nằm ngoài tầm lint theo thư mục và tests/ không import được nó mà không khởi động vòng quét. Lý do import phải ĐỘNG: import tĩnh nạp cả cây src/scan trong edge runtime TRƯỚC khi cờ chặn kịp chạy.
- (decision) src/app và src/scan KHÔNG import nhau và KHÔNG có module dùng chung. Bề mặt chung duy nhất là loadCapability của src/capability. Phương án đã loại: một src/shared/callCapability.ts bọc chung — loại vì bọc chung buộc phải hợp nhất cách xử lỗi, mà hai bên xử lỗi khác nhau có lý do: src/app đổi GateDenied thành chữ hiển thị, src/scan đổi nó thành dòng nhật ký cộng bộ đếm max_consecutive_denials. Bọc chung sẽ hoặc rò HTTP vào vòng quét, hoặc rò bộ đếm từ chối vào web. Cấm thư mục src/shared cũng chặn luôn chỗ mà một helper actor ngầm sẽ hạ cánh, thứ AD-5 cấm.
- (decision) tests và src/ingest gieo bằng HAI đường, hội tụ ở tầng capability chứ không ở trên nó. tests/factory.ts gọi thẳng capability với actor seed và KHÔNG BAO GIỜ import src/ingest. Phương án đã loại: tests dùng lại src/ingest cho DRY — loại vì nó buộc cả bộ kiểm thử phụ thuộc Q15, câu chặn duy nhất của dự án; nghĩa là mười điểm T không viết được trước sáng 15/08. Cái giá của hai đường: hai chỗ biết cách dựng một Công ty hợp lệ. Chấp nhận, vì chúng dựng hai thứ khác nhau — ingest dựng bộ dữ liệu BTC, factory dựng ca kiểm thử tối thiểu.
- (decision) Bộ giao diện: dùng THẲNG @fluentui/react-components v9 như DESIGN.md đã chốt; KHÔNG dựng cầu nối cho _ds_bundle.js. Mục Deferred của cha ('nối bộ component Claude Design vào React 19') được GIẢI TÁN chứ không phải được giải: DESIGN.md viết lại ngày 14/08 đã hạ nguyên mẫu Claude Design xuống mức 'chỉ còn giá trị tham khảo về luồng, không còn về thị giác'. Phương án đã loại: viết tệp nối window.React cho IIFE React 18 — loại vì nó ghim sản phẩm vào một bundle đã biên dịch cho React 18 trong khi cha đã ghim React 19.2.8, và vì 25 component đã biên dịch không sửa được khi lệch token.
- (decision) Chia server/client trong src/app theo MỘT luật, không theo cảm tính từng màn: page.tsx và layout.tsx luôn là server component và là NƠI DUY NHẤT trong src/app gọi loadCapability; mọi tệp mang 'use client' nhận dữ liệu qua props đã tuần tự hoá và chỉ ghi qua server action. Phương án đã loại: route handler cộng fetch phía client (SWR/React Query) — loại vì mỗi lần đọc phải dựng thêm một endpoint HTTP bên cạnh capability đọc, tức nhân đôi bề mặt và tạo thêm một chỗ quên truyền actor; và UX-1 (≤10 giây tới câu trả lời) dễ đạt hơn khi render sẵn ở server.
- (decision) KHÔNG lỗi nào băng qua ranh giới server action bằng cách ném. Mọi server action bọc thân hàm trong try/catch và TRẢ VỀ một union phân biệt được. Lý do là một cái bẫy chỉ hiện ở bản production, mà NFR-11 bắt chạy đúng bản production: Next.js nuốt thông điệp của lỗi chưa bắt trong server action và trả một câu chung, nên thiết kế dựa vào việc bắt lỗi có mã ở phía client sẽ chạy đúng dưới npm run dev rồi câm lặng trong lượt chấm.
- (decision) Ánh xạ lỗi hai chặng, không phải một: (1) mã lỗi lõi -> mã giao diện, bảng đặt ở đúng một tệp src/app; (2) mã giao diện -> chữ hiển thị tiếng Việt, cùng tệp. HTTP status CHỈ tồn tại ở route handler, không tồn tại ở server action. Điều này lấp đúng chỗ cha để mở ('web đổi thành HTTP' mà không nói đổi thế nào) và giữ quy ước cha 'lõi không chứa chuỗi hiển thị'.
- (decision) Trạng thái tải/rỗng/lỗi thành luật: bề mặt có nhiều khối hỏng độc lập được (S1 bốn khối, S3 bốn khối) phải ghép bằng các ranh giới Suspense ANH EM, mỗi khối một error boundary — không được một lần await ở mức trang. Đây là cách duy nhất thoả EXPERIENCE.md 'Timeline chậm không chặn hồ sơ' và 'một khối hỏng thì ba khối kia vẫn chạy'. Trạng thái RỖNG không bao giờ dùng chung component với trạng thái ĐANG TẢI — rỗng chỉ tới được sau một lượt đọc thành công trả về 0 dòng.
- (decision) UX-7 (vòng quét không cướp tiêu điểm) thành luật cưỡng chế được: src/scan KHÔNG BAO GIỜ gọi revalidatePath/revalidateTag, và src/app chỉ làm mới sau một thao tác của người (router.refresh() sau server action). Dữ liệu mới do vòng quét sinh ra được báo bằng một dải bấm được, không tự thay nội dung đang mở. Phương án đã loại: polling hoặc SSE tự nạp lại — loại vì chu kỳ demo 60 giây bảo đảm một vòng quét chạy đúng lúc giám khảo đang bấm.
- (decision) Chữ ký thống nhất của server action, dùng với useActionState của React 19: (prev: ActionState, form: FormData) => Promise<ActionState>. MỘT server action gọi ĐÚNG MỘT capability — không hai lần ghi trong một action; ghi kèm theo là việc của cascades trong sổ đăng ký (AD-21). Actor dựng trong chính action từ userId của phiên.
- (decision) Vai đọc từ CƠ SỞ DỮ LIỆU mỗi lượt yêu cầu, cookie phiên chỉ mang userId. Điều này trả lời câu 'vai đổi thì sao' mà cha để ngoài phạm vi: vai chỉ đổi bằng cách gieo lại dữ liệu, và đọc vai sống làm cho một phiên cũ không thể mang vai cũ. Phương án đã loại: nhét vai vào cookie/JWT — loại vì giám khảo gieo lại giữa buổi demo sẽ giữ vai cũ mà không có gì phát hiện.
- (decision) Cưỡng chế quyền ở giao diện: bề mặt chỉ-Quản-trị render qua một server component mà HÀNH ĐỘNG ĐẦU TIÊN là lời gọi capability sẽ bị Cổng từ chối; màn 403 là KẾT QUẢ của lần từ chối đó, không phải một phép kiểm vai riêng ở client. Nhờ vậy D28 ('chặn ở tầng nghiệp vụ, không phải ẩn menu') nằm trên chính đường đi, không nằm song song với nó. Ẩn mục menu vẫn làm, nhưng chỉ là thẩm mỹ.
- (decision) runScan() trả về đúng MỘT kiểu union: {stop, processed, total, cursor}. Năm điều kiện dừng của AD-11 và ba subtype SDK quy về một hàm classifyStop() duy nhất, nên mã lý do không thể được biểu diễn ba kiểu khác nhau ở ba chỗ.
- (decision) Con trỏ resume là MỘT CỘT trên chính dòng ScanLog của vòng bị cắt (resume_after_account_id), không phải hàng trong settings. Phương án đã loại: settings — loại vì settings theo AD-15/D38 là tham số NGHIỆP VỤ mà Quản trị sửa được lúc chạy; con trỏ resume không phải tham số nghiệp vụ và không bao giờ được xuất hiện trên màn hình Quản trị. FR-39 vốn đã đòi dòng ScanLog cho vòng không trọn kèm số Công ty đã quét trên tổng — con trỏ là chính sự thật đó.
- (decision) Tập Công ty đã chốt của một vòng luôn sắp theo một TOÀN THỨ TỰ ỔN ĐỊNH (account.id), không bao giờ theo updated_at. Vòng quét ghi vào chính các hàng nó đang duyệt, nên sắp theo updated_at sẽ xáo lại thứ tự giữa chừng và con trỏ resume trỏ vào chỗ sai.
- (decision) Khoá Công ty đi qua capability, không qua SQL trong src/scan: acquireAccountLock/renewAccountLock/releaseAccountLock nằm trong khối CAP_HE_THONG_NOI_BO. Khối ba chỉ bị khẳng định 'không giao với khối một' nên thêm vào đây KHÔNG động tới T-10. src/scan/lock.ts chỉ xâu chuỗi ba lời gọi, không chứa SQL — bắt buộc, vì AD-1 cấm src/scan nhập @prisma/client. Giành khoá là một câu ghi có điều kiện duy nhất (INSERT ... ON CONFLICT DO UPDATE ... WHERE expires_at < now()) rồi kiểm số dòng, cùng kỷ luật kiểm-và-ghi của AD-10; nhả khoá là đặt expires_at = now(), không DELETE.
- (decision) estimated_cost_per_account SUY RA lúc bắt đầu vòng từ các dòng ScanLogEntry của vòng hoàn tất gần nhất (lấy chi phí lớn nhất trên một Công ty), không lưu trong settings. Chưa có lịch sử thì Công ty ĐẦU TIÊN của vòng đầu tiên được nhận vô điều kiện, từ Công ty thứ hai trở đi mới cưỡng chế. Đây là chỗ tầng này lệch cách đọc mặt chữ của AD-11 — xem entry XUNG ĐỘT 2.
- (decision) Phân biệt 'lỗi không phục hồi' (điều kiện dừng 5 của AD-11) với lỗi của một Công ty: lỗi quy được về đúng một Công ty thì ghi một dòng ScanLogEntry rồi ĐI TIẾP Công ty sau, KHÔNG thử lại trong vòng; chỉ lỗi không quy về Công ty nào (xác thực, cơ sở dữ liệu, hạch toán ngân sách) mới kết thúc vòng. Điều này đóng mục Deferred 'chiến lược thử lại' của cha: KHÔNG có thử lại ở mức ứng dụng; lần thử lại duy nhất là lần SDK tự thử lại structured output.
- (decision) Nhật ký vòng quét dùng hai bảng: ScanLog một dòng mỗi vòng, ScanLogEntry một dòng mỗi Công ty trong vòng. Dòng tổng hợp mỗi 10 vòng là một dòng ScanLog mang kind='tong_hop' trong CÙNG bảng, để T-8 lấy cả 'dòng tổng kết cho từng vòng' lẫn dòng tổng hợp bằng một truy vấn. Vòng không trọn mang kind='vong' và KHÔNG đếm vào chuỗi 10 vòng theo FR-39.
- (decision) Quy ước mã: mã lý do dừng và mã lỗi giao diện viết TIẾNG ANH để đứng cùng hệ với mã từ chối của Cổng ở cha (unknown_capability, actor_not_allowed, boundary, role, limit, brake). Giá trị enum NGHIỆP VỤ lấy từ Mục 0 giữ tiếng Việt không dấu (tiep_can, chac, thong_tin_sai). Enum nghiệp vụ mới do tầng này sinh ra cũng theo kiểu Mục 0: kind của ScanLog là vong | tong_hop.
- (decision) src/ingest tách ba tệp theo một đường nối cứng: mapping.ts là nơi DUY NHẤT gọi tên trường nguồn (AD-17) và là thứ duy nhất phải chờ sáng 15/08; load.ts đọc tệp, lặp, gọi capability với actor seed, KHÔNG biết tên trường nguồn; reconcile.ts dựng báo cáo TR-3. Đường nối: mapping.ts xuất các hàm THUẦN trả về kiểu ĐÍCH (AccountInput, ContactInput, OpportunityInput, SnapshotInput) — kiểu đích đã biết từ tối nay, nên load.ts và reconcile.ts dựng được trước ngày thi.
- (decision) Ánh xạ giá trị enum KHÔNG BAO GIỜ ép ngầm. Mỗi trường được ánh xạ khai đúng một trong ba kết cục: ánh-xạ-được / mặc-định-CÓ-TÊN / không-ánh-xạ-được-thì-DỪNG-NẠP. Luật này sinh ra từ bằng chứng đo được chứ không từ nguyên tắc chung: enum trạng thái Cơ hội phía Airtable có NĂM giá trị còn stage của Mục 0 có BẢY, thiếu hẳn du_dieu_kien, thuong_luong, tam_dung; và account_type thì chính tài liệu nguồn ghi 'KHÔNG có ở nguồn, để null'. Không có luật này thì lần nạp đầu tiên hoặc nổ giữa chừng, hoặc âm thầm dồn mọi Cơ hội vào một giai đoạn sai.
- (decision) Idempotency của §7.5/NFR-8 làm bằng KHOÁ TỰ NHIÊN từ nguồn: mọi thực thể nạp mang source_ref duy nhất lấy từ định danh bản ghi nguồn, và nạp là upsert theo source_ref. Phương án đã loại: xoá sạch rồi chèn lại — loại vì AD-14 chỉ cho xoá mềm, và xoá sạch sẽ huỷ đúng phần ghi vết mà cờ --giu-ghi-vet của TR-4 sinh ra để giữ. Tên source_ref trung tính có chủ đích: tài liệu nguồn gọi nó legacyAirtableId, nhưng bộ dữ liệu BTC chưa chắc đến từ Airtable.
- (decision) Báo cáo đối soát TR-3 in ra stdout ở cuối npm run seed, và các con số đếm QUA CAPABILITY ĐỌC chứ không qua Prisma thẳng (AD-1). Mỗi thực thể một dòng: số bản ghi ở nguồn, số tạo mới, số cập nhật, số bỏ qua kèm lý do, và một dòng TỔNG. Nghiệm thu 'hai lần chạy cho cùng con số' của TR-3 áp lên dòng TỔNG; cặp tạo-mới/cập-nhật được phép khác nhau giữa hai lần chạy.
- (decision) Bộ kiểm thử chạy trên CƠ SỞ DỮ LIỆU RIÊNG, không dùng chung với bản demo. Lý do quyết định, không phải sở thích: npm run seed dựng đúng bộ dữ liệu giám khảo đang nhìn, nên một lượt npm test chạy chung sẽ xoá dữ liệu demo giữa buổi chấm.
- (decision) Dọn dẹp bằng globalSetup của Vitest gọi Prisma CLI (db push --force-reset) một lần TRƯỚC mỗi lượt chạy, cộng fileParallelism: false. Ba lý do: (1) Prisma CLI không phải là nhập @prisma/client nên không đụng danh sách cho phép của AD-1; (2) dọn TRƯỚC chứ không dọn SAU, vì một ca kiểm thử nổ giữa chừng sẽ không dọn được — dọn trước làm mỗi lượt chạy bắt đầu từ trạng thái đã biết bất kể lượt trước kết thúc thế nào, và đó chính xác là điều 'npm test chạy hai lần liên tiếp vẫn xanh' đòi hỏi; (3) chạy nối tiếp loại hẳn lớp bất định do các tệp kiểm thử tranh nhau một cơ sở dữ liệu, đổi lấy vài giây trên mười tệp.
- (decision) vitest.config.ts KHÔNG đọc process.env; nó khai test.env với địa chỉ cơ sở dữ liệu kiểm thử dạng chuỗi, còn src/config.ts vẫn là nơi đọc duy nhất. Phương án đã loại: thêm vitest.config.ts vào danh sách cho phép của AD-15 — loại vì đó là nới một AD cha, tức xung đột phải nêu lên chứ không phải override cục bộ.
- (assumption) Giả định: máy chạy demo là Windows, nên Segoe UI có sẵn và không cần nạp webfont. Hệ quả: mục Deferred 'tokens/fonts.css gọi Google Fonts' của cha được giải tán — DESIGN.md đã bỏ serif và lấy fontFamilyBase của Fluent, mà Segoe UI đứng đầu chuỗi đó và đã kiểm đủ 90/90 glyph tiếng Việt. Trên máy không phải Windows chữ sẽ rơi về phông dự phòng; chấp nhận, không nạp webfont.
- (version) Xác minh trên npm ngày 14/08: @fluentui/react-components 9.74.6 (publish 11/08), peerDependencies.react = >=16.14.0 <20.0.0 — React 19.2.8 mà cha đã ghim NẰM TRONG dải. React 19 vào từ 9.70.0; các bản 9.66-9.69 còn khai <19.0.0. @fluentui/react-icons 2.0.337 (peer >=16.8.0). @griffel/react 1.7.7 đi kèm.
- (version) Cảnh báo nguồn: mọi bài viết và issue nói 'Fluent UI không hỗ trợ React 19' đều thuộc giai đoạn peer range còn <19.0.0 (2024-2025) và đã lỗi thời. Bằng chứng dứt điểm là package.json đã publish của 9.74.6, không phải issue tracker.
- (constraint) Fluent v9 KHÔNG ship directive 'use client' — kiểm bằng bundle đã publish: lib/index.js mở đầu bằng '// Utilities' rồi export, không có directive. Mọi component v9 dùng createContext/hooks nên render thẳng trong server component sẽ lỗi. Đội phải tự bọc.
- (constraint) Ranh giới 'use client' áp cho MODULE GRAPH, không áp cho Server Component truyền vào qua children hay prop. Tài liệu Next 16 nói rõ chúng được render ở server rồi truyền xuống dưới dạng đầu ra đã render. Đây là điều làm cho luật 'page/layout là server component, lá Fluent là client component' khả thi thay vì phải kéo cả cây thành client.
- (decision) Griffel bắt buộc có registry trong App Router: một tệp 'use client' duy nhất gom createDOMRenderer + RendererProvider + renderToStyleElements qua useServerInsertedHTML, và FluentProvider bọc bên trong nó, đặt quanh {children} trong app/layout.tsx. Không có gói @griffel/next (registry trả 404); tất cả nằm trong @griffel/react.
- (decision) Registry Griffel PHẢI có cờ flush-một-lần. renderToStyleElements() trả về TOÀN BỘ CSS đã thu thập mỗi lần gọi, còn Next gọi callback một lần cho MỖI lần flush stream — thiếu cờ thì toàn bộ stylesheet bị nhân bản vào body, và bản cũ ghi đè style sau khi điều hướng phía client. Lỗi này chỉ lộ khi trang đủ lớn để stream nhiều chunk, nên nó qua mắt lúc thử trang nhỏ rồi vỡ đúng lúc chấm.
- (decision) KHÔNG khai khối webpack nào trong next.config.ts, và không dùng @griffel/webpack-loader. Next 16 mặc định Turbopack cho cả dev lẫn build, và tài liệu nâng cấp nói rõ: dự án có cấu hình webpack tuỳ biến thì next build sẽ THẤT BẠI. Griffel chạy được hoàn toàn ở runtime, biến đổi lúc build chỉ là tối ưu. outputFileTracingIncludes mà AD-13 đòi là khoá cấu hình Next thường, không phải khối webpack, nên nó an toàn.
- (constraint) Xác minh Next 16: instrumentation.ts register() vẫn đúng, stable từ v15, gọi MỘT LẦN khi khởi tạo server instance và phải xong trước khi server nhận yêu cầu — tức nó CHẠY dưới next start (production), đúng thứ AD-12 cần. process.env.NEXT_RUNTIME vẫn là cách chuẩn chặn edge. Tệp đặt ở gốc dự án, không đặt trong app/.
- (constraint) Xác minh React 19: useActionState nằm ở package react (không phải react-dom), trả về tuple BA phần tử [state, action, isPending]; useFormState của React 18 đã đổi tên và bị deprecate. Tài liệu Next 16 nói thẳng: lỗi MONG ĐỢI thì TRẢ VỀ, đừng ném — ném sẽ kích hoạt error boundary gần nhất. Điều này xác nhận luật 'không lỗi nào băng qua ranh giới server action bằng cách ném'.
- (constraint) Xác minh Next 16: revalidateTag nay BẮT BUỘC tham số thứ hai là cacheLife profile; dạng một tham số đã deprecate và lỗi TypeScript. Cộng với luật UX-7 (src/scan không gọi revalidate*), tầng này gần như không chạm API đó — nhưng ghi lại để không ai chép mẫu cũ.
- (constraint) Xác minh Next 16: server action là một endpoint POST công khai. Next có kiểm CSRF (Origin so Host), giới hạn body 1MB, mã hoá action id — nhưng phải TỰ xác thực và phân quyền trong MỌI action; chặn lúc render không phải ranh giới bảo mật. Trùng khớp D28 và cách cưỡng chế quyền của tầng này.
- (assumption) Chưa xác minh được: có bug đang mở nào giữa Fluent 9.74.6 x React 19.2 x Turbopack hay không — github.com không fetch được từ môi trường chạy. Các issue tìm thấy đều thuộc giai đoạn peer range cũ. Giảm thiểu: chạy npm install và một lần next build TRƯỚC ngày thi (DESIGN.md cũng đã cảnh báo 62 dependencies), và nếu gặp lỗi 'React Element from an older version of React' thì kiểm npm ls react chỉ ra đúng một bản.
- (event) Chắt lọc spine từ memlog: 19 AD-UI, ba xung đột với cha được nêu ở mục riêng. lint_spine.py total_findings 0. Lưu ý cơ chế: regex của linter chỉ khớp AD-<số> nên nó KHÔNG kiểm được các khối AD-UI-n; đã kiểm tay bằng script riêng — 19 khối, số liên tục 1..19, mỗi khối đủ Binds/Prevents/Rule.
- (decision) Tự phản biện (CP5) trước khi nhận kết quả reviewer, ba lỗ hổng tự tìm và đã vá: (1) ActionState chưa có chủ — nay khai ở đúng tệp ánh xạ lỗi của AD-UI-7, cấm khai lại; (2) bốn kiểu đích AccountInput/ContactInput/OpportunityInput/SnapshotInput chưa có chủ — nay do src/capability sở hữu vì chúng là kiểu tham số của chính capability ghi, src/ingest chỉ NHẬP; (3) chưa có luật cấm render tĩnh — T-9 treo vào đó, vì khung S0 bị cache tĩnh thì dải báo AI tắt không bao giờ đổi và T-9 đỏ với triệu chứng trỏ nhầm sang vòng quét.
- (event) Reviewer Gate chạy headless: hai lens của finalize_reviewers dựng thành hai subagent song song — (1) xác minh mọi khẳng định kỹ thuật trên web, (2) tấn công đối kháng dựng hai đơn vị thi công tuân thủ đủ mọi AD mà vẫn không ghép được. Cả hai trả kết quả có bằng chứng; đã vá.
- (version) Lens xác minh xác nhận ĐÚNG: ba phiên bản Fluent/Griffel và dải peer <20.0.0, mốc React 19 vào từ 9.70.0, Fluent v9 không ship 'use client', bộ ba API Griffel SSR cùng yêu cầu cờ flush-một-lần, Turbopack mặc định Next 16 và next build thất bại khi có webpack tuỳ biến, useActionState ba phần tử, Next nuốt thông điệp lỗi ở production (có tài liệu chính thức của error.js), instrumentation register() chạy dưới next start, cú pháp ON CONFLICT ... DO UPDATE ... WHERE hợp lệ trên Postgres 16 và trả 0 dòng khi khoá còn hạn.
- (decision) Vá từ lens xác minh — AD-UI-19 dựng sai chuỗi công cụ kiểm thử: test.env của Vitest KHÔNG có mặt ở tiến trình chính, mà globalSetup chạy đúng ở đó. Ba khẳng định riêng lẻ đều đúng nhưng ghép lại thì Prisma CLI không thấy địa chỉ DB kiểm thử và reset nhầm DB demo. Sửa: địa chỉ khai một lần dạng literal trong vitest.config.ts, dùng ở HAI chỗ — test.env và trường env khi spawn CLI.
- (decision) Vá từ lens xác minh — đổi prisma db push --force-reset thành prisma migrate reset --force --skip-seed. Lý do mạnh hơn lý do ban đầu: db push BỎ QUA thư mục migration, nên một migration thiếu hoặc chưa commit vẫn cho npm test xanh 10/10 rồi giết npm run seed trên máy giám khảo. Bộ kiểm thử phải dựng lược đồ qua ĐÚNG con đường mà lệnh nộp bài đi. Kèm: Prisma 7 không còn tự chạy generate sau db push và đã gỡ --skip-generate.
- (question) XUNG ĐỘT 6 với cha (từ lens xác minh) — Prisma 7 chuyển cấu hình kết nối sang prisma.config.ts ở gốc dự án và KHÔNG còn tự nạp biến môi trường. Đó là tệp THỨ BA chạm process.env, trong khi AD-15 khai đúng hai và phép kiểm quét toàn repo. Phép kiểm sẽ đỏ ngay khi tệp đó xuất hiện, trước cả dòng mã sản phẩm đầu tiên.
- (question) Ghi chú từ lens xác minh, không phải xung đột: outputFileTracingIncludes của AD-13 chỉ tác động tới output standalone / server trace. Cha lại chốt chạy next start ngay trong thư mục dự án, khi đó tệp ontology vốn đã nằm trên đĩa. Không sai cú pháp nhưng là độ chính xác giả; nghiệm thu thật vẫn là build production rồi chạy.
- (question) XUNG ĐỘT 4 với cha (từ lens đối kháng, lỗ hổng nặng nhất) — con gà và quả trứng của actor. AD-5 khai vai là trường BẮT BUỘC của actor người; AD-19 bắt mọi lượt ĐỌC qua sổ đăng ký; AD-1 không cho src/app ngoại lệ nào. Nên đọc vai phải gọi capability, mà gọi capability cần actor đã có vai. Lối ra duy nhất là một lượt đọc dưới danh nghĩa hệ thống — nhưng bước ⑦ của Cổng từ chối MỌI actor hệ thống khi phanh AI bật. Hệ quả: bấm Tắt AI thì không ai đăng nhập được và không trang nào render, làm T-9 VÀ T-1 cùng đỏ.
- (decision) Vá xung đột 4: thêm đúng MỘT capability readActorIdentity(userId) vào CAP_HE_THONG_NOI_BO, gọi bằng actor system, là lời gọi duy nhất trong repo được chạy trước khi actor người tồn tại; cùng lối đó phục vụ S11 đăng nhập. Cách đọc tầng này dựng theo và cần cha xác nhận: bước ⑥ và ⑦ CHỈ áp cho capability thuộc CAP_MAY_DUOC — phanh là phanh của phần AI, không phải phanh của toàn hệ thống, đúng như EXPERIENCE.md mô tả 'phanh chặn phần sinh ra, không chặn phần người quyết'.
- (question) XUNG ĐỘT 5 với cha (từ lens đối kháng) — AD-4 viết 'instrumentation.ts và src/app dựng Cổng với false', nhưng AD-UI-1 chỉ cho instrumentation.ts ba việc, và không tệp nào trong src/app có vòng đời khởi động. Cách đọc tầng này dựng theo: Cổng chế-độ-không-gieo dựng ĐÚNG MỘT LẦN ở mức module trong src/capability/registry.ts; instrumentation.ts và src/app DÙNG nó chứ không DỰNG nó.
- (decision) Vá từ lens đối kháng — BỎ renewAccountLock. Kiểm lại cha: AD-2 ĐÃ liệt acquireAccountLock và releaseAccountLock trong CAP_HE_THONG_NOI_BO, và cha viết 'ba khối gộp lại là một danh sách đóng mà T-10 khẳng định TỪNG PHẦN TỬ'. Nên thêm mục thứ ba sẽ làm T-10 đỏ dù con số chín không đổi. Gia hạn nay là acquireAccountLock gọi lại với cùng process_id, bằng cách thêm vế OR account_lock.process_id = EXCLUDED.process_id vào WHERE. Lập luận cũ của AD-UI-13 ('khối ba chỉ bị khẳng định không giao') đã SAI và đã bỏ.
- (decision) Vá từ lens đối kháng — AD-UI-20 mới: bốn tệp dùng chung có chủ. prisma/schema.prisma và prisma/migrations/ một chủ, không dev nào chạy migrate dev (hai migration cùng cha khác checksum thì git merge xanh nhưng migrate deploy báo drift trên clone sạch, đúng cổng nghiệm thu). src/capability/registry.ts và tests/T-10.test.ts là một cặp bất khả phân, cùng chủ, cùng lượt commit. Hai tệp _contract.ts viết TRƯỚC mọi bề mặt.
- (decision) Vá từ lens đối kháng — cô lập GIỮA các tệp kiểm thử. globalSetup chỉ dựng lược đồ, nên nó chỉ bảo đảm trạng thái sạch cho tệp ĐẦU TIÊN: T-8 chạy lẻ đếm đúng 5 dòng Nhật ký, chạy trong cả lượt đếm ra 23. Sửa: mỗi tệp T mở bằng beforeAll gọi tests/reset.ts dùng chung, cộng một phép kiểm meta khẳng định cả mười tệp đều gọi nó.
- (decision) Vá từ lens đối kháng — luật 'không ném qua ranh giới server action' trước đó phát biểu được nhưng KHÔNG cưỡng chế được, tức nó là nguyện vọng chứ không phải bất biến. Sửa: mọi server action đi qua đúng một helper action(fn) trong src/app/_contract.ts, là chỗ duy nhất có try/catch; một phép kiểm quét src/app/** khẳng định không tệp nào khai 'use server' mà không nhập helper đó.
- (decision) Vá từ lens đối kháng — dải báo dữ liệu mới trước đó không có mốc so sánh và không có đường cập nhật. Chốt: nó hiện SỐ GỢI Ý ĐANG CHỜ tại thời điểm render server, một con số tuyệt đối chứ không phải delta; mốc 'đã xem tới đâu' nằm NGOÀI phạm vi kỳ thi và nói thẳng như vậy, vì im lặng sẽ khiến một người dựng theo nghĩa delta rồi đi tìm một cột không tồn tại.
- (decision) Vá từ lens đối kháng — reconcile.ts đếm CHỈ các hàng có source_ref IS NOT NULL. Công ty do người tạo tay ở nhóm 1 không có source_ref, nên đếm chung thì giám khảo bấm tạo một Công ty giữa hai lượt seed sẽ làm TR-3 đỏ vì hành vi NGƯỜI DÙNG chứ không vì mã.
- (assumption) Chưa vá, ghi lại có ý thức: luật 'khung xương lúc tải giữ đúng số dòng của lần trước' (AD-UI-8) không cài được trong kiến trúc đã chọn — 'lần trước' là trạng thái client mà AD-UI-5 cấm lá client tự giữ. Ngày thi không ai đo nên để nguyên; nếu dựng thật thì phải hạ nó xuống mục quy ước hoặc đổi kiến trúc.
- (event) Sau khi vá: 20 AD-UI (thêm AD-UI-20), sáu xung đột với cha, lint_spine.py total_findings 0, kiểm tay 20 khối đủ Binds/Prevents/Rule và số liên tục.
- (event) spine finalized
- (event) Đồng bộ spine tầng ① với bản cha 14/08 và với lượt hoà giải mặt cắt ③↔④. Đồng bộ, không dựng lại: mọi mã AD-UI-1..AD-UI-20 giữ nguyên số và nguyên nội dung trừ chỗ trích bản cha đã cũ.
- (change) Bảng Inherited Invariants: AD-2 sửa 'CAP_MAY_DUOC giữ đúng chín' thành hạng ghi đúng SÁU cộng hạng đọc-chung, và liệt CAP_HE_THONG_NOI_BO đủ NĂM mục gồm recordAccountCost và readUserForAuth. AD-4 sửa createGate thành createRegistry({seedMode,auditSink}) -> {loadCapability}, ghi rõ gate.ts không xuất nhà máy/thể hiện/kiểu Gate và RegistryEntry extends GateEntry, cộng luật selfLimiting năm mục bắt buộc. AD-5 khai kiểu Actor đầy đủ với role: 'sales'|'admin'. AD-11 thêm hạt đếm model_calls_used. AD-15 sửa hai tệp thành ba tệp (thêm prisma.config.ts). AD-20 sửa cờ --giu-ghi-vet thành --keep-audit.
- (decision) Thêm AD-UI-21 ở CUỐI dải, không đánh số lại: src/scan/loop.ts là chủ cài đặt bộ đếm ScanLog.model_calls_used ở hạt LỆNH GỌI (tăng ngay sau mỗi query()), ghi qua writeScanLog. Kèm luật hai lời gọi selfLimiting ở ranh giới Công ty theo thứ tự recordAccountCost rồi releaseAccountLock, chạy CẢ trên nhánh dừng, cộng phép kiểm 'sau một vòng bị cắt vì ngân sách, không hàng account_lock nào còn expires_at > now()'. Phương án đã loại: tăng ở ranh giới Công ty — làm trần 20 thành trang trí vì bước ⑥ đọc số cũ suốt một Công ty.
- (change) AD-UI-10 viết lại phần capability: readActorIdentity đổi thành readUserForAuth (tên và chỗ đứng do cha chốt ở AD-2), trường vai đổi thành role với từ vựng đóng 'sales'|'admin', và ghi rõ cờ selfLimiting đã đóng va chạm với bước ⑦. src/app gọi nó ở đúng hai chỗ: _session.ts mỗi lượt yêu cầu và server action đăng nhập của S11. Bảng loại bề mặt của AD-UI-5 sửa dòng S11 theo.
- (change) AD-UI-13 sửa 'con số chín' thành 'con số sáu' và thêm đoạn releaseAccountLock mang selfLimiting — đây là chỗ gỡ bug khoá không nhả dẫn tới 10 vòng liên tiếp bị bỏ; acquireAccountLock cố ý KHÔNG mang cờ đó. AD-UI-14 thêm hai cột ScanLog.model_calls_used và ScanLog.budget_usd trên CÙNG một hàng để bước ⑥ lấy tử số và mẫu số bằng một lượt đọc, cộng đường ghi chi phí thật qua recordAccountCost ở ranh giới Công ty. AD-UI-20 sửa danh sách cột phải tạo và sửa 'nộp đúng một mục readActorIdentity' thành 'không nộp mục nào' vì cha đã khai sẵn cả hai capability.
- (change) Mục Xung đột với spine cha: bốn trong sáu mục cũ đánh dấu ĐÃ ĐÓNG kèm bằng chứng từ bản cha hiện hành — ① đường nhập của src/app (bảng AD-1 đã liệt đủ), ③ S4 (đã gỡ khỏi Deferred, thực dựng 11/12), ④ bước ⑦ chặn lượt đọc dựng actor (readUserForAuth mang selfLimiting), ⑥ prisma.config.ts (AD-15 đã liệt ba tệp). Mục ② hẹp lại còn 'chỗ lưu con số suy ra'. Mục ⑤ giữ mở và viết lại theo createRegistry. Thêm mục ⑦ mới: ai đặt budget_usd của một vòng — nghi là maxBudgetUsd mà src/scan/loop.ts truyền cho SDK. Giữ nguyên văn các mục đã đóng thay vì xoá, vì mỗi mục là bằng chứng cho một AD-UI đang dựng theo cách đọc nào.
- (change) Thêm mục '## Xung đột với spine anh em' (năm dòng, đối xứng với bảng của tầng ③ và ④): ① tôi nhận việc tăng model_calls_used ở hạt lệnh gọi; ② cha thắng ở tên readUserForAuth, cả tên của tôi lẫn authenticateUser của ④ đều bỏ; ③ mẫu số tỉ lệ ngân sách về tay tôi qua ScanLog.budget_usd; ④ cha thắng ở bảng selfLimiting năm mục, ảnh hưởng nặng nhất là releaseAccountLock; ⑤ tôi thắng ở S4 và cha đã theo. Ghi rõ không có va chạm với tầng ② và tầng ⑤, kèm lý do.
- (constraint) Sàn eslint >= 9.37.0 ghi vào mục Stack của tầng: ba luật cưỡng chế của tầng (danh sách cho phép AD-1, ba tệp process.env AD-15, cấm src/capability/caps/**) đều là no-restricted-imports với allowTypeImports, mà khoá đó vào luật lõi đúng ở 9.37.0; ở 9.36 ESLint chết lúc nạp và cơ chế cưỡng chế duy nhất biến mất trong khi lint vẫn báo xanh.
- (change) Sửa vặt cho khớp cha: Design Paradigm và sơ đồ ghi 'dựng 11/12, chỉ S7 hoãn'; cây nguồn đánh dấu S7 hoãn, S11 gọi readUserForAuth, loop.ts là chủ bộ đếm, lock.ts xâu chuỗi HAI capability (không phải ba); kịch bản C thêm hai bước writeScanLog và recordAccountCost; _session.ts đổi chú thích sang readUserForAuth và role sống. Lint: total_findings 0. Kiểm tay AD-UI-1..AD-UI-21 liên tục, mỗi khối đủ Binds/Prevents/Rule.
