# Nghiên cứu kiến trúc software

📌 **Chưa có nội dung.** Thư mục dành cho phần bổ sung sau.

## Cần điền gì

- Khảo sát kiến trúc và pattern
- Đánh giá mức phù hợp với CRM
- Đánh giá mức phù hợp với ràng buộc **1 tuần** — tiêu chí riêng, khác hẳn tiêu chí dài hạn

## Nguồn tham chiếu — đừng chép lại vào đây

Kỹ thuật BABOK cho bước kiến trúc, cửa kiểm 8 ô trước khi sang `CE`, và sắc thái về ERD tối
giản của BMad — **giữ ở đúng một nơi**:

→ [`references/steps/architecture.md`](../../.claude/skills/babok-guide/references/steps/architecture.md)

Công cụ BMad hỗ trợ phần này: `TR` technical-research · `DR` domain-research (phase
`1-analysis`) · `CA` `/bmad-architecture`. Khi cần phản biện lựa chọn kiến trúc:
`/bmad-advanced-elicitation` → method `66` Architecture Decision Records.
