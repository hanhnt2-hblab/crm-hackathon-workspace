---
topic: Nửa thực thi — Why Now
updated: 2026-08-14T19:00
---

- (decision) Chốt stack giao diện: React cộng Fluent UI v9. Gói @fluentui/react-components 9.74.6 và @fluentui/react-icons 2.0.337, xác minh trên registry npm ngày 14/08. Lý do: người dùng thử chê giao diện tự chế trông cũ, và nghiên cứu cho thấy Fluent là hệ duy nhất trong ba hệ được nêu tên có công bố token đầy đủ. Không phát minh lại bánh xe.
- (decision) Không cần theme tuỳ biến. Mọi màu trong DESIGN.md đều là token chuẩn của webLightTheme, kể cả màu dòng máy vốn chỉ là colorBrandBackground2 cộng colorBrandStroke1. Bọc FluentProvider theme webLightTheme là đủ, không dùng createLightTheme.
- (decision) Không cài @fluentui/tokens riêng vì gói độc lập đó vẫn là 1.0.0-alpha.24. Lấy tokens từ @fluentui/react-components.
- (assumption) Tên khoá JS trong object tokens mới xác minh gián tiếp qua CSS custom property đọc trên trang Fluent chính chủ, chưa mở được định nghĩa JS vì các gói npm chỉ re-export. Kiểm bằng một dòng Object.keys(tokens) ngay khi dựng.
- (event) CẢNH BÁO THỜI GIAN: @fluentui/react-components có 62 dependencies. Chạy npm install trước ngày thi, đừng để nó ăn vào quỹ 4,5 tiếng.
- (assumption) Quyết định này mới chốt nửa giao diện. Backend, cơ sở dữ liệu, cách gọi mô hình và cách triển khai vẫn là đầu ra của bước bmad-architecture, chưa chạy.
- (decision) Lớp AI chạy Haiku cho mọi lệnh gọi. Ba tầng ghim model đã ghi vào AD-15 của sườn kiến trúc, không lặp lại ở đây.
- (change) Sửa lại dòng chốt stack ghi hôm nay: bước kiến trúc ĐÃ chạy trước đó và đã chốt Node 24.19, Next 16.3.1, React 19.2.8, TypeScript 5.9, Prisma 7.9.1, PostgreSQL 16, Vitest 4.1.10, Agent SDK 0.3.232. Fluent UI là phần BỔ SUNG vào bảng Stack đó, không phải quyết định stack độc lập. Sườn kiến trúc là nguồn có thẩm quyền.
- (build by ai) Cục 0 tách làm hai ở bước 4 của bmad-build: giữ C0-1…C0-6 (cổng nghiệm thu `tsc --noEmit`), hoãn C0-7/C0-8/C0-9 (cổng `npm test`). Lý do: hai cổng khác nhau nên review và merge riêng được; chín việc một spec cũng vượt xa trần 1600 token.
- (decision by user) Xung đột ① CHỐT: `gate.ts` sở hữu `GateDenyReason`; `src/core/errors.ts` gỡ `DenyReason` và `GateDenied`. Căn cứ: AD-GT-12 khai gate.ts sở hữu từ vựng đó, và AD-GT-9 nói GateDecision là GIÁ TRỊ — Cổng trả về từ chối chứ không ném, nên GateDenied là lớp lỗi không ai ném.
- (decision by user) Xung đột ② CHỐT: dùng `PrismaTx` dẫn xuất từ `$transaction` theo AD-CP-4, bỏ ép kiểu `as unknown as Tx` trong `db.ts:66`. Căn cứ: client đã $extends không assignable vào Prisma.TransactionClient (TS2345, spine ghi đã đo trên Prisma 7.9.1); dẫn xuất thì không cần ép.
- (build by ai) Spec đặt ready-for-dev: `spec-c0-frozen-contracts.md`. Chọn [K] giữ nguyên dù ước 2100–3200 token: trần 1600 đặt cho tiếng Anh, phần vượt phần lớn là dấu tiếng Việt; và cắt tiếp phá đúng mục đích — hợp đồng chỉ có giá trị khi cả năm tầng đông cứng cùng lúc.
- (build by ai) Cục 0 cài xong 16 tệp: src/autonomy/{gate,zones}.ts · src/capability/{types,registry,gate-context,errors}.ts · src/agent/types.ts · src/app/_contract.ts · src/core/<8 thực thể>/index.ts. Thân rỗng ném 'chưa hiện thực'. tsc 0 · eslint 0.
- (build by ai) ĐI LỆCH bản nháp spec, ba chỗ, đều do spine thắng theo AD-13: (1) GateDenied KHÔNG bị xoá mà chuyển về tầng ④ 'src/capability/errors.ts' — AD-UI-6 liệt nó là lỗi MONG ĐỢI server action phải bắt, nên nó vẫn phải tồn tại, chỉ đổi chủ; (2) hợp đồng tầng ① là 'src/app/_contract.ts' chứ không phải '_actions/types.ts' — AD-UI-6 chỉ đích danh và cấm tệp khác khai lại ActionState; (3) thêm eslint.config.mjs.
- (build by ai) Matrix audit lộ một lỗ THẬT: dòng 3 của ma trận I/O nói 'tầng ③ nhập giá trị ra ngoài thì tsc hoặc lint đỏ' — đo được là KHÔNG guard nào bắt, eslint.config.mjs:7 tự khai 'CHƯA CÓ LUẬT RANH GIỚI NHẬP'. Đã cài no-restricted-imports cho src/autonomy/** với allowTypeImports, và kiểm hành vi: nhập giá trị exit=1, nhập kiểu exit=0, mã hiện tại exit=0.
- (build by ai) Dòng 4 ma trận dự đoán TS2339 tại .shape; thực tế nổ TS2344 tại ràng buộc generic của defineCap — CHẶT HƠN dự đoán vì bắt ngay chỗ khai chứ không đợi chỗ dùng. Không sửa ma trận (nằm trong khối frozen), ghi lại ở đây.
- (review by ai) Bước 4: bốn lăng kính chạy lần đầu CHẾT VÌ LỖI MẠNG (3 ConnectionRefused, 1 mất kết nối) — không thu được gì. Chạy lại với hai con thì về đủ. Blind-hunter và verification-gap KHÔNG chạy được; đây là lỗ đã biết của lượt rà này.
- (review by ai) Xếp loại PATCH chứ không bad_spec: spec nói đúng ('chép từ spine, không phát minh lại'), lỗi nằm ở chỗ tôi chép bằng trí nhớ thay vì mở từng khối AD. Đã vá 10 chỗ: ba enum sai giá trị (AccountType, DropReason, SignalType), Zone 6→3 giá trị theo AD-2, BrBFlag B1-B6→B1-B4 theo AD-CR-4, thiếu STATE_CLOSED_NO_LATEST_OPEN của AD-CR-3, extractSignals đảo tham số theo AD-AG-1, bốn danh sách sổ đăng ký sai tên theo AD-CP-1, timeline khai 3 trường không có cột nào chứa, mã bịa AD-BR-D10, registry tự mâu thuẫn về ai mở giao dịch, viện AD-13 sai chỗ.
- (review by ai) Sau vá: tsc 0, eslint 0, và ba enum đối chiếu BẰNG MÁY với prisma/schema.prisma — khớp cả ba. 14 phát hiện còn lại xếp defer vào deferred-work.md vì sửa đúng cách là đổi hình dạng kiểu, không phải sửa chữ.
- (build by dev) Cục 0 (C0-1…C0-6) XONG. Đã cài: hợp đồng năm tầng bằng chữ ký kiểu + thân rỗng, 16 tệp. Chọn tách C0-7/C0-8/C0-9 ra vì hai cổng nghiệm thu khác nhau (tsc so với npm test); loại phương án gộp chín việc một spec vì vượt trần 1600 token và phá mục đích 'năm tầng đông cứng cùng lúc'. Chốt hai xung đột: GateDenyReason về gate.ts, GateDenied về tầng ④ (không xoá — AD-UI-6 liệt nó là lỗi mong đợi), Tx dẫn xuất bỏ ép kiểu. Còn nợ: 14 khoảng trống hình dạng kiểu ở deferred-work.md, và hai lăng kính blind-hunter/verification-gap chưa chạy được vì lỗi mạng.
- (review by ai) Hai lăng kính còn thiếu đã chạy lại xong trên diff SAU vá. Bắt được một HỒI QUY do chính bản vá của tôi gây ra: đổi Tx sang dạng dẫn xuất làm MẤT vế chặn $transaction. Kiểu ITX của Prisma tự gỡ $extends/$connect/$disconnect/$use nhưng GIỮ $transaction, nên lồng giao dịch biên dịch sạch — trong khi bản Omit thủ công trước đó chặn được. Đã đo cả hai chiều, đã vá bằng Omit<derived, '$transaction'>, đo lại: TS2339.
- (review by ai) CreateSignalInput không ghi nổi một hàng signal hợp lệ: thiếu accountId, quoteStart, quoteEnd (cả ba NOT NULL) và thừa eventDate vốn không có cột nào chứa (grep event_date prisma/ trả 0). Đã bổ sung ba trường, thu hẹp signalType thành enum đóng, và ghi rõ câu hỏi còn treo về nơi lưu ngày sự kiện.
- (question by ai) CHỜ NGƯỜI QUYẾT — ngày sự kiện không có chỗ lưu. BR-D7 tính hạn đo từ ngày sự kiện (D5 lui về ngày Bản lưu), mô hình đọc được nó ở tầng ②, nhưng bảng signal không có cột. Hai đường: thêm cột event_date, hoặc chốt rằng hạn tính đúng một lần và ghi lý do vào next_action. Đường thứ hai vỡ khi tin cũ tới sau cần so ngày sự kiện (BR-D8).
- (review by ai) Bốn tầng ①②④⑤ KHÔNG có luật ranh giới nhập nào — đã đo bằng tệp thăm dò: src/app nhập thẳng db và tx rồi ghi Postgres không qua loadCapability, qua sạch cả tsc lẫn eslint; src/agent nhập @/core và @/capability, qua sạch; src/capability gọi tx() mở giao dịch, qua sạch. Cả bảy bước của AD-4 bỏ được mà không một dòng cảnh báo.
- (build by ai) Cài luật ranh giới nhập cho BỐN tầng còn lại theo đúng bảng danh-sách-cho-phép của AD-1 (không theo trực giác). allowTypeImports khác nhau có chủ đích: tầng ② và cạnh core→capability đặt false vì AD-AG-3 và AD-GT-12 cấm cả cạnh KIỂU (kéo Zod/Prisma vào là npm test trên clone sạch đỏ khi prisma generate chưa chạy); các cạnh còn lại đặt true vì chúng vốn ở phía có Prisma.
- (event by ai) Đo bằng bốn tệp thăm dò, cả bốn ĐỎ với đúng mã AD của mình: src/app nhập db+tx (AD-1·AD-4), src/agent nhập @/core (AD-AG-3), src/capability gọi tx() (AD-CR-7), src/core nhập @/capability (AD-1). Hai đối chứng: src/app nhập KIỂU Tx lọt exit=0; src/capability nhập GIÁ TRỊ db bị chặn exit=1. Mã hiện tại eslint 0, tsc 0.
- (build by ai) Sửa kèm: tám tệp src/core/*/index.ts đang nhập PrismaTx từ @/capability/types — vi phạm AD-1 (chiều là ④→⑤). Đổi sang import type { Tx } from @/core/db, và luật mới cưỡng chế điều đó.
- (question by ai) CÒN NỢ sau khi cài luật: instrumentation.ts nằm ở GỐC dự án, ngoài mọi glob src/**, nên không khối nào phủ nó — mà AD-1 gọi đúng nó là tệp nguy hiểm nhất vì nó khởi động vòng quét. Và dạng nội dòng 'import { type X }' vẫn qua mặt allowTypeImports (xét ở mức cả câu lệnh); cần no-import-type-side-effects của typescript-eslint.
- (change) Sua engines trong package.json tu >=22.6 thanh >=24.19.0. Khoang 22.6-23.5 la bay that: kiem engines qua nhung npm run seed chay node prisma/seed.ts truc tiep, ma Node boc kieu TypeScript khong can co chi tu 23.6 — may cham dung Node 22.x se chet o cong 7.5 voi trieu chung tro vao seed chu khong vao Node.
- (change) Refresh khoi bmad:context trong AGENTS.md theo thuc trang sau khi dung khung: bo cau src/ chua co ma chay duoc va ca khoi TODO lenh (package.json va CHECKLIST.md nay da khai), bo dong xac thuc AI (.env.example noi ky hon), them con tro epics.md, spec-c0-frozen-contracts.md, deferred-work.md, nam suon tang 67 AD co tien to, va bon bay quan sat duoc. Khoi 1495 -> 1664 tu.
- (build by ai) Bịt nốt hai lỗ ranh giới: (1) instrumentation.ts ở gốc dự án nay có khối riêng — AD-1 cho nó nhập src/capability và chỉ thế; đo được: nhập db → exit=1 kèm đúng thông điệp AD-1. (2) Bật @typescript-eslint/no-import-type-side-effects trên toàn mã đội — không có nó thì MỌI allowTypeImports ở trên bị qua mặt, vì tuỳ chọn đó xét ở mức cả câu lệnh nên 'import { type X }' im lặng đi qua. Đo: dạng nội dòng exit=1 (trước đây im lặng), dạng tách rời exit=0.
- (build by ai) C0-7/C0-8/C0-9 xong. tests/global-setup.ts, tests/setup.ts, tests/unit/{stage,contracts}.test.ts (36 phép kiểm chuyển từ scratchpad vào repo), và 11 tệp T*.test.ts với 49 it.todo giữ NGUYÊN VĂN điều kiện đề bài §6. npm test exit=0.
- (decision by ai) KHÔNG dùng 'prisma migrate reset' trong global-setup: cờ --skip-seed ĐÃ BỊ GỠ ở Prisma 7 (đo được: 'unknown or unexpected option'), không có cờ thay thế, nên nó luôn chạy node prisma/seed.ts — tệp chưa tồn tại. Cũng KHÔNG dùng 'db push --force-reset': nó dựng từ schema.prisma và BỎ QUA migration, tức mất sạch 4 CHECK + 4 chỉ mục một phần viết tay, và mất IM LẶNG. Đường chọn: DROP SCHEMA public CASCADE qua client rồi 'migrate deploy' — áp lại chính tệp migration giám khảo sẽ chạy.
- (event by ai) Đo sau khi chạy: CSDL demo 5442 giữ nguyên 20 bảng (không bị đụng); CSDL test 5443 dựng lại 20 bảng; và cả 8 ràng buộc viết tay sống sót đúng tên — opp_amount_non_negative, opp_latest_open_stage_iff, opp_latest_open_stage_running, signal_subtype_only_other, contact_one_primary, next_action_one_active, suggestion_one_pending_per_slot, user_email_active. Đó là bằng chứng migration THẬT SỰ chạy chứ không phải db push.
- (build by ai) Gán process.env.NODE_ENV làm ĐỎ CẢ npm run build, không chỉ tsc: Next khai nó readonly (TS2540) và next build có bước type check. Vitest vốn tự đặt NODE_ENV=test, nên đổi gán thành KHẲNG ĐỊNH — vừa hết lỗi vừa báo ngay nếu Vitest đổi mặc định.
- (build by ai) Năm lệnh §7.3: build/test/stop exit=0, stop luỹ đẳng (chạy hai lần đều 0), test tự dựng CSDL của nó sau khi đã stop sạch. seed exit=1 ĐÚNG như mong đợi — prisma/seed.ts là C5-17 của cục ⑤, không thuộc Cục 0.
