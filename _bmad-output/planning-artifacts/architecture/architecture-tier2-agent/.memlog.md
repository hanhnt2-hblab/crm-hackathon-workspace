---
scope: Tầng ② Agent Runtime — src/agent
purpose: Spine epic-altitude, thừa kế AD-1..AD-22
altitude: epic
updated: 2026-08-14T12:13
---

- (event) bắt đầu spine tầng ② Agent Runtime, epic altitude, thừa kế spine cha architecture-crm-hackathon-2026-08-14
- (constraint) AD-1 (cha) — bốn tuỳ chọn query() là một khối không tách rời: tools:[] · mcpServers · allowedTools:["mcp__crm__*"] · permissionMode:"dontAsk"; ranh giới nhập viết theo danh sách cho phép. Read-only ở tầng này
- (constraint) AD-2 (cha) — sổ đăng ký Capability là tập đóng ba khối; tầng ② không thêm, không bớt, không đọc trực tiếp
- (constraint) AD-3 (cha) — máy có đúng ba chạm ghi vào Hồ sơ chính thức; tầng ② không phải nơi quyết định chạm nào chạy
- (constraint) AD-4 (cha) — Cổng tự chủ có chuỗi bảy bước cố định, hàm thuần, ghi vết cả khi từ chối; tầng ② không bao giờ tự quyết cho phép
- (constraint) AD-5 (cha) — actor là tham số bắt buộc, không phải ngữ cảnh ngầm; mô hình không bao giờ được chọn actor
- (constraint) AD-6 (cha) — src/agent KHÔNG nhập gì từ src/core; JSON Schema khai draft-07 dựng bằng z.toJSONSchema(schema,{target:"draft-7"}); nguồn enum là ontology §8
- (constraint) AD-7 (cha) — một hàm duy nhất dựng lời nhắc ra biên, nhận đúng ba tham số: Bản lưu chuẩn hoá · Loại công ty · danh sách enum. Đầu ra là đúng lược đồ Phát hiện
- (constraint) AD-8 (cha) — bốn nguyên thủy; Claim buộc neo nguồn; đích so khớp câu trích là bản CHUẨN HOÁ, không phải bản thô
- (constraint) AD-9 (cha) — BR-D là ràng buộc, BR-B là cờ; tầng ② không tính cờ nào
- (constraint) AD-10 (cha) — máy ghi bằng kiểm-và-ghi nguyên tử; 0 dòng thì bỏ lượt ghi, không thử lại
- (constraint) AD-11 (cha) — giới hạn và năm điều kiện dừng tường minh; NFR-2 đếm lệnh gọi mô hình; ba subtype lỗi của SDK đã có ánh xạ; đơn vị công việc là một Công ty trọn vẹn
- (constraint) AD-12 (cha) — vòng quét chốt tập Công ty lúc bắt đầu, khoá theo Công ty bằng hàng trong bảng
- (constraint) AD-13 (cha) — ontology src/ontology/crm.ontology.md là tài sản lúc chạy và là trọng tài; thứ tự phân xử đề bài > Mục 0 > PRD > ontology > lược đồ
- (constraint) AD-14 (cha) — mọi xoá là xoá mềm; thời gian lưu UTC
- (constraint) AD-15 (cha) — env chỉ chứa bí mật và địa chỉ hạ tầng; đúng hai tệp được đọc process.env là src/config.ts và instrumentation.ts. src/agent KHÔNG nằm trong danh sách
- (constraint) AD-16 (cha) — cache hai tầng; khoá tầng đội là hash đúng ba tham số của AD-7 cộng phiên bản lời nhắc, TTL 24h, không chứa ngày/giờ/số ngẫu nhiên
- (constraint) AD-17 (cha) — lược đồ BTC vào qua đúng một module ánh xạ src/ingest/mapping.ts
- (constraint) AD-18 (cha) — chuẩn hoá văn bản sống ở src/core/normalize.ts; src/agent nhận bản chuẩn hoá đã lưu và KHÔNG có hàm chuẩn hoá riêng
- (constraint) AD-19 (cha) — sổ đăng ký phủ mọi thao tác chạm dữ liệu, đọc lẫn ghi
- (constraint) AD-20 (cha) — nạp dữ liệu là actor thứ ba kind='seed'
- (constraint) AD-21 (cha) — hệ quả dây chuyền giữ nguyên actor khởi phát, ghi vết mang causedBy
- (constraint) AD-22 (cha) — mô hình sinh Claim; hệ thống sinh Proposal. Đầu ra src/agent là đúng lược đồ Phát hiện, KHÔNG trường nào của Gợi ý
- (version) xác minh API SDK 0.3.232 ngày 14/08 qua tài liệu code.claude.com: permissionMode nhận 'dontAsk' — hợp lệ, đúng thứ đội đã đo; allowedTools nhận ký tự đại diện dạng mcp__server__*; tên tool MCP là mcp__<server>__<tool>; createSdkMcpServer({name,version,tools}) + tool(name,desc,zodSchema,handler)
- (version) result message có năm subtype: success · error_max_turns · error_max_budget_usd · error_during_execution · error_max_structured_output_retries. Trường: total_cost_usd · usage · num_turns · session_id · stop_reason (có giá trị 'refusal') · structured_output (chỉ khi success và có outputFormat)
- (version) outputFormat: {type:'json_schema', schema}. SDK kiểm draft-07 và tự thử lại khi lược đồ không khớp; hết lượt thì subtype = error_max_structured_output_retries. Tên tuỳ chọn giới hạn số lần thử lại ⚠ chưa xác minh
- (version) ToolSearch bật mặc định; tools:[] xoá built-in nhưng ToolSearch VẪN áp lên tool MCP, và lần khám phá tool đầu tiên tốn thêm một lượt do maxTurns đếm. Có biến môi trường ENABLE_TOOL_SEARCH (true|false|auto|auto:N)
- (version) thứ tự chọn xác thực: nhà cung cấp mây > ANTHROPIC_AUTH_TOKEN > ANTHROPIC_API_KEY > apiKeyHelper > CLAUDE_CODE_OAUTH_TOKEN > hồ sơ Anthropic > đăng nhập subscription. Đặt ANTHROPIC_API_KEY ở tiến trình cha là đủ để lật sang API key
- (version) tuỳ chọn env của query() trong bản TypeScript THAY THẾ toàn bộ môi trường tiến trình con, không hợp nhất — truyền nó sẽ xoá HOME và PATH, tức phá luôn đường đọc ~/.claude/.credentials.json
- (version) query() là async generator một lượt; phát result lỗi rồi NÉM. Phải tiêu thụ hết generator trong try/catch
- (version) ⚠ chưa xác minh: mỗi query() có sinh tiến trình con mới hay dùng lại; SDK có tự thử lại lỗi mạng tầng HTTP không; trường duration_ms và permission_denials
- (decision) AD-AG-1 — bề mặt công khai của src/agent là đúng một hàm extractSignals(input, deps); mọi phụ thuộc ngoài TIÊM vào qua deps. Lý do: AD-1 chỉ cho src/autonomy nhập src/capability, nên src/agent KHÔNG có đường hợp pháp để tự lấy MCP server. Tiêm là nước đi duy nhất không nới ranh giới nhập của cha
- (decision) phương án đã loại cho AD-AG-1: (a) src/agent nhập src/capability/mcp-server.ts — loại vì vi phạm thẳng danh sách cho phép của AD-1; (b) src/agent đọc ontology bằng readFileSync — loại vì đẻ ra chủ sở hữu thứ hai cho bẫy outputFileTracingIncludes của AD-13 và làm tầng ② không kiểm thử được nếu thiếu tệp; (c) singleton dựng sẵn trong module — loại vì thứ tự nạp module quyết định hành vi, đúng kiểu ngữ cảnh ngầm AD-5 cấm
- (decision) AD-AG-2 — một Bản lưu là một query(); không truyền resume/continue/sessionId; không gộp lô nhiều Bản lưu
- (decision) phương án đã loại cho AD-AG-2: một phiên dài dùng lại cho cả 15 Công ty để chia đều ~2,5k token phí tổn. Loại vì ba lý do đo được: khoá cache AD-16 chỉ gồm ba tham số nên ngữ cảnh mang theo từ Công ty trước làm đầu ra không còn là hàm của khoá; đơn vị công việc một Công ty trọn vẹn của AD-11 mất nghĩa; và một lượt bị nhiễm làm hỏng mọi lượt sau. Bù chi phí bằng prompt caching phía Anthropic (TTL 1h) vẫn trúng giữa các tiến trình con liên tiếp
- (decision) AD-AG-3 — src/agent là bộ biến đổi thuần: KHÔNG gọi capability nào trong đường rút Phát hiện. Dữ liệu vào bằng tham số theo AD-7, ra bằng structured_output
- (decision) phương án đã loại cho AD-AG-3: agent tự gọi mcp__crm__readArticle / readAccountType / listEnums để tự đi lấy dữ liệu. Loại vì: mỗi lượt tool là một lượt mô hình nên trần 20 của NFR-2 bị ăn bởi việc lấy dữ liệu chứ không bởi việc suy luận; đầu vào không xác định trước lúc gọi nên không dựng được khoá cache AD-16; và nó đi vòng qua chính hàm ba-tham-số mà AD-7 chốt. MCP server vẫn đăng ký đúng AD-1 làm lớp phòng thủ, không phải để dùng
- (decision) AD-AG-4 — khoá server MCP là chuỗi 'crm' nguyên văn; tên tool MCP bằng ĐÚNG tên capability; input schema không bao giờ chứa actor/userId/role/seedMode, handler đóng kín trên actor hệ thống
- (decision) AD-AG-5 — lược đồ Phát hiện là một đối tượng bọc {signals: [...]}, mỗi phần tử đúng sáu trường: statement · signal_type · signal_subtype · quote · confidence · relevance. additionalProperties:false, mọi trường required. KHÔNG có quote_start/quote_end (lõi tính theo AD-18), KHÔNG trường nào của Gợi ý (AD-22)
- (decision) phương án đã loại cho AD-AG-5: (a) trả mảng trần thay vì đối tượng bọc — loại vì mất chỗ cho kênh trả lời rỗng và một số đường structured output đòi gốc là object; (b) để mô hình trả offset câu trích — loại vì AD-18 nói offset tính trên bản chuẩn hoá ở lõi; hai nơi tính offset là đúng lỗi lệch một ký tự mà AD-18 sinh ra để chặn
- (decision) AD-AG-6 — danh sách enum vào src/agent bằng DỮ LIỆU (tham số thứ ba của AD-7), không đọc tệp. buildSignalSchema(enums) là hàm dựng lược đồ duy nhất; một phép kiểm khẳng định $schema sinh ra đúng draft-07
- (decision) AD-AG-6 phần hai — luật 'signal_subtype chỉ có nghĩa khi signal_type=other' KHÔNG cài bằng if/then của JSON Schema; nó là BR-D cưỡng chế ở lõi. Lý do: mức hỗ trợ if/then draft-07 của bộ kiểm phía SDK chưa xác minh, mà AD-6 đã ghi rõ lược đồ không hợp lệ giết cả lượt chạy ngay lúc khởi động. Lược đồ khai signal_subtype là enum-hoặc-null, required
- (decision) AD-AG-7 — PROMPT_VERSION là hằng số xuất khẩu từ src/agent/prompt.ts, dạng chuỗi tăng dần. Nó phủ CẢ prompt.ts LẪN schema.ts: đổi bất cứ thứ gì làm đổi byte gửi ra biên đều phải tăng phiên bản. Lấp khoảng trống của AD-16 (khoá cache chỉ nhắc 'phiên bản lời nhắc', không nhắc phiên bản lược đồ) mà không nới nó
- (decision) AD-AG-8 — lượt chạy không được phụ thuộc máy người chạy: systemPrompt là chuỗi tường minh của đội (KHÔNG dùng preset claude_code), settingSources rỗng, model ghim tường minh qua deps, cwd cố định. Và KHÔNG BAO GIỜ truyền tuỳ chọn env: bản TypeScript thay thế toàn bộ môi trường tiến trình con, xoá HOME nên xoá luôn đường đọc credentials subscription
- (decision) AD-AG-8 phần hai — đường lui API key là KHÔNG CÓ MÃ NÀO: đặt ANTHROPIC_API_KEY ở môi trường tiến trình cha trước khi npm start, tiến trình con thừa kế, thứ tự chọn xác thực của SDK ưu tiên nó hơn đăng nhập subscription. src/agent không đọc, không đặt, không xoá biến xác thực nào. Phép kiểm khẳng định không tệp nào dưới src/agent chứa chuỗi ANTHROPIC_ hay apiKey
- (decision) phương án đã loại cho AD-AG-8: truyền khoá API qua deps rồi options.env — loại vì tuỳ chọn env thay thế chứ không hợp nhất môi trường, tức chính đường lui lại phá đường chính; và vì nó kéo một bí mật vào chữ ký hàm của tầng ②, trong khi AD-15 giữ bí mật ở src/config.ts
- (decision) AD-AG-9 — ba lớp lỗi, ba cách xử. Lược đồ không khớp: SDK tự thử lại, đội KHÔNG thử lại thêm lần nào. Lỗi vận chuyển (tiến trình con không khởi động được, đứt mạng, query() ném trước khi có result): thử lại ĐÚNG MỘT LẦN, chờ cố định 2 giây, cùng Bản lưu, cùng khoá cache. Mọi subtype lỗi có result: KHÔNG thử lại
- (decision) phương án đã loại cho AD-AG-9: thử lại theo cấp số nhân ba lần. Loại vì NFR-1 cấm dồn hàng đợi và chu kỳ vòng quét đo cuối-đến-đầu — ba lần chờ nhân với 15 Công ty đẩy vòng quét vượt chu kỳ và làm vòng kế bị bỏ, tức đổi một lỗi tạm thời lấy một vòng mất trắng
- (decision) AD-AG-10 — luật đếm: bộ đếm NFR-2 tăng đúng một đơn vị mỗi lần src/agent gọi query(), tăng TRƯỚC khi gọi; nó KHÔNG BAO GIỜ đọc num_turns. maxTurns là phanh phụ chống chạy loạn, đặt bằng 4 để chứa một lượt ToolSearch cộng vài lượt thử lại lược đồ. Nhờ vậy ToolSearch không thể làm lệch trần 20
- (decision) AD-AG-10 phần hai — chi phí trả về là costUsd: number | null. null nghĩa là KHÔNG ĐO ĐƯỢC, không bao giờ trả 0. Cha đã ghi rằng error_during_execution có thể trả mọi trường chi phí bằng 0; trả 0 làm phanh ngân sách NFR-3 rò rỉ im lặng
- (decision) AD-AG-11 — đầu ra rỗng KHÔNG phải lỗi: signals=[] hợp lệ nghĩa là Bản lưu này không có Phát hiện nào; ghi một dòng nhật ký, đi tiếp, không thử lại. Từ chối nhận diện bằng CẤU TRÚC: result.stop_reason==='refusal', không bằng so khớp chuỗi
- (decision) AD-AG-11 phần hai — không thêm mã F mới. Phân loại theo cột 'ai sửa' của cha: error_max_structured_output_retries là F5 (sửa JSON Schema); từ chối hoặc rỗng liên tiếp nhiều Bản lưu khác nhau là F1 (sửa lời nhắc); câu trích không khớp bản chuẩn hoá là F1; mô hình gọi tool trong đường thuần biến đổi là F4
- (assumption) total_cost_usd là ước lượng phía client tính từ token đếm được nhân đơn giá mô hình. Dưới đăng nhập subscription KHÔNG có hoá đơn nào để đối chiếu, nên với dự án này nó là ĐẠI LƯỢNG THAY THẾ cho khối lượng token, không phải tiền quyết toán. Phanh ngân sách NFR-3 vì thế là phanh khối lượng dùng đơn vị đô la
- (assumption) phí tổn ~2.548 token nạp bộ đệm mỗi lượt nguội đo được ngày 14/08 là của cấu hình lúc đo. AD-AG-8 dùng systemPrompt tường minh thay preset claude_code, nên con số này DỰ KIẾN giảm mạnh — chưa đo lại, phải đo trước khi ghi vào bất kỳ tính toán ngân sách nào
- (assumption) chi phí đo được 0,025–0,039 đô mỗi lệnh gọi rút Phát hiện, tức một vòng chạm trần 20 lệnh rơi vào 0,50–0,80 đô. Giả định chống đỡ: một Bản lưu 2–5 KB, một lệnh gọi cho mỗi Bản lưu, không lượt tool nào
- (assumption) ToolSearch không tự kích hoạt trong đường thuần biến đổi vì mô hình không cần tool nào. Nếu num_turns > 1 trên một lượt không gọi tool thì giả định này sai — đó chính là phép đo phải chạy trên một Công ty thật
- (question) ai là composition root được phép nhập src/capability để dựng MCP server và trao cho tầng ②? Danh sách cho phép của AD-1 chỉ cho src/autonomy, nhưng loadCapability lại nằm ở src/capability/registry.ts và src/app, src/scan, prisma/seed.ts, tests đều cần nó. Xung đột thuộc về spine cha, tầng ② né bằng cách tiêm
- (question) AD-11 ánh xạ ba subtype của SDK nhưng SDK có năm; error_max_turns chưa có ánh xạ. Theo lập luận của chính cha thì một nhánh không ánh xạ là F9
- (question) AD-11 đưa error_during_execution về điều kiện dừng 5 tức KẾT THÚC VÒNG. Một Bản lưu hỏng giết cả vòng và bỏ lại 14 Công ty chưa quét, nghịch với luật đơn vị công việc một Công ty trọn vẹn của chính AD-11. Tầng ② TUÂN THỦ, không tự nới; nêu lên để cha quyết
- (question) quy ước tên capability của cha ghi 'tiếng Việt không dấu' nhưng ví dụ lại là appendTimelineEntry (tiếng Anh). AD-AG-4 đặt tên tool MCP bằng đúng tên capability nên mơ hồ này rò ra tới dây. Tầng ② chọn tiếng Anh theo quy ước định danh của dự án
- (direction) phép đo bắt buộc sáng 15/08 trước mọi việc khác, theo đúng thứ tự: (1) đăng nhập subscription còn chạy không; (2) in num_turns và usage của một Công ty thật để chốt luật đếm AD-AG-10; (3) đo lại phí tổn token sau khi bỏ preset claude_code
- (decision) AD-AG-6 phần ba — enum đi vào qua capability listEnums, không phải readFileSync. Đây là cách chở chứ không đổi nguồn: AD-6 vẫn đúng, ontology §8 vẫn là nguồn, và AD-13 vẫn phải ghim tệp .md vào bản build vì listEnums, Cổng và tests đều đọc nó
- (event) hoàn tất ARCHITECTURE-SPINE.md tầng ②: 11 AD-AG, 4 xung đột nêu lên với cha (X1 composition root, X2 thiếu ánh xạ error_max_turns, X3 error_during_execution giết cả vòng, X4 quy ước tên capability), 0 override, lint total_findings 0
