---
scope: Tầng ③ Cổng tự chủ — src/autonomy
purpose: Spine epic-altitude, thừa kế AD-1..AD-22
altitude: epic
updated: 2026-08-14T13:03
---

- (event) Kiểm mã hoá tiếng Việt có dấu — ăn thử một dòng
- (constraint) AD-1 (cha) — không đường nào tới lõi mà không qua Capability, không Capability nào chạy mà không qua Cổng. Ranh giới nhập là DANH SÁCH CHO PHÉP quét toàn repo, cưỡng chế bằng no-restricted-imports. src/autonomy KHÔNG được nhập @prisma/client.
- (constraint) AD-2 (cha) — sổ đăng ký Capability là tập ĐÓNG, ba khối; mỗi mục mang ten, vung, risk, allowedActors, requiresSignalSource, cascades. risk KHÔNG tham gia quyết định của Cổng — nó chỉ vào ghi vết và giao diện.
- (constraint) AD-3 (cha) — máy có đúng ba chạm ghi vào Hồ sơ chính thức: appendTimelineEntry, setNextAction, disableAi. Vị từ ⓐⓑⓒ của setNextAction là luật LÕI, không phải luật Cổng.
- (constraint) AD-4 (cha) — chuỗi bảy bước cố định, dừng ở lần từ chối đầu, thứ tự TỪ HẸP TỚI RỘNG; gate.ts là hàm THUẦN decide(entry, actor, ctx); registry.ts là nơi DUY NHẤT gọi decide và ghi vết; chế-độ-gieo là tham số dựng createGate; bước ⑦ kiểm phanh ở MỖI lời gọi; từ chối cũng ghi một dòng ghi vết.
- (constraint) AD-5 (cha) — Actor là tham số đầu tiên bắt buộc, kiểu ba nhánh human/system/seed; nhánh human MANG SẴN userId và vai. Không đọc từ session, biến toàn cục, hay AsyncLocalStorage.
- (constraint) AD-11 (cha) — mọi ngưỡng là hàng trong bảng settings, không phải hằng số biên dịch cứng; lượt bị Cổng từ chối KHÔNG tính vào trần 20 lệnh gọi mô hình; max_consecutive_denials do vòng quét đếm, không do Cổng.
- (constraint) AD-13 (cha) — ontology là tài sản lúc chạy; danh sách vùng và ranh giới trong src/autonomy ĐỐI CHIẾU §6 của nó. next build chỉ truy vết tệp qua import.
- (constraint) AD-15 (cha) — env chỉ chứa bí mật và địa chỉ hạ tầng; tham số nghiệp vụ nằm trong bảng settings, đọc MỖI LẦN DÙNG. Đúng hai tệp được đọc process.env, và gate.ts không nằm trong hai tệp đó.
- (constraint) AD-19 (cha) — sổ đăng ký phủ MỌI thao tác chạm dữ liệu, đọc lẫn ghi, mọi actor. Nghĩa là Cổng nằm trên đường đi của cả truy vấn đọc phục vụ giao diện.
- (constraint) AD-20 (cha) — actor seed đi qua Cổng như mọi actor khác; nó chỉ chạy được khi Cổng dựng ở chế-độ-gieo; nó bỏ qua các vị từ chỉ có nghĩa lúc vận hành; nó không sinh ghi vết trừ khi có cờ giữ-ghi-vết.
- (constraint) AD-21 (cha) — hệ quả dây chuyền chạy trong cùng giao dịch, cùng actor, ghi vết mang causedBy. Chúng thuộc khối nội bộ, không vào tập T-10 khẳng định bằng đúng.
- (decision) AD-GT-1 — GateContext gồm ĐÚNG NĂM trường sự kiện: aiEnabled, modelCallsUsed, modelCallsLimit, budgetSpentUsd, budgetLimitUsd. Không trường vai, không trường phán quyết dựng sẵn kiểu limitExceeded. Lý do: (a) vai đã nằm trong actor theo AD-5, đưa vào ctx là hai nguồn sự thật cho cùng một giá trị và không ai khai bên nào thắng khi lệch; (b) một cờ dựng sẵn chuyển quyết định ra khỏi hàm thuần vào bên gom, tức hai chỗ cùng quyết. Phương án đã loại: ctx mang vai đọc tươi từ cơ sở dữ liệu — an toàn hơn khi vai đổi giữa phiên, nhưng PRD §6 và mục Vòng đời vai của cha đã đặt việc đổi vai NGOÀI phạm vi, nên lợi ích bằng không mà cái giá là hai nguồn sự thật.
- (decision) AD-GT-2 — collectGateContext chạy MỖI lời gọi capability. Phương án đã loại: chụp một lần mỗi vòng quét rồi truyền xuống — rẻ hơn nhưng T-9 và FR-45 đòi phanh có hiệu lực NGAY, mà ảnh chụp đầu vòng làm bước ⑦ mù suốt phần còn lại của vòng; và AD-4 của cha đã chốt bước ⑦ kiểm ở mỗi lời gọi, nên đó là xung đột chứ không phải lựa chọn. Cửa sổ đua còn lại — bấm phanh giữa lúc gom và lúc quyết — không cần cơ chế bù: giữa collect và decide không có I/O, decide thuần và đồng bộ, nên cửa sổ nhỏ hơn hẳn ranh giới Công ty mà FR-45 vốn đã cho phép chạy nốt.
- (decision) Số đo cho câu chi phí gom: một vòng quét cỡ 15 Công ty sinh khoảng 75 lời gọi capability; mỗi lần gom là hai truy vấn nhỏ — settings và bộ đếm của lượt quét đang chạy — tức khoảng 150 truy vấn mỗi vòng. Ở nhịp demo 60 giây là dưới 3 truy vấn mỗi giây trên Postgres cục bộ. Phản đối chi phí bị bác bằng số, không bằng lập luận.
- (decision) AD-GT-2 phần hai — collectGateContext hỏng thì KHÔNG đổi thành từ chối. Ném ra ngoài, không capability nào chạy, và không ghi dòng GateDenied nào. Lý do: một dòng ghi vết mang mã brake khi thật ra cơ sở dữ liệu chết là nói dối ngay trong bằng chứng dùng để chứng minh F8; nó xếp nhầm F10 thành F8.
- (decision) AD-GT-3 — zones.ts khai hằng số TypeScript, không đọc tệp ontology lúc chạy, không sinh mã lúc build. Một phép kiểm đối chiếu ba vùng và năm ranh giới với ontology §6. Phương án đã loại: (a) đọc .md lúc chạy — kéo I/O vào src/autonomy, làm rỗng lời hứa thuần của gate.ts vì nhập gate kéo theo đọc tệp, và buộc mọi phép kiểm phải có tệp thật; (b) sinh .ts lúc build — làm lệch KHÔNG TỒN TẠI ĐƯỢC, đó là ưu thế thật, nhưng nó thêm một bước sinh mã vào chuỗi npm ci và npm run build mà §7 đòi chạy từ clone sạch, và bước sinh mã không được nối vào thì bản dựng vẫn xanh với mã cũ, tức đổi kiểu hỏng từ ồn sang im. Chốt (c) vì nó dùng ĐÚNG cơ chế AD-6 đã dùng cho enum: lệch là đỏ ở npm test, mà npm test là cổng nộp bài §7.4.
- (decision) AD-GT-4 — src/autonomy không có một lời nhập GIÁ TRỊ nào ra ngoài chính nó; các kiểu Actor và RegistryEntry vào bằng import type, cưỡng chế bằng no-restricted-imports của typescript-eslint với allowTypeImports bật. Không có dòng này thì gate.ts không gõ được kiểu tham số của chính nó mà không vi phạm AD-1 — một bức tường dựng đúng lúc 9:30 sáng.
- (decision) AD-GT-5 — ranh giới gắn vào CAPABILITY, không vào tham số lời gọi. Mục sổ đăng ký khai thêm hai trường: touches — danh sách mã ranh giới NFR-14, NFR-15, NFR-16, NFR-17, NFR-19 — và allowedRoles. crossesBoundary nhận entry và actor, không nhận tham số lời gọi. Phương án đã loại: nhét tham số lời gọi vào ctx để bước ④ soi giá trị — giữ được tính phụ thuộc giá trị nhưng đổi lại decide phải hiểu chín hình dạng tham số khác nhau, tức chín phép kiểm tự chế, đúng thứ spine sinh ra để chặn; và nó phải sửa chữ ký decide mà AD-4 đã chốt. Cách giữ phụ thuộc giá trị mà không mất gì: TÁCH CAPABILITY — capability nào chạm được ô tiền thì khai NFR-15 và chỉ người dùng được; cần cho máy thì tách ra một capability hẹp hơn không chạm ô tiền.
- (decision) AD-GT-6 — với sổ chín capability hiện tại, bước ④ KHÔNG CÓ ĐƯỜNG TỚI: cả năm ranh giới đã bị chặn sớm hơn, hoặc ở bước ① vì capability không tồn tại — sửa Mục dòng thời gian — hoặc ở bước ③ vì allowedActors không có hệ thống — đổi Giai đoạn, sửa giá trị tiền, xoá Công ty, tự liên hệ khách. Không bỏ bước ④ vì AD-4 là read-only, và vì nó là lưới bắt đúng lần nới allowedActors đầu tiên. Đổi lại nó phải được chứng minh bằng một entry TỔNG HỢP dựng trong kiểm thử, không phải bằng entry thật.
- (decision) AD-GT-7 — createGate gọi ở đúng ba gốc dựng: instrumentation.ts, gốc dựng của src/app, và prisma/seed.ts. Sổ đăng ký NHẬN Cổng qua tham số dựng chứ không nhập sẵn một thể hiện dùng chung; tests dựng bao nhiêu thể hiện tuỳ ý. Phương án đã loại: xuất sẵn một thể hiện mặc định ở mức module — tiện hơn nhưng khi đó chế-độ-gieo do THỨ TỰ NHẬP quyết định, và tests cần cả hai giá trị trong cùng tiến trình sẽ lấy nhầm cái được nạp trước.
- (decision) AD-GT-8 — allowedActors KHÔNG BAO GIỜ chứa nhánh gieo. Đường duy nhất của actor gieo là bước ②. Không có luật này thì người dựng sẽ thêm nhánh gieo vào cả chín mục cho tiện, và khi đó chế-độ-gieo không còn chặn được gì: npm start cũng gieo được.
- (decision) AD-GT-9 — GateDenyReason là union đóng sáu mã đúng như AD-4 đặt tên; decide TRẢ VỀ GIÁ TRỊ và không bao giờ ném; GateDenied là lớp lỗi của tầng ④. Ánh xạ HTTP: unknown_capability 500 — lỗi lập trình, KHÔNG phải 404 vì 404 trong từ vựng giao diện nghĩa là không tìm thấy bản ghi; actor_not_allowed, boundary, role đều 403; limit và brake 503. Hai mã cuối thực tế không tới được tầng ① vì chúng chỉ áp cho actor hệ thống. Chốt kèm: T-10 mong đợi actor_not_allowed cho cả BA lần thử, không phải boundary — hệ quả trực tiếp của thứ tự hẹp-tới-rộng ở AD-4.
- (decision) AD-GT-10 — bảng ca kiểm của decide là hợp đồng, không phải gợi ý: bảy ca một-bước-một cộng năm cặp ưu tiên. decide thuần nên toàn bộ bảng chạy không cần cơ sở dữ liệu, và đó là lý do bảng này bắt buộc chứ không tuỳ nghi.
- (decision) decide trả về ĐỒNG BỘ, không trả Promise. Một chữ ký async là lời mời để ai đó nhét await vào giữa chuỗi bảy bước, và khi đó lời hứa thuần chết mà không lint nào bắt được.
- (question) Xung đột 1, chặn — bảng danh sách cho phép của AD-1 cho phép nhập src/capability ở src/autonomy, tức chiều ③ nhập ④, nhưng KHÔNG có dòng nào cho phép ④ nhập ③. Mà AD-4 đòi registry.ts gọi decide, tức ④ PHẢI nhập ③. Đọc đúng chữ bảng thì eslint đỏ ngay dòng nhập đầu tiên của registry.ts, và eslint là cơ chế cưỡng chế duy nhất của AD-1. Không tự sửa spine cha.
- (question) Xung đột 2 — chú thích trong AD-4 viết collectGateContext gom ngân sách, phanh, VAI, trong khi AD-5 đặt vai bên trong actor. Hai nguồn sự thật cho cùng một giá trị, và không AD nào nói bên nào thắng khi lệch. AD-GT-1 chọn actor và nêu lên đây thay vì tự sửa cha.
- (question) Xung đột 3 — AD-20 viết actor gieo đi qua Cổng ở bước ⓪, AD-4 viết nhánh gieo đứng ở bước ②. Người đọc AD-20 một mình sẽ đặt nhánh gieo đầu chuỗi, và khi đó seeder gõ sai tên capability nhận CHO PHÉP thay vì unknown_capability — đúng thứ AD-4 hạ bước ② xuống để chặn.
- (question) Mở rộng cộng thêm cần tầng ④ đồng ý — AD-2 liệt kê sáu trường của mục sổ đăng ký; bước ④ và bước ⑤ không có đầu vào nếu thiếu touches và allowedRoles. Đây là cộng thêm, không làm yếu AD-2, nhưng tầng ④ dựng theo đúng sáu trường thì Cổng không quyết được hai bước đó.
- (question) Cây nguồn của cha liệt kê src/autonomy/gioi-han.ts gắn AD-11. Hai vấn đề: tên tệp tiếng Việt trái quy ước định danh trong mã là tiếng Anh; và nếu nó chứa các con số ngưỡng thì trái AD-15, vì ngưỡng sống trong bảng settings. Tầng ③ đánh giá ngưỡng ở bước ⑥ bằng SỰ KIỆN trong ctx, nên tệp đó không còn nội dung nào thuộc về Cổng.
- (assumption) Tầng ① đặt vai vào actor lúc dựng actor từ phiên đăng nhập, và ranh giới ①→④ tin actor là thật — AD-4 bảng ranh giới số 2 ghi rõ bên gọi chịu trách nhiệm truyền actor thật. Cổng không xác thực lại.
- (assumption) Bốn hành động chỉ-Quản-trị suy từ PRD §6: mở lại Cơ hội đã đóng, xem màn hình đo lường, chỉnh tham số vận hành, tắt hoặc bật toàn bộ phần AI. Đó là tập mà allowedRoles phải hẹp hơn sales ở bước ⑤.
- (assumption) Bộ đếm modelCallsUsed và budgetSpentUsd thuộc lượt quét đang chạy, đọc từ hàng Nhật ký vòng quét; ngưỡng modelCallsLimit và budgetLimitUsd đọc từ settings theo AD-11. Với actor người dùng cả bốn số đều không được đọc tới, nhưng vẫn được gom để chỉ có MỘT hình dạng ctx.
- (event) Spine cha bị vá SONG SONG giữa lúc chạy — bản đọc lúc 12:04 là 1248 dòng, bản lúc 12:18 là 1346 dòng. Bốn thứ đổi và làm sai bản nháp đầu: (1) chế-độ-gieo chuyển từ tham số dựng của CỔNG sang tham số dựng của SỔ ĐĂNG KÝ, và vào Cổng qua ctx; (2) thêm cờ selfLimiting cho disableAi và writeScanLog, bước ⑥⑦ bỏ qua; (3) mục sổ đăng ký thành BẢY trường, zone do tầng ④ sở hữu, hạng đọc-chung có seed trong allowedActors; (4) mã lỗi đổi từ F-n sang FT-n, và cây nguồn đổi gioi-han.ts thành limits.ts. Spine con viết lại toàn bộ theo bản 12:18.
- (decision) AD-GT-1 sửa: sáu trường, thêm seedMode theo AD-4 bản mới. Ngân sách vào bằng TỈ LỆ chứ không bằng số tuyệt đối, vì bảng khoá settings của AD-11 chỉ có budget_warn_ratio và budget_stop_ratio — không có khoá ngân sách tuyệt đối nào để trỏ tới. Thêm luật: thiếu hàng settings thì ném, còn không có lượt quét đang chạy là trạng thái HỢP LỆ với hai số bằng 0 — thiếu vế sau thì mỗi lần bấm giao diện ngoài giờ quét thành FT10.
- (decision) AD-GT-7 viết lại hoàn toàn: Cổng KHÔNG CÓ VÒNG ĐỜI. gate.ts xuất hàm thuần, không nhà máy, không thể hiện. Câu trả lời cho dựng-mỗi-request-hay-singleton là không dựng gì cả — vòng đời nằm ở createRegistry của tầng ④. Bản nháp đầu đề xuất createGate ở ba gốc dựng; nó sai hai lần theo bản cha mới: mâu thuẫn AD-4, và bắt instrumentation.ts cùng src/app nhập src/autonomy, thứ bảng AD-1 không cho phép.
- (decision) AD-GT-8 (allowedActors không chứa seed) bị RÚT — AD-2 bản mới khai hạng đọc-chung mang allowedActors gồm seed. Số hiệu giữ chỗ, không dùng lại. Lỗ hổng còn lại chuyển thành mục 5 của Xung đột: actor gieo ĐỌC được trong tiến trình dựng với seedMode tắt.
- (decision) AD-GT-11 mới — mỗi bước điều kiện chỉ áp cho hạng actor của nó. Bước ⑤ chỉ áp cho người: đọc chặt chữ sơ đồ của cha thì appendTimelineEntry(system) không có vai, không nằm trong allowedRoles, và TOÀN BỘ vòng quét chết với mã role. Kèm: allowedRoles rỗng nghĩa là KHÔNG vai người nào — đọc ngược lại thì một mục viết rỗng vì chưa quyết sẽ mở cho Sales tắt AI và D28 mất hiệu lực.
- (decision) AD-GT-5 bổ sung ngữ nghĩa touches: ranh giới mà THỰC THI capability nhất thiết thực hiện hành vi bị cấm, không phải bảng nó chạm tới. appendTimelineEntry chạm bảng dòng thời gian nhưng chỉ thêm mới, nên touches rỗng. Cả chín mục thật hiện có touches rỗng, và một phép kiểm khẳng định đúng thế. Nhiều mã thì trả mã đầu theo thứ tự §6.
- (decision) AD-GT-3 sửa hình thức đối chiếu: rút mã NFR bằng biểu thức chính quy, KHÔNG phân tích bảng markdown §6 — cột Vùng có một hàng tên là Ba chạm ghi được phép chứ không phải tên vùng, chữ hiển thị có dấu không khớp giá trị enum, và ô thứ năm chứa hai mã NFR-19 và D46. Lập luận cùng-cơ-chế-AD-6 của bản nháp đầu sai: AD-6 đọc khối YAML rào fence ở §8, §6 không có khối như thế. Thêm: zones.ts KHÔNG sở hữu kiểu Zone — AD-2 đã giao cho registry.ts.
- (decision) AD-GT-4 sửa theo bằng chứng tra web: dùng no-restricted-imports của LÕI ESLint với allowTypeImports, không dùng luật mở rộng của typescript-eslint (đã đánh dấu ngừng dùng từ ESLint 9.37). Thêm luật cấm dạng specifier nội dòng: với verbatimModuleSyntax bật, dạng đó bị viết lại thành lời nhập rỗng VẪN CÒN lúc chạy, và src/autonomy kéo registry.ts cùng Prisma vào đồ thị module đúng chỗ luật này cần nhất. Thêm consistent-type-imports và no-import-type-side-effects.
- (decision) AD-GT-9 bổ sung chữ ký decide đầy đủ với entry kiểu RegistryEntry hoặc undefined, luật cấm sổ đăng ký kiểm tồn tại trước khi gọi decide, và noUncheckedIndexedAccess bật. Không có ba thứ đó thì bước ① thành mã chết hoặc decide nổ TypeError ở nhánh AD-GT-9 vừa tuyên bố là không bao giờ ném.
- (question) Xung đột 1 viết lại: bảng AD-1 bản mới ĐÃ có dòng src/autonomy được nhập ở src/capability và tests — nên vấn đề bản nháp đầu nêu không còn. Vấn đề THẬT còn lại hẹp hơn và vẫn chặn: không dòng nào cho phép src/autonomy nhập NGƯỢC src/core hay src/capability, kể cả import type, mà gate.ts phải gõ kiểu Actor và RegistryEntry.
- (question) Xung đột mới — ba cách viết cho vai: AD-5 khai trường vai, kịch bản C viết role=admin, Mục 0 và PRD dùng giá trị quan_tri. Người viết actor.ts và người viết gate.ts sẽ lệch, và Quản trị bị từ chối role khi bấm Tắt AI, T-9 đỏ.
- (question) Xung đột mới — kịch bản D của cha vẽ ném GateDenied(NFR-14) trong khi mã mẫu AD-4 viết new GateDenied(d.reason). T-10 viết theo kịch bản D sẽ bắt NFR-14 và đỏ.
- (question) Xung đột mới — AD-11 không có khoá ngân sách tuyệt đối, và cây nguồn src/core chưa có tệp nào cho collectGateContext. Tầng ③ né bằng cách nhận tỉ lệ, nhưng tầng ④ và ⑤ vẫn cần một chủ sở hữu có tên.
- (event) spine finalized
- (decision) AD-GT-4 sửa theo phép đo thật (ESLint 10.8.1, typescript-eslint 8.67.0): ba luật ba vai, không phải hai luật cùng cưỡng chế. no-restricted-imports lõi bắt lời nhập GIÁ TRỊ và IM LẶNG với dạng nội dòng vì allowTypeImports cố ý cho qua; no-import-type-side-effects là luật cấm dạng nội dòng, một mình nó; consistent-type-imports chỉ bắt lời nhập giá trị đáng lẽ là nhập kiểu, và fixStyle separate-type-imports vốn đã là mặc định. Bí danh viết trung tính vì mẫu create-next-app trỏ @/* về GỐC DỰ ÁN chứ không về src/. Thêm ghi chú: create-next-app 16.3.1 không khai verbatimModuleSyntax, nên mặc định cả hai dạng đều bị xoá sạch — luật cấm là phòng thủ, không phải đang vá lỗi sống.
- (decision) AD-GT-9 bỏ noUncheckedIndexedAccess. Đã đo trên TypeScript 5.9.3: cờ đó KHÔNG bắt được ca này, vì nó đổi REGISTRY[name] thành RegistryEntry hoặc undefined, mà thứ đó gán vừa khít vào tham số đã khai | undefined; và với sổ đăng ký là object literal khoá cố định theo AD-2 thì cờ trơ hoàn toàn. Câu cũ còn tự mâu thuẫn với chữ ký in ngay trên nó. Bảo chứng thật nằm ở hai luật đang có: tham số kiểu RegistryEntry hoặc undefined, cộng lệnh cấm kiểm tồn tại trước khi gọi decide.
- (decision) selfLimiting sửa từ HAI mục lên BA — cha thêm readUserForAuth: lượt đọc dựng nên actor chạy TRƯỚC khi có actor nên phải đi bằng kind system, và bước ⑦ sẽ chặn nó đúng lúc phanh AI tắt, làm không ai đăng nhập được, T-9 và T-1 đỏ cùng lúc.
- (question) Lệch với spine anh em tầng ④ — tầng ④ đã dựng trên BẢN NHÁP ĐẦU của spine này và trích AD-GT-1 (năm trường, ngân sách tuyệt đối), AD-GT-7 (createGate rồi createRegistry nhận gate), AD-GT-8 (như luật còn hiệu lực, và ghi thành xung đột C4 với AD-2). Cả ba nay đã đổi theo cha. Tầng ④ còn dùng mã F10 thay vì FT10. Nguyên nhân chung: cha bị viết lại giữa lúc năm lượt tầng chạy song song. Trọng tài là bản cha hiện hành; thêm mục Lệch với spine anh em ④ vào spine.
- (event) Reviewer Gate vòng hai: lens xác minh kỹ thuật chạy mermaid.parse thật (ba khối hợp lệ), dựng repo thật để đo bộ luật ESLint, và chạy tsc hai lần để bác bỏ noUncheckedIndexedAccess. Lens đối kháng dừng giữa chừng không ra báo cáo, nhưng kịp báo hai dữ kiện đã dùng: cha đổi tiếp lên 1396 dòng, và spine tầng ④ đang trích AD-GT-n theo bản nháp đầu.
- (decision) AD-GT-12 (moi): GateEntry do tang 3 so huu — name, allowedActors, allowedRoles, touches, selfLimiting (bat buoc, khong ?:). Tang 4 khai RegistryEntry extends GateEntry. Dao chieu vi RegistryEntry mang z.ZodType + PrismaTx, nen ban cu keo Zod/Prisma vao do thi KIEU cua src/autonomy: tren clone sach (§7) npm test chay truoc prisma generate lam gate.ts do, du bo kiem cua Cong khong can mot byte du lieu. Chu trinh kieu 3<->4 bien mat; touches va allowedRoles thanh hop dong 3 ap len 4, trinh bien dich cuong che.
- (decision) AD-GT-1: kieu GateContext do gate.ts SO HUU; src/capability/gate-context.ts nhap kieu do va khai collectGateContext(actor, seedMode): Promise<GateContext>. seedMode la THAM SO do createRegistry giu, khong phai thu ben gom tu tim. Khong chot thi ctx.seedMode = undefined, buoc 2 khong bao gio cho phep, prisma/seed.ts gieo rong, moi T do o beforeEach. Nay la TS2739 luc bien dich.
- (constraint) Ngan sach vao ctx bang TI LE, khong so tuyet doi: AD-11 cua cha khai budget_warn_ratio/budget_stop_ratio va KHONG co budget_limit_usd. Tu so va mau so lay cung MOT hang ScanLog cua luot quet dang chay (cost_used_usd / budget_usd). Cam phat minh khoa settings moi.
- (change) Dong bo lai voi cha sau luot va AD-4: selfLimiting la NAM muc (disableAi, writeScanLog, releaseAccountLock, recordAccountCost, readUserForAuth) va la truong BAT BUOC; phep kiem dem ghi vet theo SO LAN decide duoc goi (khong phai loadCapability); co che ghi vet hai pha thuoc AD-CR-8 cua tang 5, cha chi chot ba bat bien. AD-GT-10 them bang nam ca selfLimiting mot ca mot muc, cong mot ca dem theo ten.
- (change) AD-GT-7 chot chu ky createRegistry({ seedMode, auditSink }) -> { loadCapability }; gate.ts khong xuat nha may, khong xuat the hien, khong xuat kieu Gate. Ba ban dang luu hanh (AD-4 cua cha, Cay nguon cua cha voi createGate, AD-CP-1 cua tang 4) duoc lap bang va noi ro ban nao thua vi sao.
- (change) Bang Inherited: AD-13 sua thanh 'ranh gioi thuoc 3 (zones.ts), vung thuoc AD-2 va tang 4' — truoc do ghi 'danh sach vung va ranh gioi doi chieu §6' trong khi Zone da tra ve tang 4.
- (event) DO THAT bang cai that trong scratchpad (eslint 10.8.1 + typescript-eslint 8.67.0 + typescript 6.0.3 + Node 24.15.0), bon hinh dang nhap, cau hinh KHONG type-aware. Ket qua: (A) nhap gia tri -> lo1i no-restricted-imports DO; (B) import type -> im lang; (C) import { type X } -> lo1i lo1i IM LANG, chi @typescript-eslint/no-import-type-side-effects bao; (D) nhap hon hop -> DO. consistent-type-imports IM LANG o ca bon — no chi bao khi loi nhap CHUA CO qualifier kieu. Ban ra soat kia dung.
- (event) Phat hien them, tai dinh nghia AD-GT-4: allowTypeImports cua luat LOI ESLint chi co TU 9.37.0. Bung tarball tung ban: 8.57.1/9.0/9.10/9.15/9.20/9.26/9.30/9.32/9.34/9.36 -> khong co; 9.37/9.38/9.39/10.8.1 -> co. Tren 9.36.0 cau hinh do lam ESLint CHET LUC NAP ('Unexpected property allowTypeImports'), tuc co che cuong che duy nhat cua AD-1 bien mat. Stack cua cha ghi 'ghim theo create-next-app' (^9) — do la o trong duy nhat trong bang Stack. AD-GT-4 dat san >= 9.37.0; bao len cha thanh Xung dot muc 10.
- (event) Do them: allowTypeImports xet o muc CA CAU LENH import (bao loi tu cot 1 het dong), khong o muc tung specifier — nen hinh dang D khong lach duoc. verbatimModuleSyntax bat hay tat KHONG doi mot dong ket qua lint nao (tsc --noEmit sach ca hai); no chi doi thu TypeScript phat ra. Chua do: moc chinh xac trong (9.36.0, 9.37.0], nhanh paths cua cung luat, va eslint-plugin-import-x no-restricted-paths.
- (change) AD-GT-4 viet lai: HAI luat hai vai (loi no-restricted-imports + allowTypeImports, va no-import-type-side-effects), bo consistent-type-imports khoi vai cuong che vi no doi type info va khong bit duoc hinh dang C. Canh bao: neu van bat no thi GIU fixStyle separate-type-imports — inline-type-imports lam autofix sinh ra dung hinh dang C ma luat kia cam.
- (constraint) Bi danh: create-next-app 16.3.1 khai "@/*": ["./*"] tro ve GOC DU AN, nen voi cay src/ mau dung la @/src/capability/*, khong phai @/capability/*. Viet thieu tien to src la mot mau khong bao gio khop = luat xanh vinh vien. Hoac doi paths thanh ./src/* — chon MOT, eslint.config.mjs va tsconfig.json phai khop.
- (change) Them muc '## Xung dot voi spine anh em' (thay cho 'Lech voi spine anh em 4'), bay dong, moi dong ghi: AD nao cua toi <-> AD nao cua anh em, lech cai gi, ai thang va vi sao. Do la thu con thieu ve CAU TRUC: bon va cham nang nhat deu o mat cat 3<->4 va khong spine con nao co cho bao ngang.
- (decision) Chot hat tang ScanLog.model_calls_used: NGAY SAU MOI LENH GOI MO HINH, do src/scan/loop.ts (tang 1) ghi qua writeScanLog. Buoc 6 so modelCallsUsed >= modelCallsLimit o MOI loi goi capability; tang o ranh gioi Cong ty thi suot mot Cong ty Cong doc so cu va tran 20 thanh trang tri. Chu cai dat la tang 1 — neu tang 1 phan doi thi phan doi vao muc Xung dot voi spine anh em, khong vao AD-GT-1.
- (change) Da dong: collectGateContext song o src/capability/gate-context.ts (AD-CP-7), khong phai src/core — AD-1 cho src/capability nhap src/core nen khong can tep moi va khong can ngoai le. Xung dot voi spine cha muc 7 thu hep lai con moi cau hoi 'ai dat con so ngan sach mot vong'. Them muc 9 (cay nguon cua cha in createGate, mau thuan noi tai voi AD-4) va muc 10 (san eslint).
- (event) Dinh chinh muc do ESLint o tren (mot so ky tu bi loi luc ghi). Bang do dung, doc lai cho ro: (A) import { createRegistry } from '../capability/registry' -> luat LOI no-restricted-imports BAO LOI; (B) import type { X } -> im lang; (C) import { type X } -> luat loi IM LANG, chi @typescript-eslint/no-import-type-side-effects bao loi; (D) import { createRegistry, type X } -> luat loi BAO LOI. consistent-type-imports im lang o ca bon hinh dang.
