---
scope: Kiến trúc sản phẩm Why Now — CRM AI-Native cho hackathon HBLAB, thi 15/08/2026
purpose: build-substrate — chốt stack cho src/ và các bất biến hai dev phải cùng tuân, kèm ma trận quyết định có trọng số và một trang trình bày
altitude: feature
updated: 2026-08-14T15:38
---

- (constraint) Hình thái bài toán: vòng đời bản ghi (bộ nền), mượn nền tảng dữ liệu ở nhánh đọc Bản chụp. Hai bổ ngữ đang hoạt động: bị bó thời gian (còn ~1 ngày tới 15/08 9:30) và đội nhỏ gộp vai (2 dev + 1 Sales Manager). Bó thời gian nghĩa là suy giảm theo mức độ đảo ngược được, không theo lịch.
- (direction) Người dùng chọn lối huấn luyện đầy đủ: đi qua từng quyết định, không tự quyết thay. Lý do người dùng nêu ngầm: lập luận sinh ra ở đây dùng lại cho vấn đáp vòng 2.
- (direction) Ngoài spine, bước này giao thêm hai sản phẩm: ma trận quyết định có trọng số tách riêng (cột Design chấm cái này), và một trang HTML trình bày kiến trúc để nói trước ban giám khảo.
- (assumption) Q15 chưa đóng — lược đồ dữ liệu BTC phát ngày thi chưa biết. Người dùng chốt: giả định schema theo docs/CRM clone từ Airtable/ và gắn thẳng vào mô hình. Cái giá nếu lệch: phải sửa lõi giữa giờ thi. Biện pháp giảm đã có sẵn trong PRD là TR-1 (lớp ánh xạ mỏng, module riêng).
- (constraint) NFR-7, NFR-8, NFR-13 đòi ba lệnh một-bước (khởi động, nạp dữ liệu, kiểm thử) từ clone sạch; §7.5 đòi thêm lệnh dừng. Bốn lệnh này là cổng nộp bài, không phải tiện nghi.
- (constraint) NFR-11 và §12.3: chạy local nhưng đủ như production — không dev server, không hot reload, không debug mode. Không cloud, không staging.
- (constraint) NFR-6 buộc cấu hình hai lớp: env lúc khởi động cho bí mật, cộng kho tham số runtime ghi đè env và có hiệu lực ngay, vì 0 tham số nghiệp vụ được đòi redeploy. Đây là ràng buộc kiến trúc, không phải tuỳ chọn.
- (constraint) NFR-14 tới NFR-17 cộng NFR-19: năm ranh giới máy phải chặn ở tầng nghiệp vụ, không ở UI và không bằng prompt. D25 làm chặt: chặn bất kể lối vào. Hệ quả kiến trúc — tầng AI là client bị giới hạn công cụ đứng NGOÀI tầng nghiệp vụ, và tầng nghiệp vụ phải nhận biết actor. T-10 gọi thẳng tầng nghiệp vụ với danh nghĩa hệ thống.
- (constraint) NFR-17 và NFR-19 chứng minh bằng vắng mặt: bộ công cụ của tầng AI không có thao tác xoá và không có thao tác sửa Timeline. Đây là ràng buộc về hình dạng tool set, không phải về kiểm tra runtime.
- (constraint) §5.1: state machine 7 trạng thái nằm ở tầng nghiệp vụ, guard theo actor VÀ theo vai. UI chỉ kéo thả (FR-4). Hai guard đặc biệt: tam_dung chỉ quay về đúng giai_doan_mo_gan_nhat đã lưu; mở lại Cơ hội đã đóng chỉ vai Quản trị (D43).
- (constraint) §6: đúng bốn hành động khác nhau giữa hai vai trên 27 hành động. Hệ quả — một điểm kiểm quyền duy nhất ở tầng nghiệp vụ, không rải khắp giao diện. D28: màn hình đo lường Quản trị chặn ở tầng nghiệp vụ, không chỉ ẩn menu.
- (constraint) BR-D* là ràng buộc tầng dữ liệu/nghiệp vụ, vi phạm là lỗi hệ thống. BR-B* là cờ cảnh báo tầng ứng dụng, vi phạm là trạng thái dữ liệu hợp lệ và KHÔNG chặn ghi. Trộn hai loại sinh hai lỗi ngược nhau. Đây là ranh giới kiến trúc quan trọng nhất ở tầng dữ liệu.
- (constraint) FR-34 đòi kiểm-và-ghi nguyên tử (compare-and-set): máy chỉ ghi khi ô vẫn đúng giá trị nó đã đọc, khác đi thì bỏ lượt ghi. Đây là ràng buộc chọn cơ chế giao dịch, không thoả bằng khoá lạc quan ở tầng UI.
- (constraint) Vòng quét (FR-36, FR-37): vòng lặp khép kín không dừng chờ ai duyệt; tập Công ty chốt lúc vòng bắt đầu; khoá theo Công ty (FR-38); phanh FR-45 cắt tại ranh giới Công ty. NFR-1 cấm hàng đợi dồn — vòng bị bỏ thì ghi log và drop, không backlog.
- (constraint) Ranh giới ra biên do đội tự đặt, hẹp hơn đề bài (§4.3): chỉ gửi ra nội dung Bản lưu đã chuẩn hoá, Loại công ty, và danh sách enum. Không gửi dữ liệu Cơ hội, giá trị tiền, thông tin Người liên hệ. Ràng buộc này định hình chữ ký của tầng AI.
- (constraint) Mọi xoá là xoá mềm, không có đường xoá cứng nào. Phát hiện không xoá được kể cả bởi người.
- (constraint) NFR-9: lưu UTC, hiển thị theo múi giờ người dùng, 0 lệch ngày giữa JP và Global. Tách tầng lưu và tầng trình bày về thời gian. BR-D7 tính hạn theo ngày làm việc của thị trường.
- (constraint) NFR-4 bắt buộc lớp cache theo hash prompt, TTL 24h, hit-rate quan sát được. NFR-2 trần 20 lệnh gọi mô hình mỗi vòng, chạm trần thì kết thúc vòng sớm và resume từ Công ty còn dở — cần con trỏ tiến độ bền vững. NFR-3 kiểm ngân sách trước mỗi lệnh gọi.
- (event) SỬA mục constraint trước đó về bốn lệnh: đã kiểm nguyên văn §7 đề bài, chỉ có BA lệnh một-bước — khởi động (§7.3), kiểm thử (§7.4), nạp dữ liệu (§7.5). Lệnh dừng KHÔNG có trong đề bài. Nó vẫn cần vì §7.3 đòi dữ liệu còn nguyên sau khi khởi động lại tiến trình, nhưng là phương tiện chứng minh chứ không phải mục nộp bài. CHECKLIST.md dòng 41 và docs/chien-luoc-ngay-thi.md dòng 83 đang ghi sai là bốn — sửa ở cuối bước.
- (constraint) BTC không giới hạn ngôn ngữ FE/BE/DB, đội chọn thứ nào thuận tay (hỏi đáp mục 7). Nhưng công ty KHÔNG hỗ trợ VPS và domain (hỏi đáp mục 3) — deploy local, tự giả lập hết mọi thành phần như production. Đây là ràng buộc quyết định: mọi phương án cần dịch vụ đám mây đều bị loại từ vòng ngoài.
- (constraint) Nguồn web trong đề bài là bản chụp HTML tĩnh của BTC (hỏi đáp mục 6), hai phiên bản trước/sau cho mỗi công ty. Việc chuyển trước sang sau phải làm được từ giao diện hoặc bằng một lệnh (§3). Không có crawler thật, không có tầng lấy dữ liệu web.
- (constraint) Khối lượng dữ liệu ngày thi (§3): 12–15 công ty, khoảng 30 người liên hệ, 8 cơ hội, mỗi công ty hai bản chụp. Đây là cỡ dữ liệu để chọn cơ chế lưu trữ — nhỏ tới mức mọi ràng buộc hiệu năng của cơ sở dữ liệu đều không ràng buộc.
- (constraint) Quỹ giờ ngày thi 15/08 là 5 tiếng 30 phút (9:30–15:00), hạn chót cứng 15:00. D41 buộc giữ trống MỘT NỬA quỹ đó cho tính năng BTC phát thêm — BTC đã xác nhận 14/08 là thứ Bảy vẫn có tính năng thêm. Nghĩa là kiến trúc phải cho phép thêm một tính năng lạ trong khoảng hai tiếng rưỡi, đó là tiêu chí chấm phương án chứ không phải lời khuyên.
- (constraint) D38: toàn bộ bảng tham số 0.2.1 và 0.2.2 phải sửa được ở màn hình Quản trị, hiệu lực ngay, không triển khai lại. Cấu hình đọc lúc chạy, không phải hằng số biên dịch.
- (constraint) D39: chỉ log Claude Code ngày 15/08 được tính điểm. Mọi việc quan trọng có sinh log phải diễn ra LẠI trong ngày thi. Hệ quả kiến trúc gián tiếp — thứ dựng trước ngày thi nên là thứ dễ tái sinh bằng AI trong ngày, không phải thứ chỉ chép lại.
- (constraint) D12 là rủi ro nghiệm thu duy nhất trong 47 quyết định được đánh dấu là có: nếu dữ liệu BTC điền sẵn Việc tiếp theo cho cả 8 cơ hội thì T-6 không còn ô trống để chạy. TR-5 đã có đường thoát — seeder chừa trống Việc tiếp theo cho ít nhất một Cơ hội đang chạy, và cờ ghi đè bật được lúc chạy.
- (constraint) T-2, T-10 và D25 cùng đòi một thứ: kiểm thử phải gọi THẲNG vào tầng nghiệp vụ, không qua UI, với danh nghĩa hệ thống. Đây là ràng buộc kiến trúc cứng — tầng nghiệp vụ phải là một biên gọi được từ mã kiểm thử, không phải logic nằm rải trong handler HTTP.
- (constraint) T-3 đòi bấm phát hiện mở đúng đoạn văn gốc có đánh dấu vị trí. Cộng với D21 (bản lưu lưu hai dạng, offset tính trên bản chuẩn hoá) — offset câu trích phải lưu và trả về qua API, không tính lại lúc hiển thị.
- (constraint) T-8 đòi bảng Nhật ký vòng quét có dòng tổng kết từng vòng: chạy lúc nào, quét bao nhiêu công ty, bao nhiêu nội dung mới, thêm bao nhiêu mục, mất bao lâu, lỗi gì; mỗi 10 vòng một dòng cộng dồn. Đây là bảng dữ liệu, không phải log file.
- (constraint) T-9 đòi công tắc tắt AI có hiệu lực NGAY, không cần chạy lại sản phẩm, và cắt được vòng quét đang chạy tại ranh giới Công ty. Ràng buộc này loại các phương án chạy vòng quét trong tiến trình tách biệt không chia sẻ trạng thái với ứng dụng.
- (decision) Ngôn ngữ cho src/: TypeScript toàn bộ, cả FE lẫn BE. Người dùng chốt sau khi chấm ma trận có trọng số. Lý do quyết định: hai dev thạo TypeScript nhất (tiêu chí trọng số 3), cộng với việc dùng thẳng được 28 component React đã biên dịch sẵn và 191 token CSS của Claude Design — mà 5/6 giám khảo vòng 3 chấm đúng phần giao diện. Điểm ~151 so với Python ~124 và tách Python/TS ~110.
- (decision) Tầng AI dùng Claude Agent SDK (@anthropic-ai/claude-agent-sdk), chạy bằng đăng nhập subscription Team, KHÔNG dùng ANTHROPIC_API_KEY. Người dùng chốt sau khi tôi phản đối và sau khi thử nghiệm thực tế chứng minh nó chạy.
- (event) Đã kiểm bằng thực nghiệm chứ không đọc tài liệu rồi tin. Máy không có ANTHROPIC_API_KEY và không có ANTHROPIC_AUTH_TOKEN; Claude Code đăng nhập bằng ~/.claude/.credentials.json. Cài @anthropic-ai/claude-agent-sdk rồi gọi query() — chạy được, trả kết quả đúng. Kết luận: đường subscription hoạt động tại thời điểm 14/08/2026.
- (event) Thử ba yêu cầu của sản phẩm trong một lần chạy, cả ba đạt: (1) D22 ép enum — outputFormat json_schema trả đúng signal_type=funding và confidence=chac theo enum 0.1.1 và 0.1.3; (2) BR-D2 câu trích khớp nguyên văn đoạn nguồn từng ký tự; (3) NFR-2 chặn số lượt bằng maxTurns.
- (event) Bằng chứng cho NFR-17 và T-10, có đối chứng: với tools rỗng, agent được bảo thẳng là đọc tệp rồi chạy rm để xoá — không tool nào được gọi và tệp mồi còn nguyên. Với allowedTools Read và Bash, cùng lời nhắc đó, agent gọi Read, Bash sáu lượt và tệp mồi BỊ XOÁ. Đối chứng chứng minh phép thử có giá trị. Đây là thứ biểu diễn được trước giám khảo vòng 2.
- (event) Quan sát phụ ở ca tools rỗng: mô hình VIẾT RA lệnh bash trong phần văn bản nhưng lệnh không chạy. Hành vi an toàn, nhưng nghĩa là đầu ra thô của tầng AI không được hiển thị trực tiếp cho Sales — phải đi qua schema. D22 vốn đã bắt vậy.
- (constraint) Số đo chi phí thật đo được: một lệnh gọi rút phát hiện tốn 0,025 tới 0,039 đô theo giá niêm yết. Một vòng quét chạm trần 20 lệnh gọi rơi vào 0,50 tới 0,80 đô. Nhưng D18 chỉ tạo Bản lưu mới khi dấu vân nội dung khác, và NFR-4 cache 24h theo hash prompt — nên ở trạng thái ổn định số lệnh gọi gần bằng không. Kịch bản T-8 đổi nguồn hai công ty chỉ tốn khoảng 0,05 đô.
- (constraint) Phí tổn của Agent SDK: mỗi lệnh gọi nguội mang theo khoảng 2548 token system prompt của Claude Code (đo được qua cache_creation_input_tokens). Gọi thẳng Messages API sẽ nhẹ hơn nhiều. Bộ đệm ephemeral 1 giờ khấu hao được phần này trong cùng một phiên, nhưng vòng quét 24 giờ ở chế độ vận hành thì mỗi vòng đều nguội.
- (constraint) result message của Agent SDK trả về total_cost_usd. NFR-3 đòi kiểm ngân sách trước mỗi lệnh gọi — đọc thẳng con số này thay vì tự ước lượng token. Cộng với tuỳ chọn maxBudgetUsd, NFR-3 gần như có sẵn.
- (assumption) RỦI RO ĐÃ BIẾT VÀ ĐÃ CHẤP NHẬN: tài liệu chính thức của Agent SDK, đọc ngày 14/08/2026, vẫn ghi rằng Anthropic không cho phép bên thứ ba dùng đăng nhập claude.ai cho sản phẩm của họ và bảo dùng API key. Trang hỗ trợ khai gói Pro/Max/Team/Enterprise dùng được từ 15/06/2026 NHƯNG cùng trang ghi thay đổi đó đã bị TẠM DỪNG và chưa có ngày hiệu lực mới. Nghĩa là đội đang dựa vào một cơ chế đang treo. Nó chạy hôm nay; nếu ngừng giữa ngày thi thì T-4, T-6, T-8 đỏ cùng lúc. Biện pháp giảm: đặt tầng AI sau một giao diện hẹp để đổi sang API key bằng biến môi trường, và kiểm lại một lần vào sáng 15/08 trước khi làm gì khác.
- (decision) Không làm chatbot. Người dùng có nhắc tới nhưng §4 có sáu nhóm chức năng và không nhóm nào là chatbot; không phép kiểm nào trong T-1 tới T-10 chạm tới nó. Thêm vào là lấn đúng nửa quỹ giờ mà D41 bắt giữ trống cho tính năng BTC phát thêm.
- (event) SỬA hiểu nhầm về NFR-4: prompt caching của Anthropic chỉ có TTL 5 phút và 1 giờ, không có 24 giờ. Nên NFR-4 không phải prompt caching mà là cache của đội, khoá bằng hash prompt, lưu trong cơ sở dữ liệu. Hai thứ khác nhau và nên dùng cả hai: cache 24h của đội cắt hẳn lệnh gọi (đây là cái NFR-4 đo), prompt caching của Anthropic cắt chi phí input cho lệnh gọi vẫn phải chạy.
- (decision) Thêm tiêu chí thứ chín vào ma trận theo yêu cầu người dùng: hỗ trợ dùng AI dễ tới đâu, trọng số 4. Python nhỉnh hơn ở ô này (messages.parse với Pydantic trả đối tượng đã validate; beta_tool sinh schema từ chữ ký hàm) nhưng cả hai là hai ngôn ngữ duy nhất có Agent SDK nên chọn cái nào cũng không đóng cửa đó.
- (event) SỬA con số trong mục ghi quyết định ngôn ngữ: tôi ghi Python khoảng 124, con số đúng là 139. 124 là bản chưa cộng dòng đội thạo sẵn và chưa chốt điểm từng ô. Ma trận cuối cùng, đã tự cộng lại bằng script và khớp: TypeScript toàn bộ 151, Python toàn bộ 139, Python BE cộng TS FE 110. Khoảng cách thật giữa TypeScript và Python là 12 điểm, không phải 27 — TypeScript thắng nhờ C4 chỗ cho tính năng lạ, C7 component React sẵn có, và C8 đội thạo; Python thắng ở C5 hỗ trợ dùng AI, C6 vòng quét, và C9 nguyên tử.
- (event) LỖ HỔNG CỦA TÔI, người dùng chỉ ra: tôi dựng spine mà chưa đọc docs/Đề bài/2. Thiết kế phần mềm thế hệ AI Native - phương pháp luận.md — tài liệu phương pháp luận do chính giám khảo phát. Đã đọc và vá.
- (constraint) Phương pháp luận Phần 3: có hai trường phái AI-native và đội ĐÃ CHỌN AI human-centric, không phải AI-centric. Khác biệt: human-centric coi người và AI cùng tiến hoá, và sự khôn lên của NGƯỜI phải đo được. AI-centric coi vai trò người chỉ là approve cuối cùng, và tài liệu gọi thẳng đó là cái bẫy, ví như xe tự lái nơi người chỉ đạp được phanh. Mọi thiết kế phải kiểm lại theo trục này.
- (constraint) Phương pháp luận Phần 4 là phần lõi: bốn đối tượng nguyên thủy Observation, Claim, Proposal, cộng Provenance làm sợi dây truy vết. Luật cứng nhất: không có provenance thì không hiển thị. Đề bài ĐÃ mã hoá đủ bốn thứ này dưới tên tiếng Việt — Bản chụp và Bản lưu là Observation, Phát hiện là Claim, Gợi ý là Proposal, câu trích cộng offset là Provenance.
- (constraint) Phương pháp luận Phần 5: trần tự chủ khai theo VÙNG gắn với loại đối tượng, không phải theo mức cấu hình. Ba vùng: tự do ở mức observation, chạy ngầm cho claim và proposal chưa chạm dữ liệu chính thức, và cấm tuyệt đối. Không có núm vặn tự chủ.
- (constraint) Phương pháp luận Phần 3 định nghĩa hai chỉ số và nói rõ mỗi cái đo AI: auto-accept rate đo hệ thống khôn lên, error-detection rate đo NGƯỜI khôn lên. Kiểm bằng grep: PRD và spine có auto-accept ở 8 tệp và error-detection ở 10 tệp — nửa đo lường đã hấp thụ. Nhưng observation, claim, provenance, proposal, ontology đều 0 tệp — nửa mô hình đối tượng thì chưa.
- (event) PHẢN BIỆN bản spec AI-Native CRM MVP v0.2 người dùng đưa. Tám điểm sai: (1) spec chọn AI-centric — trường phái giám khảo đã loại; bằng chứng là L3 Delegate được đặt làm đích, Experiment D là quan trọng nhất, và Approval UI chỉ có hai nút Approve/Reject không kèm bằng chứng, đúng ô vai trò con người của cột AI-CENTRIC. (2) Không có bốn nguyên thủy; domain là Customer/Lead/Task thuần CRUD. (3) Vi phạm luật không có provenance thì không hiển thị. (4) Bảy metric đều đo agent, không metric nào đo người. (5) Human Intervention Rate bị đặt sai dấu — coi người tham gia là chi phí phải giảm, trong khi human-centric coi đó là sản phẩm. (6) Tự chủ dùng núm vặn L0-L3 thay vì vùng theo loại đối tượng. (7) Domain không khớp đề bài ở mức cả mười phép kiểm T-1 tới T-10 đều đỏ. (8) Kế hoạch một ngày xếp 09:00-18:00 trong khi phiên làm việc là 9:30-15:00 và D41 bắt giữ trống một nửa.
- (event) Thứ bản spec làm ĐÚNG và đáng lấy: capability layer với agent không chạm SQL; câu Autonomy không thuộc Agent, tách Autonomy Manager khỏi Agent; max_tool_calls 20 trùng khít NFR-2 một cách độc lập; failure taxonomy F1-F10 mà đề bài không có; bốn thí nghiệm A/B/C/D đo xem context có thực sự giúp không; và nguyên tắc over-autonomy phải được ưu tiên hơn task success.
- (decision) Người dùng chốt: ontology đặt trong src/, không phải docs/, vì nó là tài sản lúc chạy mà AI đọc. Quyết định này giải được căng thẳng tôi để lại trong AD-3: tầng AI cần enum để ép D22 nhưng không được nhập từ lõi. Cả src/ai/ và src/core/ cùng đọc src/ontology/ và không đọc nhau.
- (change) Tạo src/ontology/crm.ontology.md — 14 mục, 22 quan hệ CÓ TÊN mỗi quan hệ đọc được thành một câu tiếng Việt hoàn chỉnh. Gồm: hai tầng Hồ sơ chính thức và Tầng AI-native; phân loại bốn nguyên thủy; ba vùng tự chủ cộng năm ranh giới; enum để tầng AI dựng JSON Schema; chuỗi dẫn xuất tài liệu; ranh giới ontology và playbook kèm quyền commit.
- (decision) Thêm AD-15 (ontology là nguồn sự thật chung, ở src/ không ở docs/, ngữ nghĩa thì ontology thắng còn hình dạng lưu trữ thì lược đồ thắng) và AD-16 (mọi thứ máy sinh ra phải xếp vào đúng một trong ba loại nguyên thủy; Claim buộc phải neo nguồn; bảng mới không xếp được là dấu hiệu thiết kế sai chứ không phải lý do thêm loại thứ tư). Spine hiện có 16 AD, lint 0 phát hiện.
- (assumption) Một chỗ ontology ghi nhận là biến thể chứ không phải vi phạm mô hình proposal: Việc tiếp theo do máy đặt là proposal ÁP DỤNG TRƯỚC HOÀN TÁC SAU, khác Gợi ý là DUYỆT TRƯỚC ÁP DỤNG SAU. Đề bài cố ý chọn hai chế độ khác nhau cho nhóm 3 và nhóm 4, đổi lại nhóm 4 phải có Hoàn tác một cú bấm trong 7 ngày. Nếu giám khảo hỏi vì sao lệch phương pháp luận thì đây là câu trả lời.
- (assumption) Tháp độ tin cậy nguồn tin ở Phần 7 phương pháp luận suy biến trong kỳ thi: mọi observation đến từ cùng một nguồn là bản chụp của BTC, nên tháp chỉ còn một tầng. Đã ghi rõ trong ontology mục 14 là cố ý không mô tả, để không bị hiểu là bỏ sót.
- (event) Người dùng nói thẳng kiến trúc của tôi chưa thuyết phục. Tôi đồng ý và đã nêu lý do cụ thể: spine cũ đặt paradigm là lõi sáu cạnh một biên — kiến trúc hexagonal, AI là MỘT TRONG SÁU cạnh. Đọc ra là ứng dụng web có gắn module AI, không phải hệ AI-native. Ở vòng 2 câu chúng tôi có 16 bất biến yếu hơn hẳn câu Agent tới Autonomy tới Capability tới Core và đây là vì sao.
- (decision) VIẾT LẠI TOÀN BỘ spine. Paradigm mới: năm tầng AI-native, một cổng, một chiều. Tương tác, Agent Runtime, Cổng tự chủ, Capability, Lõi domain, cộng Ontology đứng cạnh mọi tầng. Chiều phụ thuộc cũ giữ nguyên (lõi không biết agent, agent không biết lõi, cả hai đọc ontology) nhưng đọc từ trên xuống thì là hệ AI-native.
- (decision) Người dùng chốt: tầng AI CÓ capability ghi, nhưng ở mức sơ khai. Nghĩa là agent chạm dữ liệu qua Capability như mọi tầng khác, không có đường riêng, và đúng ba chạm ghi vào Hồ sơ chính thức: thêm Mục dòng thời gian, điền Việc tiếp theo vào ô trống, tự tắt AI. Đổi lại NFR-17 không còn chứng minh bằng tool set rỗng nữa.
- (decision) Hình thức chứng minh vắng mặt MỚI, mạnh hơn cái cũ: sổ đăng ký capability là TẬP ĐÓNG và một phép kiểm khẳng định Object.keys(registry) BẰNG ĐÚNG danh sách kỳ vọng. Thêm hay bớt đều đỏ. Không phải chúng tôi không thấy capability xoá, mà là đây là toàn bộ capability tồn tại và test đỏ nếu có cái thứ mười. Chín capability chung cộng nhóm chỉ-dành-cho-người.
- (decision) AD-1 là bất biến mạnh nhất của spine mới: chỉ src/capability được nhập src/core, chỉ src/autonomy được gọi src/capability, và capability CHỈ LẤY ĐƯỢC QUA SỔ ĐĂNG KÝ mà sổ đăng ký gọi Cổng trước khi trả hàm — nên một capability quên gọi Cổng không tồn tại được. Cưỡng chế bằng luật lint no-restricted-imports, không bằng lời dặn.
- (decision) AD-4 chốt chuỗi sáu bước của Cổng tự chủ theo thứ tự cố định, dừng ở lần từ chối đầu: capability có trong sổ, actor được phép, chạm ranh giới cấm, vai người dùng đủ, còn trần lượt và ngân sách, phanh AI đang bật. Và từ chối CŨNG ghi một dòng ghi vết kèm mã lý do — cổng không có nhánh im lặng.
- (change) Chắt lọc từ imagine-architect.md vào spine: capability layer thành tầng có tên (AD-1, AD-2), câu Autonomy không thuộc Agent vào paradigm và AD-4, chuỗi quyết định của cổng vào AD-4, risk trên mỗi action vào sổ đăng ký và quy ước ghi vết, execution limits thành khối cấu hình và stop conditions liệt kê tường minh vào AD-11, failure taxonomy F1-F10 thành mục riêng.
- (decision) LOẠI khỏi imagine-architect và ghi rõ lý do trong mục Xuất xứ của spine: bốn mức L0-L3 vì là núm vặn trong khi phương pháp luận Phần 5 và D16 dùng vùng theo loại thao tác; domain Customer Lead Task vì cả mười T sẽ đỏ; Human Intervention Rate là chi phí phải giảm vì đảo dấu so với human-centric; điều kiện dừng chờ người duyệt vì §4 nhóm 5 cấm vòng lặp dừng chờ ai; SQLite Python chat-first vì ma trận quyết định đã loại có điểm số.
- (constraint) Một khác biệt của đề bài so với mô hình agent thông thường, đã ghi vào AD-11: KHÔNG có điều kiện dừng chờ người duyệt. §4 nhóm 5 nói vòng lặp không dừng chờ ai ở bất kỳ bước nào. Năm điều kiện dừng là hết Công ty, phanh tắt, chạm trần 20 lệnh gọi (lưu con trỏ resume vòng sau), chạm 100% ngân sách, và lỗi không phục hồi.
- (constraint) AD-11 giải luôn câu hỏi dừng giữa chừng để lại gì: vì mọi thao tác ghi nguyên tử ở mức một capability, dừng giữa chừng không để lại bản ghi nửa vời, chỉ để lại việc chưa làm. Đây là chỗ imagine-architect bỏ trống.
- (event) Đã giao skill:bmad-review với ba lăng kính adversarial, edge-case, verification-gap soi docs/luu-tru/imagine-architect.md theo yêu cầu người dùng, vì bản đó chưa qua kiểm chứng. Đã dặn bỏ qua sáu phát hiện tôi đã nêu để không lặp, và chỉ ra mười hướng săn mới gồm mâu thuẫn trong policy.py, tương tác giữa max_tool_calls và max_steps, chạy song song không khoá, ai hết hạn approval, metric có tính được không, và cỡ mẫu 20 case có phân biệt được 5% với 0% không.
- (change) Spine mới: 18 AD, lint 0 phát hiện, khoảng 4021 từ. Thêm mục Phân loại thất bại F1-F10 và mục Xuất xứ ghi rõ lấy gì loại gì từ imagine-architect. F6 vi phạm luật nghiệp vụ và F8 cổng tự chủ quyết sai được đánh dấu là hai loại KHÔNG ĐƯỢC PHÉP xảy ra — nếu xảy ra thì lỗi ở kiến trúc chứ không ở mô hình.
- (event) Người dùng hỏi lượt chạy này có tuân thủ custom BABOK hub không. Tự kiểm bằng cấu hình thật, kết quả 5 trên 8 chỉ thị external_sources. Ba lỗ hổng: chỉ thị 2 Interface Analysis KHÔNG LÀM (0 lần định dạng, tần suất, khối lượng ở cả ba tệp), chỉ thị 3 Sequence Diagram KHÔNG LÀM và bỏ luôn bước hỏi trước kịch bản nào đáng vẽ, chỉ thị 5 phần vai đổi và người rời tổ chức không có ở đâu. Cộng một phát hiện tự lộ: hình thái bài toán xác định rồi nhưng CHỈ nằm trong memlog, 0 lần trong spine và ma trận — doc_standard 4 sẽ bắt được.
- (change) Vá cả ba lỗ hổng vào spine. Thêm mục Hình thái bài toán ở đầu (vòng đời bản ghi trội, nền tảng dữ liệu phụ ở nhánh đọc, hai bổ ngữ bị bó thời gian và đội nhỏ gộp vai, cộng phần nói rõ KHÔNG phải hình thái nào). Thêm mục Phân tích giao diện: tám ranh giới (năm trong, ba ngoài) với hai bảng — cái gì đi qua kèm định dạng tần suất khối lượng, và ai chịu gì kèm hỏng thì sao. Thêm mục Vòng đời vai và tài khoản trả lời ba câu của chỉ thị 5, trong đó câu thứ ba trả lời bằng cách nói rõ ngoài phạm vi và nêu cái giá.
- (decision) Bốn kịch bản sequence diagram do người dùng chọn, đúng quy trình hỏi trước của chỉ thị 3: A vòng quét một Công ty từ Bản chụp tới Việc tiếp theo (đi hết năm tầng, chạm tích hợp ngoài, phủ T-6 và T-8), B duyệt một Gợi ý ba lối ra (chuyển trạng thái kèm duyệt, phủ T-5), C phanh AI cắt vòng đang chạy (T-9), D máy bị từ chối ở Cổng (T-10). Hai loại kịch bản này đúng thứ chỉ thị nêu cho hình thái vòng đời bản ghi.
- (event) bmad-review với ba lăng kính adversarial, edge-case, verification-gap đã chạy xong trên imagine-architect.md: 40 phát hiện, chia bốn nhóm — phá kiến trúc hoặc phá demo, mô hình quyết định sai hoặc bất định, không đo được, và những thứ tài liệu không nói mà người dựng buộc phải tự bịa. Ba cái nguy hiểm nhất theo bản rà soát: bộ tool dựng sẵn của SDK để mở, vòng lặp chính có điều kiện dừng không thể chạm tới, và approval vừa là điều kiện dừng vừa là trạng thái chờ mà không có đường quay lại.
- (change) NĂM phát hiện của bản rà soát đánh trúng spine CỦA TÔI chứ không chỉ bản imagine-architect, đã vá cả năm. Một, nghiêm trọng nhất: bộ công cụ dựng sẵn của Agent SDK phải tắt sạch, vì Bash chạy được psql tức một đường xuống Postgres đi vòng qua cả bốn tầng dưới — spine cũ không hề nói. Đã đưa vào AD-1 cùng phép đối chứng đội đã đo. Hai: rui_ro trong sổ đăng ký không tham gia quyết định nào, giờ nói rõ nó chỉ vào ghi vết và giao diện. Ba: phép kiểm ranh giới phải khẳng định HAI vế — bị từ chối, VÀ bản ghi không đổi — nếu không thì không phân biệt được chính sách chặn với capability vốn không tồn tại. Bốn: đơn vị công việc là một Công ty trọn vẹn, kiểm ngân sách trước khi bắt đầu Công ty kế tiếp, không bao giờ cắt giữa lúc đang xử lý. Năm: lượt bị Cổng từ chối KHÔNG tính vào trần 20 vì NFR-2 đếm lệnh gọi mô hình, nhưng có trần riêng ba lần từ chối liên tiếp trên cùng capability thì kết thúc vòng.
- (constraint) Số liệu giao diện đo được thật, đã ghi vào bảng Phân tích giao diện: ranh giới Claude Code tới api.anthropic.com tốn 2548 token nạp bộ đệm và khoảng 490 token ra mỗi lệnh gọi, 0,025 tới 0,039 đô. Ranh giới src/agent tới tiến trình con mang khoảng 2,5k token phí tổn cộng 2 tới 5 KB Bản lưu. Hai ranh giới 7 và 8 là hai chỗ DUY NHẤT rời khỏi máy của đội.
- (event) Spine sau khi vá: 18 AD, 4 sequence diagram, lint 0 phát hiện, 6263 từ. Ba chỉ thị BABOK còn thiếu đã đóng. Còn lại chưa chạy là doc_standards năm mục và finalize_reviewers hai mục — cả hai thuộc bước Finalize.
- (event) Người dùng chỉ ra còn thiếu bước review theo babok hub. Đúng: doc_standards năm mục và finalize_reviewers hai mục chưa chạy mục nào. Đã chạy cả bảy chỉ thị trên năm lăng kính song song. Kết quả vòng 1: khoảng 60 phát hiện, trong đó bốn cái làm hỏng bản ghép hoặc chặn cổng nộp bài.
- (event) THỰC NGHIỆM QUYẾT ĐỊNH AD-1. Lăng kính finalize_reviewer 1 nói tools rỗng mới là một nửa, capability MCP còn cần allowedTools và permissionMode. Tôi không tin tài liệu, đã tự dựng thí nghiệm hai nhánh với một MCP server thật. Kết quả: nhánh A chỉ tools rỗng cộng mcpServers thì agent CÓ gọi capability nhưng handler chạy 0 lần và tốn 1,607 đô; nhánh B thêm allowedTools cộng permissionMode dontAsk thì handler chạy 1 lần và tốn 0,091 đô. Tức AD-1 viết như cũ thì vòng quét chạy, agent trông như đang gọi capability, KHÔNG GÌ ĐƯỢC GHI, và chi phí gấp 17 lần vì agent thử lại. T-4 T-6 T-8 đỏ cùng lúc với triệu chứng đánh lạc hướng sang mô hình không chịu dùng tool.
- (change) Vá AD-1 hai lỗ. Lỗ một: khai đủ bốn tuỳ chọn tools rỗng, mcpServers, allowedTools, permissionMode dontAsk, kèm bảng số đo hai nhánh. Lỗ hai do lăng kính đối kháng tìm ra: AD-1 cấm nhập src/core nhưng KHÔNG cấm nhập @prisma/client, nên một server action gọi thẳng prisma.account.update không vi phạm câu nào, lint xanh, ghi vết rỗng, và T-10 chứng minh chỉ có chín capability trên một sản phẩm ghi dữ liệu bằng chục đường không tên. Thêm luật chỉ src/core và prisma được nhập @prisma/client, ngoại lệ duy nhất là tests chỉ đọc.
- (decision) BỐN AD MỚI, đều bù đúng một khoảng trống mà lăng kính đối kháng dựng được cặp đơn vị tuân thủ đủ mà vẫn không ghép được. AD-19 phạm vi sổ đăng ký là MỌI thao tác ghi bất kể actor, chia ba khối CAP_MAY_DUOC chín cái khẳng định bằng đúng, CAP_CHI_NGUOI và CAP_HE_THONG_NOI_BO khẳng định không giao — không có nó thì nhóm 1 CRM làm tay hoặc làm đỏ T-10 hoặc rơi xuống ghi thẳng Prisma. AD-20 nạp dữ liệu là actor thứ ba vì seeder không có nhánh nào hợp lệ để dùng. AD-21 hiệu ứng dây chuyền giữ nguyên actor khởi phát, sinh ra từ ca D26 xoá Công ty đóng kèm Gợi ý. AD-22 mô hình sinh Claim còn hệ thống sinh Proposal, vì AD-7 cấm gửi hồ sơ ra biên nên mô hình không nhìn thấy giá trị hiện tại của ô.
- (change) Siết tám AD cũ. AD-3 điều kiện chạm ghi số hai thành THAM SỐ chứ không hằng số, vì TR-5 là đường thoát duy nhất của D12 mà kiến trúc cũ làm nó bật lên không đổi hành vi gì. AD-4 thêm bước 0 cho nap_du_lieu và giải thích thứ tự sáu bước là từ hẹp tới rộng. AD-6 thêm draft-07 vì SDK từ chối lược đồ mới hơn còn Zod 4 mặc định sinh 2020-12. AD-8 chốt đích so khớp là bản CHUẨN HOÁ, không phải bản thô — đọc sai chỗ này làm mọi Phát hiện rụng theo BR-D2 với triệu chứng giống mô hình bịa câu trích. AD-9 chia BR-B thành hai hạng vì BR-B5 và BR-B6 là chỉ số trên quần thể, không có hàng nào để đặt cột. AD-12 khoá là hàng có lease chứ không phải biến bộ nhớ, và vòng quét sống ở instrumentation.ts. AD-13 thêm thứ tự phân xử Mục 0 hơn PRD hơn ontology hơn lược đồ. AD-18 hàm chuẩn hoá sống ở đúng một tệp, thuần thôi chưa đủ.
- (constraint) Bốn phát hiện chặn cổng nộp bài, đã vá vào mục Ba lệnh một-bước. Một: npm test phải là vitest run chứ không phải vitest, vì hướng dẫn Next.js đặt chế độ theo dõi và tiến trình không bao giờ trả mã thoát. Hai: docker compose up -d trả về khi container khởi động chứ không phải khi Postgres sẵn sàng, bắt buộc healthcheck. Ba: seeder phải chạy prisma migrate deploy chứ không phải migrate dev vì migrate dev có thể hỏi tương tác và có thể reset cơ sở dữ liệu. Bốn: Node 24.15.0 dính 12 CVE trong đó ba mức High, đổi lên 24.19.0.
- (decision) CHỐT ứng dụng chạy TRÊN HOST, chỉ Postgres trong compose. Đây là câu hỏi lăng kính doc_standard 2 chỉ ra là chưa ai trả lời mà kéo theo cả tầng AI. Lý do: credential subscription không có trong ảnh, mà Claude Code CLI cũng phải cài thêm. Cái giá đã ghi rõ: docker compose up -d chỉ dựng Postgres nên tiến trình web do đội mở sẵn trước khi giám khảo ngồi xuống. Nếu đổi sang API key thì container hợp hơn hẳn và phải dựng lại quyết định này.
- (change) Sửa ba mũi tên đi vòng trong chính sơ đồ tuần tự của tôi: vòng quét đọc cờ ai_bat thẳng từ DB, vòng quét ghi Nhật ký thẳng xuống DB, Cổng ghi ai_bat thẳng xuống DB. Cả ba đều đi vòng qua tầng Capability mà AD-1 vừa cấm — nếu mũi tên đúng thì AD-1 sai, không thể cả hai cùng đúng.
- (change) Hình thái bài toán vá ba chỗ. Một: THÊM hình thái phụ thứ hai thay hệ cũ ở nhánh nạp — spine cũ viết không có hệ đang chạy để đối soát, sai sự kiện, vì addendum có cột Hệ thống đang chạy ~1.200 công ty và Tomahawk PRD có legacyAirtableId ghi rõ để đối soát. Hệ quả cụ thể: bảng ánh xạ trường Tomahawk §12 đọc được TỐI 14/08, tức giảm được bất định của Q15 chứ không chỉ giảm thiệt hại. Hai: cặp gộp vai đáng lo nhất KHÔNG phải dev kiêm tester mà là Domain SME kiêm End User — Sales Manager vừa là chủ quy trình vừa làm người dùng cuối trong khi 5/6 giám khảo vòng 3 là Sales do chính người đó quản lý trực tiếp, và ở đây không có T nào đỡ. Ba: bỏ persona chi tiết khỏi nhóm được cắt vì chien-luoc nói dồn sức vào UX.
- (change) Vệ sinh trích dẫn theo chỉ thị doc_standards 5. Sửa số hiệu SAI: ma trận ghi Decision Analysis là 10.19 trong khi canon của repo ghi 10.16, còn 10.19 là Estimation. Đã bỏ hẳn số, giữ tên. Gỡ mệnh đề dẫn Chỉ thị BABOK khỏi spine, gỡ phương pháp luận Phần X khỏi trường Binds của AD-2 AD-8 AD-13 vì Binds ở mọi AD khác chỉ chứa mã dự án. Gom xuất xứ ontology về một bảng ánh xạ đầu tệp, giữ nguyên hai blockquote vì nguồn phải đứng cạnh câu trích.
- (change) Ontology sửa hai lỗi đường dẫn tốn kém: mục 12 ghi src/ai/ trong khi cây nguồn là src/agent/, và gán việc đối chiếu ranh giới cho src/core/guard.ts trong khi AD-13 nói src/autonomy/. Sửa mẫu số error-detection rate cho khớp D30 là Phát hiện có phản hồi — trước đó ghi Gợi ý đã quyết, mà tử số đếm trên Phát hiện nên tỉ lệ vượt 100% ngay khi có phản hồi trên Phát hiện chưa sinh Gợi ý. Thêm cột Tên trong mã cho hai bảng đối tượng, vì không ai suy ra được Article là Bản lưu.
- (event) Spine sau vòng vá 1: 22 AD, lint 0 phát hiện. Bảy chỉ thị BABOK đã chạy đủ. Chuẩn bị vòng review 2 soi đúng phần vừa vá.
- (decision) QUY ƯỚC MỚI do người dùng chốt: mọi định danh trong mã phải là TIẾNG ANH — tên hàm, tên tệp, tên kiểu, tên capability, trường sổ đăng ký, giá trị discriminant của actor, tên bảng, tên cột, tên enum. Giữ tiếng Việt CHỈ cho giá trị enum (vì Mục 0 ghim và mô hình phát ra đúng chuỗi đó theo D22) và chữ hiển thị. Đã đổi 59 định danh trên ba tệp. Làm bây giờ miễn phí vì src/ chưa tồn tại.
- (event) Vòng review 2 chạy hai lăng kính: đối kháng trên bản đã vá, và kiểm nhất quán sáu lượt. Kết quả 8 mâu thuẫn MỚI do chính bản vá vòng 1 sinh ra, cộng 31 phát hiện truy vết. Bốn mâu thuẫn ở mức CHẶN — hệ không khởi động nổi.
- (change) M1 vòng lặp ghi vết, người dùng yêu cầu vá luôn. Gốc: Cổng vừa quyết định vừa ghi, mà ghi thì cần Prisma trong khi AD-1 cấm src/autonomy nhập Prisma, còn đi qua capability thì phải qua chính Cổng — đệ quy vô hạn ngay lời gọi capability ĐẦU TIÊN của cả hệ. Vá bằng cách TÁCH QUYẾT ĐỊNH KHỎI I/O: gate.ts thành hàm THUẦN decide(entry, actor, ctx) không I/O không nhập gì; registry.ts ở src/capability gom ngữ cảnh qua collectGateContext, gọi decide, rồi writeAuditRow. Không cần ngoại lệ nào vì src/capability vốn được nhập src/core. Cách này còn khớp đúng bảng phân tầng (Cổng không chịu trách nhiệm truy cập dữ liệu) và làm gate.ts kiểm thử được bằng bảng đầu-vào-đầu-ra thuần.
- (change) M2 lỗ ĐỌC: src/app không có một dòng mã hợp pháp nào để lấy dữ liệu, vì AD-1 cấm nhập Prisma và src/core, còn AD-2 khai phạm vi sổ đăng ký CHỈ GỒM THAO TÁC GHI. Vá bằng cách mở phạm vi sang cả ĐỌC lẫn GHI. Capability đọc nằm ở khối CAP_CHI_NGUOI nên thêm màn hình mới không làm đỏ T-10, khối một vẫn bằng đúng chín.
- (change) M3 vị từ chạm ghi số hai có BA phiên bản khác nhau ở Mục 0, AD-3 và ontology. Kiểm nguyên văn D12: 'Ô đang trống HOẶC ô do chính hệ thống đặt trước đó thì tự điền'. Bản vá vòng 1 của tôi đánh rơi nhánh ô-do-máy-đặt, và không có nhánh đó thì bất biến lấy-hạn-ngắn-nhất của ontology KHÔNG CÓ ĐƯỜNG CÀI. Giờ ba nhánh: ô trống, ô máy đặt và chỉ để rút ngắn hạn, ô người đặt quá hạn khi cờ bật. Đổi tên capability setNextActionIfEmpty thành setNextAction vì tên cũ khai một vị từ đã hết đúng, mà T-10 khẳng định theo TÊN.
- (change) M4 bước gieo đặt sai chỗ và không có nguồn hợp pháp. Hạ nhánh seed từ bước 0 xuống bước 2 vì nó là vị từ RỘNG NHẤT, đặt trước bước hẹp nhất là ngược nguyên tắc từ-hẹp-tới-rộng của chính AD-4. Và chế-độ-gieo thành THAM SỐ DỰNG createGate({seedMode}), không phải thứ Cổng tự đánh hơi từ npm_lifecycle_event hay process.argv — cả hai đều là ngữ cảnh ngầm mà AD-5 cấm. Không có tham số dựng thì MỌI T đỏ ở beforeEach vì npm test không phải npm run seed.
- (change) M5 instrumentation.ts là vùng không luật: nó ở gốc dự án, ngoài mọi thư mục src/, nên danh sách CHẶN của AD-1 không phủ nó — mà nó lại là tệp khởi động vòng quét. Đổi AD-1 sang DANH SÁCH CHO PHÉP với bảng bốn dòng, quét toàn repo. Đây là thay đổi rẻ nhất và bịt được nhiều nhất: bốn trong tám mâu thuẫn vòng 2 có chung gốc là danh sách chặn bỏ sót.
- (change) M6 thứ tự phân xử thiếu hàng trên cùng. Thêm đề bài chính thức lên trên Mục 0, và gộp nội dung với ngữ nghĩa thành MỘT thứ tự vì không tranh chấp thật nào tự khai mình thuộc loại nào. Mục 0 được phép chặt hơn đề bài ở các dòng nhãn làm-chặt-hơn vì đó là bỏ bớt quyền hệ thống. Định nghĩa hẹp cho hình-dạng-lưu-trữ, liệt kê hết. Và sửa tử số error-detection rate trong ontology từ dấu cộng thành HOẶC — cộng hai bộ đếm sự kiện trên mẫu số đếm Phát hiện phân biệt làm chỉ số vượt 100% trước mặt giám khảo.
- (change) M7 mốc cắt giữa bóc thẻ HTML và chuẩn hoá văn bản bỏ trống, nên AD-17 và AD-18 giẫm nhau: cắt kiểu nào cũng làm đỏ nghiệm thu của AD bên kia, và hai cách cắt cho hai bộ offset khác nhau. Chốt: src/ingest chịu mọi thứ phụ thuộc định dạng nguồn và không bao giờ chạm khoảng trắng hay Unicode; normalize.ts chịu ngược lại và không bao giờ thấy một thẻ HTML nào. Cấm .replace(/s+/ và .normalize( ở CẢ src/ingest.
- (change) M8 vế WHERE của kiểm-và-ghi chỉ so cột nội dung nên không phủ nhánh quá hạn: người dời hạn mà không đổi nội dung vẫn bị máy ghi đè, ô đã hết quá hạn mà câu lệnh vẫn khớp. Giờ vế WHERE lặp lại TOÀN BỘ vị từ, gồm cả due_date và set_by.
- (constraint) HAI SỰ KIỆN SAI trong spine, đã kiểm nguồn và sửa. Một: quỹ giờ là 4,5 tiếng (9:30-12:00 cộng 13:00-15:00 theo thể lệ), không phải 5 tiếng 30 phút — PRD đã đúng, spine sai. Hai: đề bài mục 7 dòng 202 viết nguyên văn 'Bản dựng production: không dev server, không hot reload, không bật chế độ gỡ lỗi', nên quyết định vòng 1 dùng npm run dev là VI PHẠM ĐỀ BÀI. Đổi sang npm ci và npm run build một lần lúc dựng máy, rồi npm start chạy bản production. Luật này còn gỡ luôn nguy cơ hot-reload sinh hai vòng quét song song của AD-12.
- (change) Vá 31 phát hiện truy vết: FR-38 sửa thành NFR-16 ở AD-7, BR-D7 sửa thành FR-51 ở AD-22, sáu bước sửa thành bảy bước ở ba chỗ, sáu quan hệ ERD đảo chiều cho khớp câu ontology, cardinality Gợi ý tới Phát hiện chốt là đúng-một để khớp khoá ngoại của AD-22, participant C thiếu ở kịch bản C, alt/else ở kịch bản A biến hai việc thành loại trừ nhau nên tách thành opt, thêm ESLint và Claude Code CLI vào Stack, thêm bảy tệp chủ của luật vào cây nguồn gồm agent/client.ts, capability/mcp-server.ts, scan/loop.ts, prisma/seed.ts, next.config.ts, vitest.config.ts, eslint.config.mjs.
- (event) Spine sau vòng vá 2: 22 AD, lint 0 phát hiện, bảy sơ đồ mermaid cân alt/end. Chuẩn bị vòng review 3.
- (decision) Thêm Fluent UI v9 vào bảng Stack: @fluentui/react-components 9.74.6 và @fluentui/react-icons 2.0.337, không cài @fluentui/tokens riêng vì gói độc lập vẫn là alpha. Lý do sườn phải cập nhật: chính sườn đặt luật mọi dòng ghim tường minh trong package.json, một ô để trống là trạng thái không ai trong đội đã thử.
- (decision) Chốt lớp AI chạy Haiku cho mọi lệnh gọi, ghim ở ba tầng: options.model, CLAUDE_CODE_SUBAGENT_MODEL, ANTHROPIC_DEFAULT_HAIKU_MODEL. Đặt một biến là chưa đủ vì CLAUDE_CODE_SUBAGENT_MODEL mới là thứ ghi đè model khai trong AgentDefinition và frontmatter subagent, còn ANTHROPIC_DEFAULT_HAIKU_MODEL mới phủ các lệnh gọi nền. Ghi vào AD-15 vì đó là chỗ khai biến môi trường nào được đọc và bởi ai.
- (change) Xoá hai mục Deferred đã hết hiệu lực sau khi DESIGN.md viết lại trên lớp token Fluent: nối _ds_bundle.js của Claude Design vào React 19, và tokens/fonts.css gọi Google Fonts cho Noto Serif. Fluent đặt Segoe UI đầu chuỗi, đã kiểm đủ 90/90 glyph khối U+1EA0-U+1EF9 bằng cách đọc bảng cmap của tệp font, nên ràng buộc chạy offline cũng hết.
- (event) Ba sửa đổi trên phát sinh ngoài bước kiến trúc, trong phiên làm việc ngày 14/08 sau khi người dùng thử chê giao diện. Sườn lúc đó đã ở trạng thái sau vòng review 3.
- (event) Vòng review 3 chạy ba lượt: mâu thuẫn do vòng 2 sinh ra, tham chiếu chết sau khi đổi tên, và mermaid so với luật. Kết quả 8 mâu thuẫn CHẶN cộng 17 phát hiện truy vết cộng 6 phát hiện sơ đồ. Bản rà soát nói thẳng: bảy trên tám quy về ĐÚNG HAI GỐC, vá hai chỗ đó là đóng cả bảy.
- (change) GỐC MỘT: AD-2 mở phạm vi sổ đăng ký sang ĐỌC ở vòng 2 nhưng giữ nguyên phát biểu T-10 khối một bằng đúng chín. Vòng quét là actor system và phải đọc danh sách Công ty theo AD-12, đọc cờ phanh và trần ngân sách theo AD-11 — mà đưa readAccountList và readSetting vào thì khối một hết bằng chín và T-10 đỏ, còn không đưa vào thì bước 3 từ chối mọi lời gọi và T-8 T-9 đỏ. Không có lối ra thứ ba. Vá bằng cách TÁCH KHỐI MỘT THÀNH HAI HẠNG: hạng ghi đúng sáu capability và đó mới là tập T-10 khẳng định bằng đúng, vì NFR-14 tới NFR-17 nói về chỗ máy GHI chứ không nói chỗ máy đọc; hạng đọc-chung mở, allowedActors gồm cả ba, và một phép kiểm khẳng định không mục nào của hạng này nhập hàm ghi của src/core. Mệnh đề chứng minh vắng mặt đổi thành máy ghi được đúng sáu chỗ, đây là cả sáu, phép kiểm đỏ nếu có cái thứ bảy.
- (change) GỐC HAI: AD-4 đảo chiều ở vòng 2 (sổ đăng ký gọi Cổng) nhưng bảng danh sách CHO PHÉP của AD-1 giữ chiều cũ (Cổng gọi sổ đăng ký). Hàng src/capability thành hàng chết, còn bốn nơi thật sự phải nhập nó — src/scan, src/app, src/agent, tests, prisma/seed.ts — đều rơi vào ô mọi-nơi-khác-cấm. Người dựng eslint.config.mjs theo đúng bảng sẽ CHẶN ĐIỂM VÀO CỦA TOÀN HỆ. Đây là mâu thuẫn nguy hiểm nhất về mặt hai-người-xây-độc-lập vì cả hai đều tuân thủ tuyệt đối. Vá bằng bảng năm hàng theo chiều mới, cộng một câu nói rõ Cổng là nhánh cụt trong đồ thị phụ thuộc.
- (change) A2 tự khoá: bước 6 và 7 của Cổng từ chối đúng hai capability là cơ chế thi hành của chính trần đó. Điều kiện dừng 4 đòi tự tắt AI khi chạm 100% ngân sách nhưng bước 6 từ chối disableAi với mã limit; điều kiện dừng 2 đòi ghi Nhật ký dừng-vì-phanh nhưng bước 7 từ chối writeScanLog với mã brake. Kết cục AI không bao giờ tự tắt được và không điều kiện dừng nào để lại được dòng Nhật ký mà T-8 đòi truy vấn. Vá bằng cờ selfLimiting bật ở ĐÚNG HAI mục, có phép kiểm.
- (change) A4 ghi vết ghi TRƯỚC khi capability chạy: loadCapability chỉ trả hàm, nên dòng ghi vết commit xong rồi entry.fn ném lỗi thì ghi vết khai một thao tác chưa hề xảy ra — đúng ca T-10 phải phân biệt. Và ở thời điểm đó chưa ai đọc bản ghi đích nên giá-trị-cũ và giá-trị-mới mà quy ước đòi không tồn tại để ghi. Vá: từ chối thì ghi ngay vì chưa có giao dịch nào để bám; cho phép thì mở một giao dịch bao trọn, chụp before, chạy fn, ghi vết trong cùng tx. Phép kiểm đếm đổi thành số dòng bằng số lần từ chối cộng số lần capability chạy xong.
- (change) A5 createGate seedMode không có đường nối tới loadCapability, và tests cần cả hai giá trị trong một tiến trình Vitest (T-10 đòi false, tiện ích gieo đòi true). Vá bằng cách chuyển seedMode thành tham số dựng của SỔ ĐĂNG KÝ chứ không phải của Cổng — createRegistry({seedMode}) — vì sổ đăng ký mới là thứ có điểm vào. Cổng vẫn thuần và nhận seedMode trong ctx.
- (change) A8 cascades: AD-21 đòi cùng giao dịch nhưng không có gì mang giao dịch đi, vì AD-5 khai capability nhận actor làm tham số đầu mà không có tham số tx. Chốt cascades là HÀM NỘI BỘ của src/core nhận tx và actor của lời gọi gốc, không phải mục sổ đăng ký — nếu là mục thì mỗi cái mở giao dịch riêng và D26 để lại trạng thái nửa vời khi bước hai hỏng.
- (change) Ba sơ đồ vẽ sai luật, đã sửa. Kịch bản B vẽ src/app đọc THẲNG cơ sở dữ liệu — đúng thứ AD-1 vừa cấm tuyệt đối, và là ví dụ vẽ đẹp nhất của đường tắt mà AD-1 sinh ra để chặn. Kịch bản A vẽ vòng quét tự lấy khoá không qua capability nào, phải thêm acquireAccountLock và releaseAccountLock vào khối ba. Sơ đồ Design Paradigm vẫn vẽ gate tới cap, ngược chiều AD-4 — mà đây là sơ đồ người ta đọc trước tiên và nhớ lâu nhất.
- (change) Đổi tiền tố bộ phân loại thất bại từ F1-F10 thành FT1-FT10, vì F8 và F10 đang mang HAI NGHĨA khác nhau ở hai tài liệu cùng ràng buộc: F8 của spine là cổng tự chủ quyết sai, F8 của Mục 0 là ghi đè Việc tiếp theo quá hạn. Cộng 16 sửa chuỗi khác: quy ước tên capability còn ghi tiếng-Việt-không-dấu kèm ví dụ tiếng Anh, hai tên trường cho một thứ (ten/vung so với name/zone), ontology khai thứ tự phân xử ngược AD-13, số bước Cổng còn lệch ở bốn chỗ, ràng buộc unique thiếu vế deleted_at, cờ CLI --giu-ghi-vet đổi thành --keep-audit, quỹ giờ 4,5 trừ 2,5 ra 2 chứ không phải 2,5.
- (event) Spine sau vòng vá 3: 22 AD, lint 0 phát hiện, bảy sơ đồ mermaid cân alt/end và không lane nào thiếu. Bản rà soát đưa tiêu chí dừng: nếu vòng 4 chỉ còn tìm ra lỗi cỡ sửa-chuỗi thì đó là lúc dừng vá.
- (decision) Người dùng yêu cầu chạy bmad-architecture với BABOK hub cho TỪNG KHỐI CON của Design Paradigm. Đã phóng năm run epic-altitude song song, mỗi tầng một thư mục riêng: tier1-tuong-tac, tier2-agent, tier3-autonomy, tier4-capability, tier5-core. Mỗi run thừa kế AD-1 tới AD-22 làm ràng buộc read-only, đánh số riêng theo tiền tố AD-UI, AD-AG, AD-GT, AD-CP, AD-CR để không đụng dải cha, và chỉ quyết cái cha để mở. AD mới mâu thuẫn AD cha là xung đột phải nêu lên chứ không override.
- (assumption) RỦI RO THỜI ĐIỂM đã biết: năm run tầng con đọc spine cha TRƯỚC khi vòng vá 3 hoàn tất, nên phần Inherited Invariants của chúng có thể trích bản cũ ở ba chỗ — bảng danh sách CHO PHÉP của AD-1, mệnh đề chín capability của AD-2, và khối mã loadCapability của AD-4. Mã AD giữ nguyên nên tham chiếu không chết; phải đối chiếu ba chỗ đó khi năm run báo cáo về.
- (event) Bốn trên năm run epic-altitude đã về: tier2 Agent 11 AD-AG, tier5 Core 12 AD-CR, tier1 Tương tác 20 AD-UI, tier4 Capability 10 AD-CP. Tier3 Cổng còn chạy hai lăng kính rà soát. Tổng 53 AD con, tất cả lint sạch. Chúng nêu tổng cộng 20 xung đột với cha; tôi kiểm từng cái, khoảng một nửa là đọc bản cũ trước vòng vá 3, nửa còn lại là thật.
- (change) TÔI SAI VỀ Q15, tier1 chứng minh bằng số. Ở vòng vá 1 tôi khẳng định nhận đúng hình thái thay-hệ-cũ mở ra đường giảm bất định: đọc trước bảng ánh xạ trường Tomahawk §12 tối 14/08. Tier1 đi con đường đó và BÁC: chỉ khoảng 19 phần trăm số dòng chạm mô hình Why Now, Snapshot khớp 0 trường, enum nguồn hẹp hơn đích với 5 giai đoạn so với 7. Đó là migration sang một sản phẩm khác, không phải lược đồ BTC. Đã sửa mục Hình thái: Q15 KHÔNG giảm được bất định, chỉ giảm thiệt hại như AD-17 vốn đã làm. Giá trị thật là phạm vi chờ co lại còn đúng một tệp mapping.ts.
- (change) S4 Bảng Pipeline GỠ khỏi Deferred. Tôi chép lý do hoãn từ bước UX (không hành trình nào dẫn tới) mà không đối chiếu bộ T. T-1 viết nguyên văn kéo cơ hội qua ba giai đoạn, FR-4 là phải-có, và S4 mang FR-4. Hoãn nó là mất MỘT ĐIỂM CHẤM cho một lý do đã hết đúng — T-1 chính là hành trình dẫn tới nó. Thực dựng 11 trên 12 bề mặt, chỉ S7 hoãn.
- (change) BOOTSTRAP ACTOR, tier1 tìm ra: bước 7 của Cổng chặn chính lượt đọc DỰNG NÊN actor. Lượt đọc đó chạy trước khi có actor nên buộc phải đi bằng kind system, và phanh AI tắt thì nó bị từ chối mã brake. Kết cục bấm Tắt AI thì KHÔNG AI ĐĂNG NHẬP ĐƯỢC, T-9 và T-1 đỏ cùng lúc. Cùng hình dạng với lỗi disableAi vòng trước: hạ tầng bị chặn bởi chính cái phanh nó phục vụ. Thêm readUserForAuth vào cờ selfLimiting, giờ đúng ba mục có cờ.
- (change) Ba lỗ do chính vòng vá của tôi để lại, tier1 và tier4 bắt được. Một: AD-11 bắt máy ghi estimated_cost_per_account vào settings mà không capability nào cho phép, thêm recordAccountCost vào khối ba. Hai: bảng Structural Seed vẫn in chín mục cũ trong khi AD-2 đã tách sáu ghi cộng năm đọc-chung — T-10 viết theo bảng nào thì ra hai kết quả, đã viết lại bảng thành hai hạng. Ba: closeSuggestionBySystem vẫn nằm trong khối ba trong khi AD-21 khai cascades là hàm nội bộ không đi qua loadCapability, đã gỡ.
- (change) AD-4 khối mã viết entry.fn(tx, actor) — tx trước actor, vi phạm AD-5 đòi actor LUÔN đầu tiên. Đã đảo. Và disableAi một chiều mà không mục nào bật AI lại được: thêm enableAi vào khối hai, chỉ Quản trị, máy không bao giờ có.
- (constraint) ĐÃ ĐO TẠI CHỖ với Zod 4.4.3: z.toJSONSchema trên z.date() NÉM LỖI Date cannot be represented in JSON Schema, và ném LÚC DỰNG chứ không lúc chạy, nên nó giết tiến trình web khi khởi động. z.iso.datetime() sinh draft-07 hợp lệ. Đã vào AD-6: ngày tháng qua ranh giới là chuỗi ISO, đổi sang Date ở lõi.
- (event) KIỂM VÀ BÁC một khẳng định của tier4: nó nói Prisma 7 bỏ đường nhập @prisma/client. Cài thật prisma 7.9.1 và @prisma/client 7.9.1 rồi require.resolve — module TỒN TẠI, resolve OK, có export dấu chấm. Khẳng định sai. Nhưng nó chỉ ra chỗ mỏng thật: Prisma 7 sinh client ra thư mục do generator khai, nên luật lint chặn mỗi chuỗi @prisma/client sẽ để lọt đường nhập thẳng vào thư mục sinh ra. Đã sửa hàng đầu bảng danh-sách-cho-phép thành chặn HAI tên, và ghim output trong schema.prisma.
- (decision) PHÂN VAI CHA/CON theo đúng luật của skill: cha chốt BẤT BIẾN, con chốt CƠ CHẾ. Áp lần đầu cho ghi vết. Tier5 chỉ ra bản vá vòng 3 của tôi có lỗ: ghi vết trong giao dịch thì khi entry.fn ném BusinessRuleError giao dịch cuộn lại và DÒNG GHI VẾT BIẾN MẤT CÙNG NÓ — mất đúng bằng chứng FT6 cần và đúng thứ Penalty hộp đen của barem hỏi tới. Đã đổi AD-4 sang phát biểu ba bất biến (không lời gọi nào không để lại vết kể cả lời gọi thử rồi hỏng; dòng đó mang giá trị cũ mới khi có ghi thật; không bao giờ khai thao tác chưa xảy ra) và nhường cơ chế ghi hai pha trên một hàng cho AD-CR-8 của tier5.
- (constraint) Ba việc phải làm TỐI 14/08 hoặc đầu giờ 15/08, do các tầng con nêu. Một: đo lại phí tổn token vì AD-AG-8 bỏ preset claude_code và khai systemPrompt tường minh, nên con số 2548 token và 0,025 tới 0,039 đô là của cấu hình cũ mà phanh ngân sách NFR-3 đang đứng trên đó; cùng lượt đo chốt luôn ToolSearch có kích hoạt không. Hai: kiểm glob mcp__crm__* có khớp thật không, đường lui là liệt kê đủ năm tên tool. Ba: chạy thử prisma db push --force-reset xem có hỏi xác nhận không.
- (constraint) Hai bẫy im lặng đáng nhớ nhất từ tier5. Một: market khác country, hai cột rất dễ bị gộp lúc 9h30 sáng, và gộp thì một Gợi ý được duyệt đổi luôn lịch ngày làm việc làm MỌI hạn BR-D7 tính lệch — không phép kiểm nào bắt. Đã ghi cảnh báo thẳng vào ontology. Hai: chỉ mục unique một phần phải viết tay vì Prisma không sinh, nên ai chạy migrate dev sinh migration đè lên thì bất biến một-Gợi-ý-chờ-mỗi-ô biến mất KHÔNG CÓ GÌ ĐỎ cho tới lúc hai Gợi ý mâu thuẫn nằm cạnh nhau trước mặt giám khảo; cần phép kiểm hành vi chèn hai hàng đợi lỗi.
- (constraint) Sáu cột Account chưa ai khai ngoài bảng trọng số 0.2.3: website, specialty_area, revenue_range, deal_value_tier, founded_year, country. Chúng là TỪ VỰNG ĐÓNG của enum o, tức hợp đồng của AD-22, nên đổi là đổi migration. Đã bổ sung vào ontology mục 2 cùng cảnh báo market khác country.
- (assumption) Ghi nhận cho lượt sau: lint_spine.py chỉ khớp regex AD-<số> nên nó KHÔNG kiểm được các khối AD-UI-n, AD-AG-n, AD-GT-n, AD-CP-n, AD-CR-n của năm spine con. Tier1 đã kiểm tay bằng script riêng. Nếu còn dùng dải có tiền tố thì phải kiểm tay hoặc sửa linter.
- (change) Sửa sơ đồ Design Paradigm: bỏ hai cạnh ui-->gate và agent-->gate vì AD-1 cấm mọi thư mục ngoài src/capability và tests nhập src/autonomy; thêm hai cạnh có thật ui-->cap và agent-->cap; thêm gate-->onto và nét đứt chỉ-kiểu gate-->core. Gốc lỗi: bản vá M1 đảo chiều từ gate-gọi-cap sang cap-gọi-gate nhưng chỉ sửa bảng AD-1, bỏ sót sơ đồ. Người dùng phát hiện khi đối chiếu sơ đồ với mô tả.
- (event) Đủ năm spine epic-altitude: tier1 20 AD-UI, tier2 11 AD-AG, tier3 10 AD-GT, tier4 10 AD-CP, tier5 12 AD-CR. Tất cả lint sạch. Tổng 63 AD con.
- (change) LỖI CỦA TÔI VỀ CÁCH VÁ, tầng ③ chỉ ra: tôi sửa MỘT CA của một hình dạng lỗi thay vì quét cả hình dạng. Vòng trước tôi thêm selfLimiting cho readUserForAuth vì bấm Tắt AI thì không ai đăng nhập được. Nhưng releaseAccountLock và recordAccountCost CÙNG hình dạng — cả hai chạy tại hoặc sau lúc trần chạm, nên bước 6 và 7 từ chối chúng. Hậu quả nặng nhất: khoá không nhả, mà AD-12 suy trạng thái vòng-đang-chạy TỪ khoá với lease 10 phút, nên ở nhịp demo 60 giây là MƯỜI VÒNG LIÊN TIẾP BỊ BỎ; giám khảo bật lại AI thì mười phút không có gì xảy ra. Giờ luật phát biểu theo HÌNH DẠNG LỖI kèm bảng năm dòng, selfLimiting là trường bắt buộc không optional.
- (constraint) ĐÃ ĐO, sàn cứng eslint >= 9.37.0. Bản hoà giải bung tarball từng bản: 8.57.1, 9.0, 9.10, 9.15, 9.20, 9.26, 9.30, 9.32, 9.34, 9.36 KHÔNG có allowTypeImports trong luật lõi; 9.37, 9.38, 9.39, 10.8.1 có. Trên 9.36 cấu hình đó làm ESLint CHẾT LÚC NẠP với Unexpected property allowTypeImports, tức cơ chế cưỡng chế DUY NHẤT của AD-1 và AD-15 biến mất mà không ai biết. Bảng Stack của tôi để ô eslint trống với chữ ghim-theo-create-next-app, tức đúng chỗ hở. Đã ghim sàn.
- (constraint) Cũng đo được: consistent-type-imports IM LẶNG ở dạng import { type X }; luật DUY NHẤT bắt được nó là no-import-type-side-effects. Và allowTypeImports xét ở mức cả câu lệnh import nên dạng lẫn import { fn, type X } không lách được. verbatimModuleSyntax bật hay tắt không đổi một dòng kết quả lint nào. Bí danh: create-next-app 16.3.1 khai @/* trỏ về gốc dự án, nên với cây src/ mẫu đúng là @/src/capability/*.
- (change) Ba xung đột chặn của tầng ③ nhắm vào cha, cả ba thật, đã vá. Một: bảng danh-sách-cho-phép của AD-1 không có hàng nào cho src/autonomy gõ kiểu Actor, nên gate.ts KHÔNG BIÊN DỊCH ĐƯỢC và tầng ③ là tầng duy nhất không có đường hợp lệ; thêm hàng ngoại lệ chỉ-kiểu có tên, cưỡng chế bằng allowTypeImports cộng no-import-type-side-effects. Hai: ba cách viết cho vai là vai, role=admin, quan_tri — đợt đổi 59 định danh của tôi BỎ SÓT chữ vai trong kiểu Actor; chốt role với từ vựng đóng sales và admin theo token Mục 0, vì ba cách viết làm bước 5 từ chối Quản trị đúng lúc bấm Tắt AI và T-9 đỏ vì một lỗi chính tả. Ba: kịch bản D ném GateDenied(NFR-14) còn mã mẫu ném GateDenied(d.reason).
- (change) Công thức đếm ghi vết sửa lần hai: số dòng = số lần DECIDE được gọi, không phải số lần loadCapability được gọi. collectGateContext chạy TRƯỚC decide và có thể ném khi Postgres chớp; lời gọi đó chưa hề đi qua Cổng nên chưa có quyết định nào để ghi. Đếm theo loadCapability làm phép kiểm đỏ vĩnh viễn sau một lần mạng chớp, với triệu chứng trỏ vào ghi vết chứ không vào mạng.
- (decision) HOÀ GIẢI MẶT CẮT ③↔④ bằng một thay đổi kiểu có đòn bẩy: GateEntry do tầng ③ SỞ HỮU với năm trường, tầng ④ khai RegistryEntry extends GateEntry. Trước đó gate.ts nhập RegistryEntry mang ZodType và PrismaTx, nên tầng ③ kéo Prisma và Zod vào đồ thị KIỂU của nó — trên clone sạch theo nghiệm thu §7 thì npm test chạy trước prisma generate làm gate.ts đỏ, dù bộ kiểm của Cổng không cần một byte dữ liệu nào, và câu Cổng-kiểm-thử-được-không-cần-CSDL mất hiệu lực đúng chỗ nó quý nhất. Đảo lại thì touches và allowedRoles thành hợp đồng trình biên dịch cưỡng chế TS2741 chứ không còn là trường xin thêm, chu trình kiểu biến mất, và lỗi ctx.seedMode undefined làm seed rỗng và mọi T đỏ ở beforeEach giờ là TS2739.
- (decision) Thêm mục XUNG ĐỘT VỚI SPINE ANH EM vào các spine con. Đây là thứ thiếu về CẤU TRÚC chứ không phải nội dung: bốn va chạm nặng nhất đều nằm ở mặt cắt ③↔④ và spine con chỉ có mục Xung đột với spine cha, nên không có chỗ nào báo lên được. Tầng ③ tự nhận ra và thêm trước khi được yêu cầu.
- (assumption) MẪU LẶP đáng ghi cho lần sau: cùng một mệnh đề selfLimiting rơi HAI VÒNG LIÊN TIẾP ở bước chép lại, vì nó nằm ở cuối một AD-4 dài trên trăm dòng, sau ba đoạn về seedMode. Cách chép hiện tại đọc từ trên xuống và hết sức trước khi tới đó. Lần đồng bộ sau: đọc AD-4 TỪ DƯỚI LÊN. Đã đưa chỉ dẫn này vào prompt của hai lượt hoà giải và đồng bộ.
- (event) Spine cha sau vòng vá 4: 22 AD, lint 0 phát hiện, bảy sơ đồ cân. Đã sửa cây nguồn bỏ createGate cho khớp AD-4, ghim sàn eslint, chốt hạt đếm lệnh gọi mô hình là hạt lệnh-gọi với chủ cài đặt là src/scan/loop.ts.
- (change) Sửa ba sơ đồ tuần tự A, B, C theo đúng chiều sau bản vá M1: mọi lời gọi capability nay đi X->>C rồi C->>G decide, thay cho X->>G rồi G->>C. Đổi nhãn participant thành Capability sổ đăng ký cộng hàm, và Cổng THUẦN, để khớp AD-4. Thêm một Note ở sơ đồ A nói mọi lời gọi C đều qua decide, chỉ vẽ ở chỗ vùng tự chủ khác nhau, tránh phải lặp cạnh decide sáu lần.
- (change) Sửa thêm một chỗ không nhất quán phát hiện lúc sửa: sơ đồ B viết editThenApprove(nguoi, ...) trong khi hai nhánh còn lại và cả sườn dùng human. Đổi thành human.
- (event) Sau khi sửa: không còn cạnh X->>G nào ngoài C và R, không còn G->>C nào. Đếm cạnh gọi Cổng: 10 C->>G, 1 R->>G, 4 G->>G nội bộ. Bảy sơ đồ mermaid cân alt/opt/end.
- (change) Áp Concept Modelling 10.11 và Glossary 10.23 vào ontology theo lựa chọn vá-có-trọng-điểm của người dùng. Hub CÓ hai kỹ thuật này trong generated/catalog.md nhưng bmad-architecture.toml khai 0 lần — tám chỉ thị external_sources không có kỹ thuật từ vựng nào, đó là lý do lượt dựng ontology không hề gọi tới. Ba việc: mục 2 thành noun concept CÓ ĐỊNH NGHĨA mỗi đối tượng một câu nói nó LÀ GÌ; tách bảng thuộc tính thành mục 2b kèm khai rõ lược đồ vật lý do AD-CR-11 tầng 5 sở hữu; thêm mục 15 Glossary ba lớp là đội-nói, trong-mã, tài-liệu-nguồn-gọi. Ontology từ 296 lên 349 dòng, 14 lên 15 mục.
- (constraint) Glossary bắt được bốn cặp dễ nhầm có giá thật. Thị trường khác Quốc gia — gộp thì đổi lịch ngày làm việc và mọi hạn BR-D7 lệch, không phép kiểm nào bắt. Bản thô khác nguyên văn — so câu trích với bản thô thì mọi Phát hiện rụng với triệu chứng giống mô hình bịa câu trích. Cơ hội bằng deal của Tomahawk nhưng Tomahawk có 5 giai đoạn còn đề bài 7, không ánh xạ một-một. Việc tiếp theo KHÁC Task của Tomahawk. Cộng mục từ cố ý không dùng: pipeline value và doanh thu bị D34 cấm.
- (decision) Nối hai chỉ thị mới vào _bmad/custom/bmad-architecture.toml, giờ 10 external_sources. Kiểm bằng tomllib: hợp lệ, cả hai có mặt. Hub gốc nằm ở C:/Workspace/dev/personal/babok-bmad-skill chứ không phải babok-bmad như registry ghi — KHÔNG sửa hub vì đó là repo khác và người dùng chưa yêu cầu.
- (constraint) ĐÃ ĐO glob MCP hai lượt: allowedTools dạng mcp__crm__* CÓ KHỚP, handler chạy đúng một lần mỗi lượt, chi phí ngang với liệt kê đủ tên là 0,075 so với 0,076 đô. Giữ glob. Đường lui nếu sáng 15/08 nó im lặng không khớp là liệt kê đủ tên, triệu chứng nhận ra ngay bằng handler chạy 0 lần.
- (constraint) PHÁT HIỆN QUAN TRỌNG từ chính lượt đo đó: chi phí một lượt gọi TẢN RẤT RỘNG. Cùng lời nhắc cùng cấu hình, ba lượt cho 0,075 và 0,076 và 0,073 đô, MỘT lượt cho 0,727 đô — gấp mười lần trung vị. Lượt đo đầu suýt làm tôi kết luận sai rằng glob đắt gấp tám lần; đo lại lần hai mới thấy đó là nhiễu. Hệ quả cho AD-11: estimated_cost_per_account phải lấy GIÁ TRỊ LỚN NHẤT quan sát được chứ không lấy trung bình, vì đặt trần bằng trung bình thì một Công ty xui làm vòng quét cắt sớm giữa chừng — đúng thứ luật đơn vị công việc sinh ra để chặn. Gặp lượt đắt hơn thì tăng, không bao giờ giảm.
- (change) Sửa hai chỗ còn ghi BỐN lệnh một-bước: CHECKLIST.md dòng 41 và docs/chien-luoc-ngay-thi.md dòng 79 và 83. §7 có BA lệnh, đã kiểm nguyên văn 14/08; lệnh dừng là phương tiện chứng minh dữ liệu còn nguyên chứ không phải mục nộp bài.
- (constraint) prisma db push --force-reset: đọc help của prisma 7.9.1 — cờ này nghĩa là buộc reset trước khi push, và --accept-data-loss là cờ RIÊNG để bỏ qua cảnh báo mất dữ liệu. Trong globalSetup của kiểm thử nên dùng cả hai, hoặc tốt hơn là prisma migrate deploy trên một database riêng cho test.
- (event) BƯỚC KIẾN TRÚC ĐÓNG. Sáu spine đều status final: spine cha 22 AD cộng năm spine tầng với 21 AD-UI, 11 AD-AG, 10 AD-GT, 10 AD-CP, 13 AD-CR. Tất cả lint 0 phát hiện. Đã qua ba vòng review đối kháng, một lượt hoà giải mặt cắt tầng 3 với tầng 4, và một lượt đồng bộ tầng 1 với tầng 5.
- (event) spine finalized
- (event) Chạy lại đủ BẢY chỉ thị BABOK trên CẢ BỘ: doc_standards 5 mục (1 shipped bmad-review lenses structure prose, cộng 4 của hub) và finalize_reviewers 2 mục, soi cha cộng năm spine tầng cộng ontology cộng ma trận. Bốn trên năm lăng kính về; lăng kính kiểm-thực-tế bị người dùng dừng giữa chừng nên KHÔNG khởi lại.
- (change) VI PHẠM DO CHÍNH TÔI GÂY RA HÔM NAY: ba chỗ tôi vừa thêm vào ontology là Concept Modelling 10.11, Data Modelling 10.15, Glossary 10.23 chính là rải tham chiếu khung, đúng thứ chỉ thị doc_standards 4 cấm. Số hiệu đều đúng nhưng BABOK là khung của công cụ đội dùng, không ai chấm đội theo nó. Đã gỡ tên và số, giữ nguyên nội dung. Ontology giờ 0 lần nhắc khung; toàn bộ 8 tệp bàn giao còn đúng một chỗ xuất xứ ở ma-tran dòng 3.
- (change) Hình thái: bốn chỗ nói thiếu hoặc quá tay, đã sửa. Một, spine viết kỹ-thuật-đúng-là-phỏng-vấn-workshop-chứ-không-phải-khảo-sát trong khi đội ĐANG chạy bộ 116 dòng có cấu trúc, và với dân số 6 người có tên thì khảo sát không phải proxy mà là ĐIỀU TRA TOÀN BỘ. Hai, spine viết giảm-thiểu-duy-nhất cho cặp vai Domain SME kiêm End User trong khi mối kiểm CÓ tồn tại — bộ câu hỏi đã tách Phần A quản lý và Phần B rep; khiếm khuyết thật là kênh truyền đi qua chính người bị kiểm (nhờ anh chuyển) và chưa gửi. Ba, hình thái thay-hệ-cũ khai bốn kỹ thuật chạy hai, đã nêu lý do loại Process Analysis và Data Mining. Bốn, tàn dư đường-giảm-bất-định ở mục Deferred mâu thuẫn với chính mục Hình thái cách đó 1390 dòng.
- (change) doc_standards 2+3 tìm được 4 khoảng trống thật trên 87 AD — tầng 1, 2, 4 không có cái nào. Mẫu hệ thống: đã-loại xuất hiện 13 lần trong spine nhưng 43 lần trong memlog, tức lý do sống ở sổ làm việc mà người đọc spine sáng mai không mở. Đã kéo hai lý do quan trọng nhất về spine và bổ sung hai phương án loại cho selfLimiting.
- (change) TÔI LẬP LUẬN SAI ở lý do loại phương án hai-actor-là-hằng-số-biên-dịch: tôi viết phá T-1, nhưng hằng số vẫn mang vai thật nên T-1 vẫn xanh. Lý do THẬT nằm ở tầng 5: nó phá AD-UI-10 vì vai phải đọc SỐNG (giám khảo gieo lại dữ liệu giữa demo thì phiên cũ giữ vai cũ) và làm actor_user_id trỏ vào hư không. Đã thay.
- (change) finalize_reviewer 2 đối kháng tìm SÁU mục chặn, hai là lỗi biên dịch thật. Nặng nhất: tử số cost_used_usd của phanh ngân sách chỉ tồn tại ở MỘT dòng của tầng 4, không ai khai cột không ai ghi — budgetUsedRatio thành NaN, bước 6 không bao giờ chặn, điều kiện dừng 4 là đường chết, và cờ selfLimiting của disableAi đang bảo vệ một đường không chạy. Đã khai cột và chỉ định người ghi là src/scan/loop.ts.
- (change) maxBudgetUsd là trần của MỘT query(), không phải của cả vòng — tôi viết sai đơn vị. Theo số đã đo (0,073 tới 0,727 đô một lượt so với khung vòng 0,50 tới 0,80) thì truyền số cả vòng làm phanh SDK gần như không bao giờ nổ; mà nếu nổ thì error_max_budget_usd ánh xạ về điều kiện dừng 4 và TẮT AI MỘT CHIỀU vì một Công ty đắt bất thường. Hai phanh hỏng theo hai kiểu ngược nhau. Giờ truyền phần CÒN LẠI là scan_budget_usd nhân budget_stop_ratio trừ cost_used_usd, để hai phanh chạm cùng một ngưỡng.
- (change) Hai lỗi biên dịch: params khai z.ZodType nhưng AD-CP-6 truyền entry.params.shape, mà .shape chỉ có trên ZodObject — đã kiểm tại chỗ với zod 4.4.3, ZodType thật sự không có .shape. Đổi sang z.ZodObject<z.ZodRawShape> ở cả tầng 3 và tầng 4. Và CapContext mang tx trong khi AD-CR-7 đặt giao dịch ở LÕI — tầng 4 mở thêm giao dịch nữa là hai giao dịch trên hai kết nối, completeAuditRow rơi ra ngoài giao dịch chính và phá bất biến pha-2-trong-tx của AD-CR-8. Đã bỏ tx khỏi CapContext và bỏ db.$transaction khỏi bước 7.
- (change) Màn đăng nhập S11 không có capability nào liệt được hai tài khoản: readUserForAuth đọc MỘT, mà S11 phải render HAI nút, và ba lối khác đều bị chặn sẵn. Thêm readLoginCandidates, cùng hình dạng lỗi với readUserForAuth nên cũng mang selfLimiting — giờ SÁU mục có cờ.
- (decision) Khối ba CAP_SYSTEM_INTERNAL đổi từ DANH SÁCH sang VỊ TỪ: mọi mục allowedActors chỉ system chạm chỉ bảng hạ tầng. Ba tài liệu đang khai 5 so với 10 mục và không cách nào cùng xanh. Đếm cứng thì mỗi lần tầng con cần một lượt đọc hạ tầng lại phải xin cha sửa số; vị từ thì thêm màn hình vận hành không làm T-10 đỏ.
- (change) doc_standard 1 tìm 44 đề xuất, ba việc phải xong trước khi ai viết T-10. Một, readAccountList nằm ở HAI KHỐI LOẠI TRỪ NHAU nên phép kiểm không-giao đỏ ngay lần chạy đầu. Hai, AD-2 và AD-21 khai NGƯỢC NHAU về chỗ đứng của hệ quả dây chuyền. Ba, cột đích của Gợi ý mang hai tên là target_field ở cha và o ở tầng 5, tức hai migration khác nhau và lỗi chỉ lộ lúc chạy. Đã vá cả ba.
- (change) 41 ĐỊNH DANH TIẾNG VIỆT còn sót, vi phạm chính quy ước tôi đặt: CAP_MAY_DUOC, CAP_CHI_NGUOI, CAP_HE_THONG_NOI_BO (ba tên hằng số này đi thẳng vào mã T-10), tenCapability, thamSo, thoi_gian_quyet, đọc được. Đã đổi hết sang tiếng Anh trên sáu spine và ontology.
- (change) Ba lỗi cơ học đáng nhớ. Một, đường dẫn nguồn của ontology dùng gạch DÀI thay gạch nối nên không mở được — ngay dòng nguồn-của-tệp-này-khai-một-lần. Hai, một ô bảng vỡ vì chuỗi xuống-dòng bị diễn giải thành ký tự thật, làm MẤT chính ký tự đang được đặt tên, ở đúng mục AD-18 nói lệch-một-ký-tự-là-lệch-offset. Ba, một câu thiếu chữ khi khiến nó đọc ngược nghĩa: đặt-ở-đây-để-nó-trôi thay vì để-KHI-nó-trôi.
- (change) Tầng 5 thiếu HAI BẢNG bị đặt hàng mà chưa ai khai: scan_log_entry (AD-UI-14 đặt hàng) và notification (đích của cascade createNotification FR-31 cùng hai capability readNotifications và markNotificationSeen của tầng 4). Không có chúng thì S0 không render được và FR-31 không dựng được — đúng hình dạng lỗi mà AD-CR-13 đã tự bắt cho bảng user. Tổng bảng hạ tầng bảy, không phải năm.
- (change) estimated_cost_per_account đổi thành CHẠY-MAX trong bộ nhớ của vòng, không phải số chốt lúc mở vòng. Chốt lúc mở vòng thì cả 15 Công ty của vòng ĐẦU chạy vô điều kiện vì lịch sử vẫn rỗng ở Công ty thứ hai thứ ba — mà vòng đầu chính là vòng chạy trước mặt giám khảo, tức luật đơn-vị-công-việc-trọn-vẹn mất hiệu lực đúng lúc nó được chấm. Với chạy-max thì chỉ Công ty đầu tiên không được bảo vệ.
- (event) Sau vòng vá này: sáu spine lint 0 phát hiện. Người dùng dừng lăng kính kiểm-thực-tế giữa chừng nên nhóm khẳng định phiên bản và tương thích chéo CHƯA được rà lại lượt này — bản rà trước đó vẫn còn hiệu lực.
- (event) Lăng kính kiểm-thực-tế bị dừng giữa chừng nhưng vẫn kịp chốt báo cáo, và nó tìm ra HAI LỖI CHẶN mà tra tài liệu KHÔNG THẤY — chỉ lòi ra vì cài gói thật. Đây là bằng chứng cho nguyên tắc chạy-thử hơn tra-tài-liệu hơn suy-luận.
- (constraint) CHẶN 1, đã tự tái hiện độc lập: Prisma 7 KHÔNG DỰNG ĐƯỢC PrismaClient nếu thiếu driver adapter. new PrismaClient() ném PrismaClientInitializationError với thông điệp A driver adapter is required to connect to your database. Bảng Stack thiếu @prisma/adapter-pg, và tier5 AD-CR-6 viết base.$extends mà không bao giờ khai base dựng thế nào. Hậu quả: dòng mã đầu tiên chạm cơ sở dữ liệu ném lúc khởi tạo, npm run seed chết, npm test chết, npm start chết, cả T-1 tới T-10 đỏ cùng lúc với triệu chứng trỏ vào cấu hình chứ không vào mã. Đã thêm hàng Stack, mở dòng AD-1 phủ ba tên, và khai base kèm adapter ở tier5.
- (constraint) CHẶN 2: Prisma 7 KHÔNG tự chạy prisma generate khi cài. Thư mục sạch, npm i, rồi import ngay thì Error Cannot find module .prisma/client/default. Cổng clone-sạch của §7 hỏng: giám khảo clone, npm ci, docker compose up -d, npm run seed rồi nổ ở dòng import đầu tiên. Đã đổi seed thành ba lệnh prisma generate rồi migrate deploy rồi node prisma/seed.ts, cộng postinstall. Node 24 chạy .ts thẳng nên không cần tsx.
- (change) B3 sửa lần hai và lần này mới đúng ngữ nghĩa. Vòng trước tôi vá thành truyền phần-còn-lại vào maxBudgetUsd; bản kiểm-thực-tế chỉ ra cách đúng là TÁCH HẲN HAI CON SỐ. maxBudgetUsd là trần của MỘT query theo sdk.d.ts, và AD-AG-2 chốt một Bản lưu bằng một query độc lập không resume nên KHÔNG có tích luỹ xuyên lời gọi. Giờ: trần vòng là bộ đếm cộng dồn cost_used_usd trong loop.ts so với scan_budget_usd, đó mới là nguồn của điều kiện dừng 4; còn maxBudgetUsd là trần một lượt đặt khoảng 2 lần giá trị lớn nhất quan sát, và error_max_budget_usd nghĩa là Bản-lưu-này-quá-đắt-bỏ-vòng-đi-tiếp chứ KHÔNG phải điều kiện dừng 4.
- (change) TÔI BÁC SAI một phát hiện của tầng 4. C7 gồm ba mệnh đề: cấm url trong datasource (ĐÚNG), bắt buộc driver adapter (ĐÚNG), và đường-nhập-không-còn-là-@prisma/client (SAI). Tôi bác đúng mệnh đề ba nhưng câu bác nuốt luôn mệnh đề hai, mà mệnh đề hai là mệnh đề CHẶN. Đã viết lại thành ba dòng phân xử riêng.
- (change) Sáu sửa nhỏ từ cùng bản: số CVE của Node là 11 không phải 12; eslint ghim 9.39.5 thay vì dải mở >=9.37.0 vì npm chọn bản theo bộ peer chứ không tất định; peer của Agent SDK là >=0.93.0 chứ 0.117.1 là bản mới nhất không phải sàn; tên kiểu thật là AnyZodRawShape không phải ZodRawShape nên gõ nhầm thì không import được; outputFileTracingIncludes VÔ TÁC DỤNG với kiến trúc đã chốt vì nó chỉ tác động output standalone mà cha chạy next start ngay trong thư mục dự án — giữ khoá thì vô hại nhưng đừng coi là cơ chế.
- (constraint) MÁY ĐỘI VẪN ĐANG CHẠY NODE 24.15.0 trong khi Stack đã ghim 24.19.0 — kiểm bằng node -v. Việc nâng đã chốt mà chưa thi hành. Phải nâng TỐI 14/08 cùng lượt npm ci, không thì sáng 15/08 mất thời gian cài Node trong quỹ 4,5 tiếng.
- (event) Bản kiểm-thực-tế cũng xác nhận 20 khẳng định là ĐÚNG, gồm cả bốn subtype SDK, sáu tuỳ chọn query, tương thích chéo toàn bảng Stack dựng thật build xanh, instrumentation.ts với register và NEXT_RUNTIME chạy thật in ra ROOT_INSTRUMENTATION_RAN nodejs, và Fluent 9.74.6 với React 19.2.8 trên Turbopack build production XANH prerender 3/3 — mục Deferred của tier1 ghi bug-đang-mở-chưa-xác-minh-được nay có bằng chứng dương một phần. Còn ngỏ: chưa thử DataGrid, và nhập Fluent bằng Node ESM trần ném SyntaxError về tabster nên nếu tệp Vitest chạy môi trường node mà nhập Fluent thì có thể vướng.
- (event) Nâng Node lên 24.19.0 bằng winget gói OpenJS.NodeJS.LTS — đúng bản Stack ghim, không phải bản 26 mặc định. npm lên 11.17.0.
- (event) DỰNG KHUNG DỰ ÁN. create-next-app 16.3.1 với --ts --app --src-dir --eslint --no-tailwind, dựng ra scratchpad rồi ghép vào để KHÔNG đè bốn tệp repo đã có là .gitignore, README.md, AGENTS.md, CLAUDE.md. .gitignore được NỐI thêm khối Next, mục cấm phát tán BABOK còn nguyên. package.json ghim đủ bảng Stack gồm @prisma/adapter-pg và test là vitest run.
- (constraint) BÍ DANH: bản rà trước kết luận SAI. Nó nói create-next-app khai @/* trỏ về gốc dự án nên phải viết @/src/capability/*. Dựng thật với --src-dir cho paths là @/* trỏ ./src/*, tức @/capability/* mới đúng. Bản rà đọc template KHÔNG dùng --src-dir. Đã sửa tầng 3 và tầng 4, kể cả các dòng import type mẫu — để nguyên thì luật lint mang mẫu không bao giờ khớp, tức một luật xanh vĩnh viễn, đúng cái bẫy hai tầng đó tự cảnh báo.
- (event) NGHIỆM THU B1 VÀ B2 BẰNG CHẠY THẬT. prisma generate in ra Loaded Prisma config from prisma.config.ts rồi Prisma schema loaded from prisma/schema.prisma, exit 0, 3 giây. Hai phát hiện chặn về driver adapter và về việc Prisma 7 không tự chạy generate giờ không còn là suy luận từ tài liệu.
- (constraint) NGUYÊN NHÂN npm CHẬM, đo được: @fluentui/react-components là 68 gói và 51.725 TỆP, chiếm phần lớn khối lượng node_modules. Trên Windows có Defender quét từng tệp thì đó là lý do năm lượt cài đều lê thê. Đây không chỉ phiền tối nay: nếu sáng 15/08 phải cài lại vì bất kỳ lý do gì thì mất hàng chục phút trong quỹ 4,5 tiếng.
- (event) SAI LẦM QUY TRÌNH CỦA TÔI: chạy npm install rồi gõ lệnh tiếp theo trong khi nó ở nền, lặp năm lần. Chính việc đó giết tiến trình cài giữa chừng và làm hỏng node_modules — cuối cùng đúng MỘT gói hỏng là next, thiếu package.json nên bin không liên kết được. Bài học: khi lượt cài xuống nền thì KHÔNG gõ gì thêm cho tới khi có thông báo.
- (event) Sửa xong bằng ba bước nhỏ thay vì cài lại toàn bộ: npm rebuild nối lại .bin trong 4 GIÂY (93 mục); quét toàn cây tìm gói thiếu package.json ra đúng một cái là next; xoá và cài lại riêng next trong 55 giây. npm run build sau đó XANH trong 10 giây, prerender 4/4.
- (constraint) PHÁT HIỆN MỚI, đã đo: Turbopack phân giải await import() LÚC BUILD. Thiếu src/scan/bootstrap.ts thì next build đỏ ngay với Module not found, chứ không phải đỏ lúc chạy. Nhập động tránh việc NẠP trong edge runtime, không tránh việc PHÂN GIẢI — nên tệp phải tồn tại từ đầu dù rỗng. Đã ghi vào AD-UI-1.
- (change) Viết bốn tệp hạ tầng cộng hai tệp Prisma cộng một khung rỗng. docker-compose.yml có hai service: db dùng volume có tên với healthcheck pg_isready, và db_test dùng tmpfs ở cổng 5433 để npm test không đụng database demo. vitest.config.ts đặt fileParallelism false vì T-1 tới T-10 chạm cùng một database. instrumentation.ts đúng ba việc với nhập động. .env.example — phát hiện .env* trong .gitignore nuốt luôn tệp mẫu nên đã thêm ngoại lệ !.env.example. prisma/schema.prisma và prisma.config.ts là khung tối thiểu, CỐ Ý chưa có mô hình nào vì đó là AD-CR-11 của tầng 5. src/scan/bootstrap.ts là khung rỗng có ghi năm việc phải điền.
- (change) Sửa AD-1 của cha cho khớp AD-UI-1 của tầng 1: composition root nhánh máy là src/scan/bootstrap.ts dựng thể hiện mức module khi instrumentation.ts NHẬP ĐỘNG nó, không phải instrumentation.ts tự dựng. Lời nhập tĩnh ở tệp đó chạy trước guard NEXT_RUNTIME và kéo cả cây src/capability tới src/core/db.ts tới @prisma/client vào edge runtime.
- (constraint) CÒN CHẶN cho sáng 15/08: Docker daemon KHÔNG chạy, nên chưa kéo được ảnh postgres:16-alpine. Đây là thứ duy nhất tối nay còn chặn, vì §7.3 là docker compose up -d. Cần người dùng tự mở Docker Desktop rồi docker pull postgres:16-alpine.
- (change) Đồng bộ spine cha với thực trạng sau khi dựng khung. Bảng Stack: ghim SỐ THẬT cho bốn gói kiểm thử (jsdom 30.0.1, testing-library/react 16.3.2, testing-library/dom 10.4.1, vite-tsconfig-paths 6.1.1) và @vitejs/plugin-react 6.0.5 — bản trước ghi mới-nhất và ^28 ^17 ^11 ^6, npm bác cả bốn. Node đánh dấu ĐÃ NÂNG. Thêm hàng Fluent kèm cảnh báo 68 gói 51.725 tệp và câu ĐỪNG XOÁ node_modules.
- (constraint) npm 11.17 CHẶN install script của gói phụ thuộc theo mặc định — sau npm ci nó cảnh báo 3 packages have install scripts not yet covered by allowScripts, trong đó có prisma preinstall. Đã đo: next build và npm run lint VẪN XANH khi chúng bị chặn, nên không phải việc chặn. Nhưng nếu sáng 15/08 gặp lỗi lạ ở tầng Prisma thì đây là chỗ nhìn đầu tiên, lệnh là npm approve-scripts --allow-scripts-pending. Đã ghi vào mục Ba lệnh một-bước.
- (change) Cây nguồn khớp thực trạng: thêm src/scan/bootstrap.ts, prisma.config.ts, .env.example, public/, next-env.d.ts; sửa chú thích next.config.ts (bỏ outputFileTracingIncludes vì nó vô tác dụng với kiến trúc đã chốt), vitest.config.ts (fileParallelism false), docker-compose.yml (HAI service). Sơ đồ Triển khai thêm db_test tmpfs cổng 5433, nối vào nút npm test để nó không thành nút mồ côi.
- (change) PHẠM VI QUÉT CỦA ESLINT phải sửa lại trong AD-1. Câu cũ ghi quét toàn repo; thực tế eslint.config.mjs phải BỎ QUA _bmad-output, _bmad, docs — chúng được theo dõi trong git nên .gitignore không giúp. Đã đo: không có mấy dòng đó thì npm run lint ĐỎ với 2 lỗi 18 cảnh báo, toàn bộ từ _ds_bundle.js của nguyên mẫu Claude Design, một tệp không ai trong đội sở hữu. Sau khi thêm ignores thì lint EXIT 0.
- (event) eslint.config.mjs hiện tại CHƯA CÓ luật ranh giới nhập của AD-1 — mới chỉ có ignores và bộ mặc định của Next. Đã ghi chú tường minh ở đầu tệp rằng no-restricted-imports cộng no-import-type-side-effects phải cài sáng 15/08, kèm nhắc luật viết theo danh sách CHO PHÉP.
- (decision) NGƯỜI DÙNG ĐỔI KẾ HOẠCH: hôm nay 14/08 làm HOÀN THIỆN sản phẩm; ngày mai 15/08 chỉ chỉnh sửa và làm tính năng mới người ra đề công bố. Chuyển từ chế độ chuẩn bị sang chế độ dựng thật.
- (event) Docker Desktop đã mở. Phát hiện CỔNG 5432 BỊ CHIẾM bởi opms-db-1 (postgres:12.4) của dự án khác, tự khởi động cùng Docker. KHÔNG đụng vào nó — đổi cổng của mình sang 5442 (demo) và 5443 (kiểm thử). Đây cũng là rủi ro thật cho máy chấm: máy nào có sẵn Postgres là docker compose up -d đỏ ngay ở cổng nộp bài §7.3, nên dùng cổng riêng là quyết định chứ không phải chữa cháy.
- (constraint) PHÁT HIỆN MỚI, đã đo: Prisma 7 KHÔNG CÒN TỰ NẠP .env khi dùng prisma.config.ts. Thiếu process.loadEnvFile thì mọi lệnh migrate ném The datasource.url property is required in your Prisma config file — thông điệp trỏ vào prisma.config.ts chứ không vào .env, nên rất dễ đi lạc hướng. Dùng process.loadEnvFile của Node (>=20.6), không cần gói dotenv.
- (event) LƯỢC ĐỒ ĐẦY ĐỦ đã dựng và áp: 19 model, 12 enum, 20 bảng trong CSDL. Bốn CHECK và hai chỉ mục unique MỘT PHẦN viết tay trong migration vì Prisma không diễn đạt được. Đã KIỂM HÀNH VI chứ không chỉ tin migration: chèn hai Gợi ý chờ cùng ô thì Postgres ném duplicate key đúng tên chỉ mục; đổi cái cũ sang duyet thì cái mới vào được; hai Gợi ý target_field NULL cùng tồn tại đúng ý FR-51; và CHECK opp_latest_open_stage_iff chặn Cơ hội thang mà không có latest_open_stage.
- (change) TẦNG ⑤ đã dựng phần nền, tsc sạch: actor.ts (ba nhánh, Role đóng hai giá trị), errors.ts (GateDenied tách hẳn BusinessRuleError, 16 mã BR-D/NFR, FT1-FT10), db.ts (nơi DUY NHẤT nhập @prisma/client, extension lọc deleted_at, đường thoát dbIncludingDeleted có danh sách ba nơi dùng đóng, tx() mở giao dịch trong LÕI), config.ts (nơi duy nhất trong mã đội đọc process.env), normalize.ts (sáu phép biến đổi có thứ tự, loại NFKC và loại đụng U+3000 vì văn bản JP dùng cả hai có nghĩa, NORMALIZER_VERSION, findQuote so trên bản CHUẨN HOÁ), opportunity/stage.ts (tám chuyển tiếp hợp lệ, năm nhóm cấm, resumeOrReopen KHÔNG nhận giai đoạn đích), audit.ts (ghi HAI PHA, miễn trừ seed đặt ở nơi DỰNG chứ không thành nhánh if), settings.ts (11 khoá, đọc mỗi lần dùng không nhớ đệm), metrics.ts (hai chỉ số theo D30 của PRD, đếm Phát hiện PHÂN BIỆT không cộng bộ đếm sự kiện, sàn cỡ mẫu trả null chứ không trả 0).
