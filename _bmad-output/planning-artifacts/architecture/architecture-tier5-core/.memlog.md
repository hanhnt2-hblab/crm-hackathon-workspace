---
scope: Tầng ⑤ Lõi domain — src/core, prisma
purpose: Spine epic-altitude, thừa kế AD-1..AD-22
altitude: epic
updated: 2026-08-14T13:23
---

- (event) Bắt đầu — đã đọc spine cha (AD-1..AD-22, Consistency Conventions, Structural Seed), toàn bộ src/ontology/crm.ontology.md, PRD §5.1/§5.2/§5.3/§3.1/§3.2/§7, Mục 0 bảng 0.2.1–0.2.3 và D26/D30/D42/D43/D44.
- (constraint) AD-1 (cha) — src/core là nơi DUY NHẤT được nhập @prisma/client; không đường nào tới lõi mà không đi qua Capability; lọc deleted_at ở đúng một chỗ trong src/core.
- (constraint) AD-2 (cha) — sổ đăng ký Capability là tập đóng ba khối; lõi không tự khai thêm capability, và ghi vết KHÔNG nằm trong ba khối.
- (constraint) AD-3 (cha) — máy có đúng ba chạm ghi vào Hồ sơ chính thức; vị từ ba nhánh ⓐⓑⓒ của setNextAction là hợp đồng của lõi.
- (constraint) AD-4 (cha) — Cổng thuần; sổ đăng ký gom ngữ cảnh và ghi vết; writeAuditRow là nguyên thuỷ nội bộ của src/core, gọi thẳng, không qua Cổng, và không có nhánh nào bỏ ghi.
- (constraint) AD-5 (cha) — actor là tham số đầu tiên, ba nhánh human/system/seed, không ngữ cảnh ngầm, không mặc định.
- (constraint) AD-6 (cha) — src/agent không nhập src/core; lõi đối chiếu ontology §8; một phép kiểm so enum ontology với prisma/schema.prisma.
- (constraint) AD-7 (cha) — ra biên đúng ba thứ; lõi không bao giờ là nơi dựng lời nhắc.
- (constraint) AD-8 (cha) — bốn nguyên thuỷ; Claim buộc neo nguồn; đích so khớp câu trích là bản CHUẨN HOÁ, không phải bản thô.
- (constraint) AD-9 (cha) — BR-D thành ràng buộc CSDL cộng kiểm ở lõi, vi phạm NÉM LỖI; BR-B hai hạng: cờ trên hàng tính lúc ghi, chỉ số quần thể tính lúc đọc trong src/core/metrics.ts.
- (constraint) AD-10 (cha) — ghi với actor.kind='system' là kiểm-và-ghi nguyên tử, vế WHERE lặp lại TOÀN BỘ vị từ cho phép ghi; 0 dòng thì bỏ lượt ghi và ghi nhật ký, không thử lại.
- (constraint) AD-11 (cha) — trần lượt, ngân sách và năm điều kiện dừng nằm ở src/scan; lõi chỉ cung cấp settings và Nhật ký.
- (constraint) AD-12 (cha) — khoá vòng quét là hàng trong bảng account_lock có lease, không phải biến bộ nhớ; trạng thái 'đang chạy' suy từ khoá.
- (constraint) AD-13 (cha) — thứ tự phân xử: đề bài > Mục 0 > PRD > ontology > lược đồ, riêng HÌNH DẠNG LƯU TRỮ thì lược đồ thắng; định nghĩa hai chỉ số sống ở đúng src/core/metrics.ts và đối chiếu bảng D30.
- (constraint) AD-14 (cha) — mọi bảng có deleted_at, lõi không phát sinh DELETE; timestamptz UTC; ngày làm việc theo lịch thị trường của Công ty khách.
- (constraint) AD-15 (cha) — env chỉ chứa bí mật; mọi tham số nghiệp vụ nằm trong bảng settings và đọc MỖI LẦN DÙNG; lõi không có hằng số nghiệp vụ biên dịch cứng.
- (constraint) AD-16 (cha) — inference_cache khoá bằng hash đúng ba tham số của AD-7 cộng phiên bản lời nhắc, không chứa thời gian.
- (constraint) AD-17 (cha) — src/ingest/mapping.ts là nơi duy nhất biết tên trường nguồn; lõi không biết lược đồ BTC.
- (constraint) AD-18 (cha) — src/core/normalize.ts là nơi DUY NHẤT chuẩn hoá văn bản, thuần, không thấy thẻ HTML nào; Bản lưu lưu hai dạng; có cột normalizer_version.
- (constraint) AD-19 (cha) — sổ đăng ký phủ MỌI thao tác chạm dữ liệu, đọc lẫn ghi; nên mọi hàm đọc của lõi đều có capability đọc tương ứng.
- (constraint) AD-20 (cha) — actor 'seed' đi qua Cổng ở bước ②, được mọi capability ghi, bỏ qua vị từ chỉ có nghĩa lúc vận hành, và mặc định KHÔNG sinh ghi vết; cờ BR-B vẫn do lõi tính.
- (constraint) AD-21 (cha) — hệ quả dây chuyền chạy trong CÙNG giao dịch, CÙNG actor, ghi vết mang causedBy.
- (constraint) AD-22 (cha) — mô hình sinh Claim, hệ thống sinh Proposal; queueSuggestion suy xác định trong lõi và giữ bất biến bằng ràng buộc unique một phần trên (account_id, o, trạng thái 'cho').
- (decision) AD-CR-1 — Máy trạng thái Cơ hội cưỡng chế HAI LỚP: bảng chuyển tiếp là một hàm THUẦN trong src/core/opportunity/transitions.ts (nguồn sự thật duy nhất, ném lỗi có mã), cộng ba ràng buộc CSDL giữ HÌNH DẠNG mà hàm thuần không giữ nổi qua đường migration/sửa tay. Phương án đã loại: (a) chỉ hàm thuần — một câu UPDATE trong migration hay một lần sửa tay để lại Cơ hội 'thang' mà latest_open_stage rỗng, và đường mở lại chết im lặng; (b) chỉ ràng buộc CSDL — CHECK không thấy giá trị cũ nên không phát biểu được from→to, phải dùng trigger PL/pgSQL, mà trigger không mang được actor/vai, không ánh xạ được mã lỗi, và nằm ngoài tầm mắt của mọi phép kiểm TypeScript; (c) trigger + hàm thuần cả hai đầy đủ — hai bản luật phải đồng bộ tay, đúng thứ AD-13 dựng ra để chặn.
- (decision) AD-CR-1 — latest_open_stage (giai đoạn mở gần nhất) cập nhật theo đúng ba luật, đặt thành bất biến hai chiều: vào một giai đoạn ĐANG CHẠY thì đặt NULL; rời một giai đoạn đang chạy sang tam_dung/thang/thua thì ghi chính giai đoạn vừa rời; đi từ tam_dung sang thang/thua thì GIỮ NGUYÊN. Hệ quả: (stage thuộc tam_dung/thang/thua) đúng khi và chỉ khi latest_open_stage khác NULL — một CHECK duy nhất canh được cả ba luật. Phương án đã loại: giữ giá trị cũ khi quay lại giai đoạn đang chạy — không sai ngay, nhưng biến cột thành 'lịch sử một nửa' và làm CHECK hai chiều mất hiệu lực.
- (decision) AD-CR-1 — Mở lại và quay lại đi đúng MỘT hàm lõi resumeOrReopen(actor, id) KHÔNG nhận giai đoạn đích; đích luôn suy từ latest_open_stage. Phương án đã loại: dùng chung changeStage(actor, id, to) rồi kiểm to === latest_open_stage — cùng kết quả khi đúng, nhưng để lộ một tham số mà mọi lời gọi hợp lệ đều phải đoán đúng, và giao diện sẽ phải tự đọc cột đó.
- (decision) AD-CR-2 — Bảng phân bổ BR-D1..BR-D11: cái nào thành CHECK/UNIQUE/khoá ngoại, cái nào thành kiểm ở lõi, cái nào cả hai. Nguyên tắc phân bổ: ràng buộc CSDL chỉ nhận luật phát biểu được trên MỘT hàng hoặc một chỉ mục và KHÔNG cần tham số từ bảng settings (AD-15 cấm hằng số nghiệp vụ đông cứng ở nơi thứ hai). Vì thế BR-D7/BR-D8/BR-D9 nằm hoàn toàn ở lõi, còn BR-D1/BR-D3/BR-D4/BR-D10/BR-D11 có mặt ở cả hai.
- (decision) AD-CR-3 — Mã lỗi là TỪ VỰNG ĐÓNG, mỗi luật đúng một mã, mã bằng đúng mã luật (BR-D1..BR-D11, NFR-14..NFR-19) cộng năm mã máy trạng thái. BusinessRuleError mang code + detail máy đọc được, không mang chữ hiển thị (Consistency Conventions cha: lõi không chứa chuỗi hiển thị). Phương án đã loại: mã theo tầng HTTP (409/422) — tầng ① sẽ phải ánh xạ ngược để dựng thông điệp, và hai lỗi khác luật gộp về một mã.
- (decision) AD-CR-4 — Danh sách cờ mỗi capability làm bẩn khai ở SỔ ĐĂNG KÝ (tầng ④) theo đúng AD-9, nhưng lõi nhận id Cơ hội chứ không nhận danh sách cờ và TÍNH LẠI CẢ BỐN cờ BR-B1..BR-B4 trong cùng giao dịch. Tính siêu tập không bao giờ cho cờ sai, còn khai thiếu thì có. Danh sách khai vẫn có việc thật: một phép kiểm khẳng định mọi capability ghi chạm Opportunity/NextAction đều khai danh sách khác rỗng. Phương án đã loại: (a) lõi tin đúng danh sách khai — BR-B1 liên bảng (cờ ở Opportunity, sự thật ở NextAction) và BR-B4 gỡ miễn trừ khi rời tam_dung, nên người viết capability chỉ tính lại phần mình biết; (b) khai danh sách trong lõi — lõi sẽ phải biết tên capability, tức biết từ vựng của tầng ④.
- (decision) AD-CR-5 — metrics.ts: mẫu số error-detection rate = Phát hiện PHÂN BIỆT có ít nhất một phản hồi qua hai đường (nút hữu ích/không hữu ích của FR-42, CẢ HAI CHIỀU; hoặc một Gợi ý sinh từ nó bị Bỏ kèm lý do bất kỳ). Tử số = tập con bị bác vì SAI (không hữu ích, hoặc lý do thong_tin_sai). Phương án đã loại: mẫu số dùng đúng hai đường của tử số — tỉ lệ khi đó luôn bằng 1, chỉ số chết. Đây là xung đột sống mà AD-13 nêu tên, nay chốt tại đúng một tệp.
- (decision) AD-CR-5 — Cả ba tỉ lệ đếm theo THỰC THỂ PHÂN BIỆT, không theo lượt phản hồi: một Phát hiện vừa bị bấm 'không hữu ích' vừa có Gợi ý bị bỏ với thong_tin_sai đếm MỘT lần ở cả tử lẫn mẫu. Gợi ý đóng bởi hệ thống (system_close_reason khác NULL) đứng ngoài cả hai chỉ số — không ai quyết gì cả.
- (decision) AD-CR-5 — Thêm sàn cỡ mẫu metrics_min_sample (mặc định 10): dưới sàn thì trả tỉ lệ kèm cờ 'chưa đủ mẫu' và KHÔNG bật cảnh báo. Ở chế độ demo 60 giây, một Phát hiện duy nhất bị bác sẽ cho 100% và bật đèn đỏ trước mặt giám khảo.
- (decision) AD-CR-5 — Mẫu số BR-B5 là MỌI Phát hiện trong cửa sổ, không chỉ Phát hiện signal_type='other'. Phương án đã loại: chỉ đếm trong nhóm 'other' — mẫu số teo lại còn vài hàng nên tỉ lệ nhảy loạn, trong khi thứ cần đo là bao nhiêu phần đầu ra của mô hình rơi vào ngăn không dùng được. Ngưỡng unclassified_warn_ratio chưa có trong 0.2.2, đội đặt 0.30, sửa được ở màn hình Quản trị.
- (decision) AD-CR-6 — db.ts xuất HAI client: db (đã lọc deleted_at IS NULL qua Prisma extension trên mọi thao tác ĐỌC) và dbIncludingDeleted (không lọc, tên dài cố ý để grep ra ngay). Đường thoát có danh sách nơi dùng đóng: metrics.ts (D30 đòi mục đã xoá mềm vẫn làm dữ liệu đo), audit.ts (ghi vết không bao giờ lọc), reconcile.ts (báo cáo đối soát TR-3). Phương án đã loại: (a) một client kèm cờ includeDeleted trên mỗi lời gọi — cờ mặc định đúng thì người ta quên, và không grep ra được nơi nào đang đọc bản ghi đã xoá; (b) view SQL đã lọc — Prisma phải sinh model riêng cho view, gấp đôi kiểu cho mỗi bảng.
- (decision) AD-CR-6 — Extension KHÔNG chạm truy vấn ghi, nên mọi update/updateMany của lõi phải tự mang deleted_at: null trong vế WHERE; và extension NÉM LỖI ở mọi thao tác delete/deleteMany, biến lời hứa 'lõi không phát sinh DELETE' của AD-14 thành cơ chế thay vì kỷ luật.
- (decision) AD-CR-7 — Một thao tác ghi = MỘT giao dịch, sáu bước theo thứ tự cố định: ghi chính (kiểm-và-ghi khi actor là hệ thống) → trường dẫn xuất trong cùng câu UPDATE → hệ quả dây chuyền (AD-21) → tính lại cờ BR-B → mục Dòng thời gian → hoàn tất dòng ghi vết. Thứ tự này là quyết định: tính cờ TRƯỚC hệ quả dây chuyền sẽ cho cờ tính trên trạng thái giữa chừng; ghi mục Dòng thời gian trước ghi chính thì một BR-D ném lỗi sẽ để lại mục kể chuyện không xảy ra (nếu ai đó lỡ đặt nó ngoài giao dịch).
- (decision) AD-CR-8 — Ghi vết ghi HAI PHA trên một hàng: writeAuditRow ghi pha quyết định (không nhánh nào bỏ ghi, ghi cả CHO PHÉP lẫn TỪ CHỐI) và trả id; completeAuditRow ghi pha kết quả (giá trị cũ, giá trị mới, nhánh vị từ đã chạy, outcome) TRONG CÙNG giao dịch với thao tác ghi. Lý do: AD-4 đặt ghi vết TRƯỚC khi capability chạy, lúc đó chưa có giá trị mới, trong khi Consistency Conventions đòi dòng ghi vết mang giá trị cũ và giá trị mới. Phương án đã loại: (a) hai hàng cho một lời gọi — phá phép kiểm 'số dòng ghi vết bằng số lần loadCapability'; (b) dời ghi vết xuống sau khi chạy — mất dòng ghi vết của mọi lần bị TỪ CHỐI, phá AD-4.
- (decision) AD-CR-8 — Miễn ghi vết cho actor 'seed' (AD-20) cài ở NƠI DỰNG chứ không thành nhánh trong writeAuditRow: sổ đăng ký nhận auditSink chọn một lần lúc dựng (sink thật, hoặc sink rỗng khi seedMode bật và không có cờ giữ-ghi-vết). Nhờ vậy AD-4 ('không có nhánh nào bỏ ghi') và AD-20 ('seed không sinh ghi vết') cùng đúng, và phép kiểm đếm dòng vẫn phát biểu được trên từng sink.
- (decision) AD-CR-8 — outcome của dòng ghi vết là từ vựng đóng bốn giá trị: applied, rule_rejected, no_row_affected (AD-10 kiểm-và-ghi 0 dòng), denied. Giá trị no_row_affected là chỗ DUY NHẤT chứng minh được luật 'máy không ghi đè người vừa gõ' đã chạy thật.
- (decision) AD-CR-9 — normalize.ts làm ĐÚNG sáu phép biến đổi, theo thứ tự: CRLF/CR → LF; Unicode NFC; U+00A0 → khoảng trắng thường; gộp chuỗi khoảng trắng ngang; gộp từ ba dòng trống trở lên về hai; cắt khoảng trắng cuối mỗi dòng và hai đầu văn bản. Phương án đã loại: NFKC — nó gộp ký tự toàn rộng về nửa rộng, làm câu trích tiếng Nhật khác hẳn nguyên văn trên Bản chụp; và gộp U+3000 (khoảng trắng toàn rộng) về khoảng trắng thường, vì U+3000 mang nghĩa trong văn bản tiếng Nhật.
- (decision) AD-CR-9 — normalize phải LUỸ ĐẲNG (normalize(normalize(x)) === x), có phép kiểm khẳng định; nếu không thì offset câu trích lưu sẵn của AD-18 không ổn định. NORMALIZER_VERSION là số nguyên xuất từ chính tệp đó, ghi vào Bản lưu lúc tạo; đổi hàm là tăng số, và chỉ Phát hiện thuộc phiên bản cũ mới phải tính lại offset.
- (decision) AD-CR-10 — Lõi KHÔNG đọc actor.role để quyết định gì; vai chỉ vào dòng ghi vết. Điểm kiểm quyền duy nhất là Cổng bước ⑤ (AD-4, D28). Phương án đã loại: lõi kiểm lại vai cho đường mở lại Cơ hội (D43) — hai điểm kiểm quyền là hai chỗ trôi khỏi nhau, và PRD §6 đã kết luận cần đúng MỘT điểm kiểm. Ngoại lệ có chủ đích và không phải quyền: lõi vẫn từ chối actor.kind='system' cho mọi chuyển giai đoạn, vì đó là ranh giới NFR-14 chứ không phải vai.
- (decision) AD-CR-11 — Cột o (ô đích của Gợi ý) là enum ĐÓNG tám giá trị lấy từ bảng 0.2.3, và giá trị enum bằng ĐÚNG tên cột trên Account: website, country, specialty_area, revenue_range, industry, deal_value_tier, founded_year, account_type. Gợi ý loại 'thêm tin mới' mang o = NULL, nên chỉ mục unique một phần không chặn nhiều Gợi ý thêm tin cùng lúc (Postgres coi NULL là phân biệt) — đúng ý FR-51 vốn chỉ nói về Ô.
- (decision) AD-CR-11 — Ràng buộc unique một phần của AD-22 phải viết TAY bằng SQL trong migration: Prisma @@unique không diễn đạt được chỉ mục một phần. Vị từ gồm cả ba vế: trang_thai='cho' AND deleted_at IS NULL. Thiếu vế deleted_at thì một Gợi ý đã xoá mềm khoá vĩnh viễn ô đó và Công ty ấy không bao giờ nhận Gợi ý mới.
- (decision) AD-CR-12 — Xác nhận hoãn chỉ mục hiệu năng của cha, với lý do đã kiểm lại chứ không chép: 15 Công ty, 8 Cơ hội, và bảng lớn nhất trong 4,5 tiếng thi (audit_record ở chu kỳ quét 60 giây) cỡ vài nghìn hàng. Hai ngoại lệ KHÔNG phải hiệu năng nên không hoãn: chỉ mục unique một phần của AD-22 (đúng đắn), và unique trên account_lock.account_id (ngữ nghĩa lease của AD-12).
- (assumption) Account phải mang đủ tám cột của bảng 0.2.3 (website, country, specialty_area, revenue_range, industry, deal_value_tier, founded_year, account_type). Ontology §2 chỉ liệt bảy thuộc tính và thiếu sáu trong tám cột đó; coi danh sách của ontology là KHÔNG ĐẦY ĐỦ chứ không phải mâu thuẫn ngữ nghĩa — nếu không thì sáu trong tám ô đích của Gợi ý không có chỗ để ghi.
- (assumption) market (thị trường JP/Global/KR, đầu vào lịch ngày làm việc của BR-D7/AD-14) và country (ô đích Gợi ý ở bảng 0.2.3) là HAI cột khác nhau. Gộp chúng làm một thì một Gợi ý được duyệt sẽ đổi lịch ngày làm việc và mọi hạn tính lại lệch.
- (assumption) BR-D6 lệch quá một bậc thì KẸP về biên ±1 và ghi nhật ký F1, không loại bỏ Phát hiện. Loại bỏ sẽ đốt mất bằng chứng, mà Phát hiện là bằng chứng và không có đường xoá (ontology §9 mục 6).
- (question) ERD của cha vẽ AuditRecord }o--|| Account (bắt buộc đúng một Công ty). Dòng ghi vết của disableAi hay của một lần sửa settings không có Công ty nào, và dòng TỪ CHỐI thì thậm chí chưa biết bản ghi đích. Lược đồ tầng này khai account_id NULLABLE — nêu lên như xung đột, không tự sửa ERD cha.
- (question) Consistency Conventions của cha đòi dòng ghi vết mang 'giá trị cũ · giá trị mới', nhưng AD-4 đặt writeAuditRow ngay sau decide() và TRƯỚC khi capability chạy. Hai pha trên một hàng (AD-CR-8) là cách vá không nới AD nào; nếu cha không nhận thì phải chọn hy sinh một trong hai.
- (question) Cấu trúc của mục Dòng thời gian sinh từ đổi giai đoạn (PRD §5.1 cột 'Hành động kèm theo') chưa được khai ở đâu: nội dung, có phải enum không, ai là 'ai thêm'. Tầng ⑤ chốt tối thiểu: entry_kind='stage_change', payload mang from/to, actor như mọi mục khác.
- (event) Đã viết ARCHITECTURE-SPINE.md — 12 quyết định AD-CR-1..AD-CR-12, sơ đồ stateDiagram-v2 cho máy trạng thái Cơ hội, sơ đồ thứ tự sáu bước trong một giao dịch, mục Xung đột với spine cha gồm ba mục.
- (event) Ba sơ đồ mermaid đã dựng thử bằng mermaid-cli 11.16.0 (stateDiagram-v2, sequenceDiagram, graph TD) — cả ba ra SVG, không lỗi cú pháp.
- (event) lint_spine.py: total_findings 0.
- (event) spine finalized
- (event) Đồng bộ spine tầng ⑤ với bản cha 14/08 và với lượt hoà giải mặt cắt ③↔④. Đồng bộ, không dựng lại: mã AD-CR-1..AD-CR-12 giữ nguyên số; cơ chế hai pha của AD-CR-8 giữ NGUYÊN vì cha đã nhường cơ chế cho tầng này.
- (change) Bảng Inherited Invariants: AD-2 liệt hạng ghi CAP_MAY_DUOC đúng SÁU và CAP_HE_THONG_NOI_BO đúng NĂM mục gồm recordAccountCost và readUserForAuth, kèm trỏ tới AD-CR-13. AD-4 thêm phép đếm ghi vết theo decide, chữ ký createRegistry({seedMode,auditSink}), quyền sở hữu GateEntry/GateContext của tầng ③ với RegistryEntry extends GateEntry, và luật selfLimiting năm mục bắt buộc. AD-5 khai kiểu Actor đầy đủ với role: 'sales'|'admin'. AD-1 thêm sàn eslint >= 9.37.0 vì ranh giới src/core do no-restricted-imports canh.
- (change) AD-CR-8: thêm khối trích dẫn ghi rõ cha đã nhường CƠ CHẾ cho tầng ⑤ (cha chốt ba bất biến, tầng ⑤ sở hữu hình dạng hai pha một hàng) — đây là thắng lợi của tầng này, không phải một ngoại lệ được tha. Sửa phép đếm từ 'số lần loadCapability' thành 'số lần decide được gọi' theo cha, kèm lý do: collectGateContext chạy TRƯỚC decide và có thể ném, nên cách nói cũ làm phép kiểm đỏ vĩnh viễn sau một lần Postgres chớp. Cột actor_role đổi enum sales/quan_tri thành sales/admin.
- (change) AD-CR-10: thêm khối mã khai kiểu Actor và Role đầy đủ, chốt tên trường là role (không phải vai) và giá trị là 'admin' (không phải 'quan_tri'), kèm lý lẽ: đây là định danh trong mã nên tiếng Anh, còn ngoại lệ 'giá trị enum giữ tiếng Việt không dấu' chỉ áp cho enum nghiệp vụ của Mục 0. Tiêu đề AD đổi theo. Consistency Conventions ghi role là ngoại lệ thứ hai bên cạnh enum o.
- (decision) Thêm AD-CR-13 ở CUỐI dải, không đánh số lại: readUserForAuth mà cha vừa thêm đọc bảng người dùng, mà lược đồ AD-CR-11 KHÔNG có bảng nào như vậy — audit_record.actor_user_id là khoá ngoại không có đích. Tầng này dựng bảng user tối thiểu (id, email unique một phần, display_name, role enum sales/admin, source_ref, deleted_at), enum role dùng chung một khai báo với Actor.role và audit_record.actor_role. TR-2 gieo đúng hai hàng, không capability ghi nào.
- (constraint) Còn ngỏ trong AD-CR-13, cần cha quyết: cách xác thực. Lược đồ cố ý KHÔNG có cột mật khẩu — đề bài không có yêu cầu xác thực, TR-2 chỉ nói gieo hai tài khoản, và password_hash kéo theo bcrypt cộng luồng đổi mật khẩu giữa 4,5 tiếng. Ba đường: (1) chọn người dùng không mật khẩu — ĐANG DỰNG, đủ cho mọi T và giám khảo đổi vai bằng một lần bấm; (2) thêm password_hash — đúng nghĩa hơn nhưng không T nào chấm; (3) hai actor là hằng số trong mã — rẻ nhất nhưng phá AD-UI-10 của tầng ① và làm actor_user_id trỏ vào hư không.
- (change) Mục Xung đột với spine cha: mục 1 (thời điểm ghi vết) đánh dấu ĐÃ ĐÓNG vì cha đã nhường cơ chế; thêm mục 4 MỚI và CHẶN — bảng người dùng cho readUserForAuth, kèm ba đường xác thực để cha chọn một. Thêm mục '## Xung đột với spine anh em' ba dòng: ① từ vựng vai sales/quan_tri thua từ vựng cha vì gate.ts gõ Actor qua ngoại lệ chỉ-kiểu nên lệch là lỗi biên dịch ở tầng ③; ② phép đếm ghi vết, cả tôi lẫn tầng ④ đều đổi theo cha; ③ tên readUserForAuth và quyền sở hữu bảng user. Ghi rõ không có va chạm với tầng ②.
- (change) Sửa vặt: cây nguồn thêm src/core/user.ts và dòng bảng user trong schema.prisma; Capability -> Architecture Map thêm hai dòng readUserForAuth và nhóm capability hạ tầng (ghi rõ hạch toán và bộ đếm là việc của tầng ① theo AD-UI-21); frontmatter binds thêm TR-2, TR-3, T-9. Lint: total_findings 0. Kiểm tay AD-CR-1..AD-CR-13 liên tục, mỗi khối đủ Binds/Prevents/Rule.
